:= Backlog — MIAMI (slider-gradle)
:description: Resume du backlog — source de verite : slider-plugin/.agents/INDEX.adoc
_.date: 2026-08-06
:toc: left
:sectnums:

_Source de verite_ : link:slider-plugin/.agents/INDEX.adoc[INDEX.adoc] — ce fichier est un resume.

'''
== Session 067 (2026-08-07) — Aplatissement structure `slider-plugin/slider/` → `slider-plugin/` ✅ TERMINÉ

Fix structure repo (aucun EPIC feature concerné — session architecture).
Le dossier `slider-plugin/slider/` (sous-projet Gradle `include("slider")`)
violait le contrat d'architecture « Plugin Indépendant + Racine
Consommateur » (article 0124) : `{name}-plugin/` doit être un build
autonome avec `src/` à sa racine et zéro `include()` / zéro sous-projet.
slider-gradle était le SEUL borough avec cette indirection — héritage du
premier commit `d3aacdd`.

211 fichiers déplacés via `git mv` (rename detection, historique préservé) :
`slider-plugin/slider/{build.gradle.kts, src, gradle.properties}` →
`slider-plugin/` + fusion `.gitignore`. Suppression `include("slider")` —
build mono-projet autonome conforme au pattern (bakery/codex/planner/
capsule/document). Chemins fixtures Playwright `../../build/` → `../build/`.
5 refs `:slider:` nettoyées. Path Gradle `:slider` → `:`. Zéro test
supprimé/modifié fonctionnellement. `./gradlew check` BUILD SUCCESSFUL
10m36s, zéro régression. Version 0.0.16 inchangée. Commit + push (clôture
pilote).

Rapport complet : `slider-plugin/.agents/sessions/067-aplatir-slider-plugin-structure.adoc`.

'''
== Session 066 (2026-08-06) — Publication slider 0.0.16 + BOM 0.0.17 + sync capsule ✅ TERMINÉ

Session pure ops/release (zéro code de production modifié, zéro test
modifié). Publication slider 0.0.16 sur Maven Central via nmcp + BOM
0.0.17 (constraint slider 0.0.16) + synchronisation consommateur
capsule-gradle. Reflète le delta non publié accumulé depuis 0.0.15
(S-059) : AssistantManager réécrit 652→300 lignes -352 (US-8.2 S-060 —
délégation `LlmBuildService` codebase N1 via `LlmProviderChatModelAdapter`,
suppression 3 factories + 4 streaming + 8 hello* tasks) + domaine
`slider.pipeline` complet (US-8.3 baby-steps 8.3a-d S-062→065 —
`DeckState`, `DeckContextValidator`, `DeckPipelineGraph` koog,
`GenerateDeckPipelineTask`, Cucumber BDD) + dépendance `koog-agents:1.0.0`
directe slider.

Bundle nmcp slider `106d110f-27de-43c1-869e-9e9a70026f5d` (AUTOMATIC, 3
artefacts signés GPG : slider:0.0.16 POM+JAR+sources+javadoc + 2 plugin
markers). BOM 0.0.17 publiée (bundle `b432cc13-f08b-489c-8f67-c80d9d5d8b04`
AUTOMATIC, constraint slider 0.0.16 — BOM 0.0.16 publiée S-062 gardait
constraint 0.0.15). Sync capsule-gradle (catalogue slider 0.0.15→0.0.16,
`./gradlew compileKotlin` VERT avec slider 0.0.16 mavenLocal). Vérification
repo1.maven.org : POM slider 0.0.16 + BOM 0.0.17 HTTP 200 (après ~6 min
propagation). `./gradlew :slider:check` VERT pré-pub (UP-TO-DATE 22s,
959 unit + 45 functional + 265 cucumber, 0 FAIL, 1 SKIP préexistant).

4 fichiers build bumpés sur 3 repos (slider, BOM, capsule) — non
committés (Règle 1, accord pilote pour publish mais pas pour commit).
Secret-scan fin de session : zéro match dans le code de production.

Rapport complet : `slider-plugin/.agents/sessions/066-pub-slider-0.0.16.adoc`.

'''
== Session 065 (2026-08-06) — SLD-8.3d Cucumber `26_deck_pipeline_koog.feature` ✅ US-8.3 clôturée

SLD-8.3 baby-step 8.3d livré en TDD/BDD strict (RED → GREEN → REFACTOR).
*Pure BDD* — zéro fichier de production modifié (pattern SLD-10.6 / S-054 :
logique métier déjà livrée par 8.3a/b/c, le baby-step 8.3d ajoute uniquement
la couverture BDD end-to-end). 2 fichiers test créés :
`26_deck_pipeline_koog.feature` (12 scénarios BDD end-to-end, tags
`@pipeline @koog`, English only Gherkin) + `DeckPipelineKoogSteps.kt`
(222 lignes, package `slider.steps`, Java8 lambda `En`, fakes
`RecordingPromptBuilder` + `FakeDeckLlm` privés en mémoire mirroring
`DeckPipelineGraphTest` 8.3b + `DeckTranslationSteps` SLD-5).

12 scénarios : happy path (3 — pipeline complet génère deck, deckContextJson
preserved, subject/language/ragContext preserved), invalid context → FAILED
(3 — subject manquant, duration 0, languageCode "xx" non supporté), LLM
failure modes (3 — propose throws ProposeContextFailed, propose blank,
generate throws GenerateDeckFailed avec contextValid true), edge conditions
(3 — generate non appelé si context invalide, transitions stage
INITIALIZED→CONTEXT_VALIDATED via RecordingPromptBuilder, asMermaidDiagram
non-blank). Domaine `slider.pipeline` inchangé (10 fichiers prod + 4 fichiers
test de 8.3a/b/c). `./gradlew :slider:check` VERT (959 unit inchangés, 45
functional inchangés, 265 cucumber +12, 0 FAIL, 1 SKIP préexistant). Zéro
régression.

**US-8.3 clôturée** (4/4 baby-steps : 8.3a 35 unit S-062, 8.3b 17 unit S-063,
8.3c 7 functional + 11 i18n S-064, 8.3d 12 cucumber S-065 = 71 tests 3
niveaux, ~9 pts). **EPIC SLD-8 terminé** (US-8.0/8.1/8.2/8.3 ✅, US-8.4
caduque fusionné dans 8.3a-d). Domaine `slider.pipeline` clos (10 fichiers
prod + 4 fichiers test).

Rapport complet : `slider-plugin/.agents/sessions/065-sld-8.3d-deck-pipeline-koog-cucumber.adoc`.

'''
== Session 064 (2026-08-06) — SLD-8.3c GenerateDeckPipelineTask ✅ TERMINÉ

SLD-8.3 baby-step 8.3c livré en TDD strict (RED → GREEN → REFACTOR). Domaine
`slider.pipeline` enrichi (3 fichiers prod) : `PromptManagerDeckPromptBuilder.kt`
(adaptateur `DeckPromptBuilder` ← `AssistantManager.PromptManager` — assemblage
system+user prompts des 2 étapes, parse `deckContextJson` via
`SliderConfig.yamlMapper`), `ChatModelDeckLlm.kt` (adaptateur `DeckLlm` ←
`ChatModel` langchain4j — prompt complet envoyé comme `UserMessage` unique),
`GenerateDeckPipelineTask.kt` (thin Gradle adapter `generateDeckPipeline` —
`@ServiceReference llmService`, lit `-Psubject`/`-Planguage`/`-Pauthor.*`/
`-Pdeck.output`/`-Pdeck.ragContext`/`-Pai.provider`, instancie
`DeckPipelineGraph`, exécute, écrit `.adoc`, RAG-free, mock-LLM fallback
`-Pollama.baseUrl` préservé). Câblage `AssistantManager.createChatTasks` →
`registerGenerateDeckPipelineTask`. Constante `TASK_GENERATE_DECK_PIPELINE`.
Clé i18n `task.generateDeckPipeline.description` ajoutée aux 10
`Messages_*.properties` + 11 tests i18n + `TaskDescriptionsI18nFunctionalTest`.
7 functional tests GradleTestKit. `./gradlew :slider:check` VERT (959 unit +10,
45 functional +7, 254 cucumber = 1258, 0 FAIL, 1 SKIP préexistant). Zéro
régression. Domaine `slider.pipeline` : 10 fichiers prod + 4 test.

Rapport complet : `slider-plugin/.agents/sessions/064-sld-8.3c-generate-deck-pipeline-task.adoc`.

'''
== Session 063 (2026-08-05) — SLD-8.3b DeckPipelineGraph ✅ TERMINÉ

SLD-8.3 baby-step 8.3b livré en TDD strict (RED → GREEN → REFACTOR). Domaine
`slider.pipeline` enrichi (3 fichiers prod purs) : `DeckPromptBuilder.kt`
(port — prompts propose/generate depuis `DeckState`), `DeckLlm.kt` (port —
`propose`/`generate` synchrones mockable sans coroutine), `DeckPipelineGraph.kt`
(graphe koog `strategy<DeckState, DeckState>` 3 nœuds proposeContext/
validateContext/generateDeck + edges conditionnels `validateContext→generateDeck
onCondition{it.contextValid}` / `validateContext→nodeFinish onCondition{!
it.contextValid}`, méthode `execute` séquentielle try/catch par nœud pattern
`codebase.koog.KoogAugmentedContextGraph`, `asMermaidDiagram()`). 17 unit tests
purs (happy path DECK_GENERATED, context invalide→FAILED, LLM propose/generate
throws→FAILED, appels prompt builder + LLM forwarding, transitions stage,
asMermaidDiagram non-blank, préservation subject/language/ragContext).
Dépendance `koog-agents:1.0.0` ajoutée directe slider (BOM 0.0.16 constraint,
exclude `org.jetbrains:annotations` pour conflit testcontainers).
`./gradlew :slider:check` VERT (949 unit +17, 38 functional, 254 cucumber
= 1241, 0 FAIL, 1 SKIP préexistant). Zéro régression.

Rapport complet : `slider-plugin/.agents/sessions/063-sld-8.3b-deck-pipeline-graph.adoc`.

'''
== Session 062 (2026-08-05) — SLD-8.3a DeckState + DeckContextValidator ✅ TERMINÉ

SLD-8.3 baby-step 8.3a livré en TDD strict (RED → GREEN → REFACTOR). Domaine DDD
`slider.pipeline` créé (16ème domaine). 4 fichiers prod purs : `DeckStage.kt`
(enum 5 stages), `DeckState.kt` (data class immutable 11 champs + invariants
croisés), `ValidationResult.kt` (sealed Valid/Invalid), `DeckContextValidator.kt`
(object pur `validate(json): ValidationResult` — 8 checks ordonnés, Jackson +
LanguageCatalog N0). 35 unit tests purs (17 DeckStateTest + 18
DeckContextValidatorTest). `./gradlew :slider:check` VERT (932 unit +32, 38
functional, 254 cucumber = 1224, 0 FAIL, 1 SKIP préexistant). Zéro régression.
Publication BOM 0.0.16 sur Central (bundle `e12c25b5` AUTOMATIC) — prérequis
débloqué.

Rapport complet : `slider-plugin/.agents/sessions/062-sld-8.3a-deck-state-validator.adoc`.

'''
== Session 061 (2026-08-05) — Cadrage formel SLD-8.3 DeckPipelineGraph ✅ TERMINÉ

Cadrage formel US-8.3 `DeckPipelineGraph` (koog orchestration). Zéro code modifié —
session de vision et découpage baby-step. Patterns koog 1.0.0 consommés depuis
`office/pilotage/codebase/KOOG_AGENTIC_PATTERNS.adoc` (15 patterns, architecture
"koog orchestre / langchain4j exécute"). Audit codebase : `KoogAugmentedContextGraph`
(pattern hybride graphe koog + `execute()` séquentiel), `AugmentedState` (data class
immutable), `LlmProvider` (fun interface suspend), `LlmBuildService` (BuildService Gradle).

Domaine cible : `slider.pipeline` (nouveau), `DeckState` (data class immutable, 11 champs),
`DeckContextValidator` (validation pure, sealed Valid/Invalid), `DeckPipelineGraph`
(graphe koog `strategy<DeckState, DeckState>` — 3 nœuds propose-context/validate-context/
generate-deck + edges conditionnels), `GenerateDeckPipelineTask` (tâche Gradle thin
`generateDeckPipeline`).

4 baby-steps TDD/BDD prévus (~9 pts) : 8.3a DeckState+Validator (20-25 unit) → 8.3b
DeckPipelineGraph (15-20 unit mock LLM) → 8.3c GenerateDeckPipelineTask (5-8 functional) →
8.3d Cucumber `26_deck_pipeline_koog.feature` (8-12 scénarios). Non-périmètre : pas de
retry/boucle, pas de Checkpoints, pas de Self-Reflection, pas de modification des tâches
existantes (rétrocompatibilité). Publication BOM 0.0.16 + codebase 0.0.5 Central en attente.

Rapport complet : `slider-plugin/.agents/sessions/061-cadrage-sld-8.3.adoc`.

'''
== Session 060 (2026-08-05) — SLD-8 US-8.2 AssistantManager câblage codebase ✅

SLD-8 US-8.2 livrée en baby-step TDD/BDD 3 niveaux. `AssistantManager.kt`
réécrit (652 → 300 lignes, -352) : suppression 3 factories gemini/mistral/
huggingface + 4 streaming + 8 hello* tasks + catalogues. `resolveModel
(provider, serviceProvider)` délègue à `LlmBuildService` (codebase N1)
wrappé dans `LlmProviderChatModelAdapter` (nouveau, 48l, `LlmProvider`
suspend → `ChatModel` langchain4j via `runBlocking`). Injection
`@ServiceReference llmService` sur `RagTask` + `TranslateDeckTask` +
`registerLlmBuildService()`. Mock-LLM fallback (`-Pollama.baseUrl` →
`createOllamaChatModel` legacy) préserve 9 scénarios Cucumber
`01_propose_deck_context`. `TranslatorManager` déprécié conservé (expulsion
future EPIC SLD-CR4). Cross-borough : `codebase-plugin:0.0.5` republié
mavenLocal (JAR S-176 n'incluait pas LlmBuildService). US-8.2a (catalog +
build), US-8.2b (LlmProviderChatModelAdapter TDD 5 unit), US-8.2c
(AssistantManager réduit), US-8.2d (RagTasks + TranslateDeckTask câblés).
`./gradlew :slider:check` VERT (900 unit +5, 38 functional -2, 254 cucumber
= 1192 total, 0 FAIL, 1 SKIP préexistant).

Rapport complet : `slider-plugin/.agents/sessions/060-sld8-assistant-manager-wiring.adoc`.

'''
== Session 059 (2026-08-05) — Publication slider 0.0.15 + Coordination CAP-MODEL ✅

Publication slider 0.0.15 (post-SLD-11 expulsion `slider.capsule`) + BOM 0.0.15 +
sync capsule. Bump 0.0.14 → 0.0.15 sur 4 fichiers (slider build, BOM build, BOM
catalogue, capsule catalogue). Bundle nmcp slider `832cea2a` (AUTOMATIC, 3 artefacts
signés GPG) + BOM `02307b00` (AUTOMATIC). `./gradlew :slider:check` VERT pré-pub
(895 unit + 40 functional + 254 cucumber). Zéro code production modifié côté slider.

Coordination capsule 069 : EPIC CAP-MODEL TERMINÉ (unification modèle capsule.feed,
côté capsule seul — 7/7 US, +124 unit tests, `CapsuleScriptReader` remplace
`CapsuleManager.parseScript`, `capsule.CapsuleModels` purgé). Slider inchangé.

Rapport complet : `slider-plugin/.agents/sessions/059-publish-slider-0.0.15.adoc`.

'''
== Session 057 (2026-08-05) — EPIC SLD-9 i18n Completion TERMINÉ (9.2/9.3/9.4) ✅

EPIC SLD-9 (i18n Completion via LLM) TERMINÉ — 5/5 US (9.0/9.1 S-047 +
9.2/9.3/9.4 cette session). Périmètre restrictif respecté : i18n only, zéro
refacto, zéro production modifié. Pattern plantuml-gradle appliqué fidèlement.

SLD-9.2 — 9 classes traduction par langue (8 Messages{Zh,Hi,Es,Ar,Bn,Pt,Ru,Ur}
TranslationTest + MessagesEnReferenceTest), 39 clés véritablement traduisibles
(12 clés identiques cross-langue exclues). +366 tests unitaires.

SLD-9.3 — `TaskDescriptionsI18nFunctionalTest` (GradleRunner, 20 tests). 17
tâches i18nisées × 10 langues + 10 tests spécifiques EN/FR. +20 functional.

SLD-9.4 — SliderMessagesTest étendu (29→55) + `25_slider_messages_resolution.feature`
(31 scénarios BDD) + `SliderMessagesResolutionSteps`. +26 unit +31 cucumber.

Tests : 977 unit / 46 functional / 279 cucumber = 1302 total (was 859, +443).
`./gradlew :slider:check --rerun-tasks` VERT 0 FAIL / 1 SKIP préexistant.

Rapport complet : `slider-plugin/.agents/sessions/057-sld-9-completion.adoc`.

'''
== Session 058 (2026-08-05) — EPIC SLD-11 Expulsion slider.capsule → capsule ✅ TERMINÉ

EPIC SLD-11 (expulsion `slider.capsule` → capsule) TERMINÉ — 6/6 US (11.0 cadrage
S-056 + 11.1→11.6). Cross-borough (slider + capsule modifiés en coordination),
non-regression stricte (tests portés côté capsule avant suppression côté slider).
Boundary realignment : slider produit HTML Reveal.js uniquement, capsule parse
`.adoc` directement (format `*-script.txt` supprimé côté slider, recréé côté
capsule via `extractSpeakerNotes`).

SLD-11.1 : domaine `capsule.feed` côté capsule (3 fichiers prod) + 27 unit tests
portés. SLD-11.2 : tâche `extractSpeakerNotes` côté capsule + 13 unit + 4 functional.
SLD-11.3 : Cucumber `capsule_speaker_notes.feature` (13 scénarios). SLD-11.4 :
pipeline `translateAndExtractSpeakerNotes` + 2 unit + 2 functional + 3 cucumber.
SLD-11.5 : expulsion côté slider — 4 prod + 8 test + 3 features + 3 steps +
2 functionalTest supprimés, câblage + KDoc + 11 `.properties` + 10 tests i18n
nettoyés. SLD-11.6 : `generateCapsuleScript` dépend de `extractSpeakerNotes` +
2 functional + 1 cucumber. `parseScript` conservé (rétrocompatibilité pipeline vidéo).

Tests slider : 895 unit (was 977, -82) / 40 functional (was 46, -6) / 254 cucumber
(was 279, -25) = 1189 total (was 1302, -113). `./gradlew :slider:check` VERT 0 FAIL
/ 1 SKIP préexistant. Capsule-gradle : +42 unit +8 functional +16 cucumber = 66
nouveaux tests, 0 FAIL (1 failure préexistant burnin ffmpeg non lié).

Rapport complet : `slider-plugin/.agents/sessions/058-sld-11-expulsion-capsule.adoc`.

'''
== Session 056 (2026-08-05) — Cadrage EPIC SLD-11 ✅ TERMINÉ

Cadrage formel EPIC SLD-11 — Expulsion `slider.capsule` : realignment boundary
slider ↔ capsule. Audit cross-borough (slider + capsule) revelant une fuite de
responsabilite : le domaine `slider.capsule` (4 fichiers production + 8 fichiers
test, 1251 lignes) fait du travail de capsule (preparation de script video) a
l'interieur de slider. Slider doit s'arreter au HTML Reveal.js ; l'extraction
des notes orateur et la serialisation `*-script.txt` sont des preoccupations de
capsule.

*Symptomes* : doublon de modele divergent (`SlideSegment`/`CapsuleScript` avec
champs differents), format intermediaire proprietaire maintenu des deux cotes,
3 features Cucumber + 5 classes test unitaire slider testent une logique non-
slider.

*Boundary cible* : capsule parse `.adoc` directement (nouveau domaine
`capsule.feed`), slider s'arrete au HTML Reveal.js, format `.txt` supprime,
`generateCapsule`/`translateAndGenerateCapsule` migrent vers capsule-side.

6 US ~14 pts, methode baby-step DDD/TDD/BDD 3 niveaux (unit → functional →
cucumber), English only. Non-regression stricte (tests portes avant suppression
source). Cross-borough (2 repos modifies en coordination).

Rapport complet : `slider-plugin/.agents/INDEX.adoc` § EPIC SLD-11.

'''
== Session 054 (2026-08-05) — SLD-10.6 BDD Content Validation ✅ TERMINÉ (EPIC SLD-10 TERMINÉ)

US SLD-10.6 (Cucumber `24_slide_content_validation.feature`) livrée en
baby-step TDD/BDD (RED → GREEN → REFACTOR). 2 fichiers test créés :
feature `24_slide_content_validation.feature` (11 scénarios BDD end-to-end,
tags `@content @validation`) + `ContentValidationSteps.kt` (141 lignes,
package `slider.steps`, Java8 lambda style `En`). 3 groupes de steps : a
priori (`ContentSlideAssertion` sur `SlideContent`+`SlideLayout` — 5
scénarios), a posteriori (`ContentRenderAssertion` sur `ContentRenderData`
snapshot DTO — 5 scénarios), cross-validation (1 scénario). Zéro fichier de
production modifié (pure BDD — logique métier déjà livrée S-051/053). 237 →
248 Cucumber (+11 scénarios : 5 a priori + 5 a posteriori + 1
cross-validation). 585 unitaires (inchangé). 0 FAIL, 1 SKIP préexistant.
`./gradlew :slider:check` VERT. Zéro régression. **EPIC SLD-10 TERMINÉ**
(7/7 US — 10.0/10.1/10.2/10.3/10.4/10.5/10.6 ✅). Domaine `slider.content`
clos (9 fichiers prod + 8 fichiers test).

'''
== Session 053 (2026-08-05) — SLD-10.5 Content Render Validation ✅ TERMINÉ

US SLD-10.5 (Assertions Playwright enrichies) livrée selon cadrage S-052.
3 baby-steps TDD (RED → GREEN → REFACTOR), 3 niveaux tests (unit →
functional). Domaine `slider.content` enrichi (2 nouveaux fichiers prod) :
`ContentRenderData.kt` (snapshot DTO pur, mirroring `SlideRenderData` RTL,
8 champs, invariants `init`, factory `EMPTY` avec bypass via paramètre
privé `bypassInvariants`) + `ContentRenderAssertion.kt` (object pur,
`assertAll(ContentRenderData): ContentAssertionResult`, réutilise les 4
codes `ContentAssertionCode` existants, délègue overflow à
`OverflowDetector.detect` sur les `TextBlock` réels, nouvelle constante
`MIN_CONTRAST_RATIO = 4.5` WCAG AA). Adaptateur Playwright
`ContentPlaywrightAdapter.kt` (src/test, object, extrait snapshot depuis
`Page` réelle — `extractTextBlocks`/`computedFontSize`/`computedContrast`
JS injecté WCAG luminance). Functional test LTR
`ContentPlaywrightFunctionalTest.kt` (port 4337, fixture
`capsule-feed-demo-en-deck.html`, réutilise `PlaywrightFixtureSupport`
S-034). 17 unit + 1 functional = +18 tests. 585 unitaires (was 567), 0
FAIL, 1 SKIP préexistant. `./gradlew :slider:check` VERT. Zéro régression.

'''
== Session 052 (2026-08-05) — Cadrage exhaustif SLD-10.5 ✅ TERMINÉ

Cadrage formel US SLD-10.5 (Assertions Playwright enrichies) de l'EPIC
SLD-10. Zéro code modifié — session de vision et découpage. Audit Playwright
cross-borough (6 boroughs : slider, bakery, capsule, container-provision,
magic-stick, workspace-bom) confirmant l'absence de contrat Playwright
partagé et le pattern snapshot DTO `SlideRenderData` unique à slider (gold
standard du workspace).

Décision architecturale clé : SLD-10.5 est l'**adaptateur Playwright** de
SLD-10. Il introduit `ContentRenderData` (snapshot DTO pur, mirroring
`SlideRenderData` RTL) contenant les mesures réelles extraites d'une `Page`
Playwright (bounding boxes, font sizes calculées, contraste WCAG, présence
notes DOM). Un nouvel object pur `ContentRenderAssertion` consomme ce
snapshot et délivre `ContentAssertionResult` (sealed existant) en
réutilisant les 4 codes de `ContentAssertionCode` (pas de nouveau code).

Deux objects d'assertion coexistent : `ContentSlideAssertion` (S-051,
estimation a priori sur `SlideContent`+`SlideLayout`) et
`ContentRenderAssertion` (S-052, mesure a posteriori sur
`ContentRenderData`). Les 4 codes sont partagés. Nouvelle constante
`MIN_CONTRAST_RATIO = 4.5` (WCAG AA) — vit sur `ContentRenderAssertion` car
le contraste réel n'est mesurable qu'après rendu.

3 baby-steps TDD prévus : (1) `ContentRenderData` snapshot DTO + 8 unit,
(2) `ContentRenderAssertion` object pur + 9 unit, (3)
`ContentPlaywrightAdapter` + `ContentPlaywrightFunctionalTest` (LTR fixture,
port 4337, réutilise `PlaywrightFixtureSupport`). 17 unit + 1 functional
attendus. SLD-10.6 (Cucumber `24_slide_content_validation.feature`) à
planifier ensuite.

Rapport complet : `slider-plugin/.agents/sessions/052-sld-10.5-cadrage.adoc`.

'''
== Session 051 (2026-08-05) — SLD-10.4 ContentSlideAssertion ✅ TERMINÉ

US SLD-10.4 livrée — `ContentSlideAssertion` + codes assertions P0/P1.
Domaine `slider.content` enrichi (3 nouveaux fichiers) :
`ContentAssertionCode.kt` (enum P0_OVERFLOW/P0_MISSING_NOTES/P1_FONT_SIZE/
P1_CONTRAST), `ContentAssertionResult.kt` (sealed Passed/Failed +
`ContentAssertionFailure`), `ContentSlideAssertion.kt` (object `assertAll
(content, layout): ContentAssertionResult`, délègue overflow à
`OverflowDetector` via `TextBlock` synthétiques, constantes
`MIN_BODY_FONT_SIZE=14.0` + `MIN_TITLE_BODY_RATIO=1.25`). Pattern de
référence `slider.rtl.RtlSlideAssertion` (sealed result pour `when`
exhaustif). 11 nouveaux tests unitaires. 567 unitaires (was 556), zéro
régression. `./gradlew :slider:check` VERT.

== Session 050 (2026-08-05) — SLD-10.3 AsciidocContentParser ✅ TERMINÉ

US SLD-10.3 livrée — `AsciidocContentParser` (parseur `.adoc` → `SlideContent`).
Domaine `slider.content` enrichi (2 nouveaux fichiers) : `SlideContent.kt`
(aggregate root : title, subtitles, paragraphs, lists, speakerNote) +
`AsciidocContentParser.kt` (parseur ligne par ligne `parseDeck(adoc):
List<SlideContent>`, étend `AsciidocSpeakerNoteParser` aux titres `==`,
sous-titres `===`, paragraphes, listes `* `, notes `[NOTE.speaker]`). 34
nouveaux tests unitaires (12 SlideContent + 22 AsciidocContentParser). 556
unitaires (was 522), zéro régression. `./gradlew :slider:check` VERT.

== Session 049 (2026-08-05) — Cadrage SLD-10 + SLD-10.1 + SLD-10.2 ✅ TERMINÉ

Cadrage formel EPIC SLD-10 — Slide Content Model & Rendering Validation.
Domaine DDD `slider.content` modélisant le contenu d'une slide AsciiDoc et des
assertions de rendu Playwright pour valider que le texte est dans l'écran,
que les notes orateur sont bien rendues, et que la lisibilité est correcte.
7 US, ~18 pts. Pattern de référence `slider.rtl.RtlSlideAssertion` +
`slider.capsule.AsciidocSpeakerNoteParser`. Deux US livrées en baby-step TDD :
SLD-10.1 (`TextBlock` + `SlideLayout`, 20 unit) + SLD-10.2 (`OverflowDetector`
+ `OverflowResult` sealed, 12 unit). 522 unitaires, zéro régression.

== Session 048 (2026-08-05) — Vision SLD-10 Slide Content Model ✅ TERMINÉ

Session de vision et cadrage (zéro code). Le pilote a exprimé le besoin d'un
nouvel EPIC de modélisation du contenu de slide pour évaluer le rendu —
validation du texte dans l'écran (overflow), balise AsciiDoc `notes`, et autres
assertions Playwright. SLD-9.2→9.4 (i18n completion) mis en pause.

== Session 055 (2026-08-05) — Publication slider 0.0.14 + synchronisation consommateurs ✅ TERMINÉ

Publication slider 0.0.14 sur Maven Central + synchronisation consommateurs
(capsule-gradle + workspace-bom). Aucune feature — session pure ops/release.
Bump 0.0.13 → 0.0.14 (SLD-9.1 AssistantManager i18n + SLD-10 content domain
complet 7/7 US). Bundle nmcp `6f7b84be-93db-4f31-a83f-bebe1b656c2a`
(AUTOMATIC), 3 artefacts signés GPG (slider:0.0.14 + 2 plugin markers
education.ccp.slider + education.ccp.slider.translator). `./gradlew
:slider:check` VERT pré-publication. Vérification repo1.maven.org HTTP 200.
Synchronisation capsule-gradle (2 catalogues slider 0.0.6 → 0.0.14 + plugin
ID `com.cheroliv.slider` → `education.ccp.slider` — ancien namespace
inexistant sur Central). Publication workspace-bom 0.0.14 (bundle
`c8bf911c`, constraint slider 0.0.14). Commits + push 3 repos. Zéro code
de production modifié.

== Session 045 (2026-08-04) — Fix CR-GLOBALE-002 + Publication 0.0.13 ✅ TERMINÉ

Fix 3 bugs CR-GLOBALE-002 (Option A) + publication Maven Central 0.0.13.
US-1 : Guard plan vide — `DeckTranslator.translate` → `TranslationOutcome.EMPTY`
si plan vide, `TranslationOutcome.of()` accepte liste vide, `TranslateDeckTask`
skip avant résolution LLM (clé i18n `task.translateDeck.noTargets` dans 11
Messages_*.properties). Tests 3 niveaux : 2 unit + 1 functional (guard
`-Ptarget.languages=fr` sur deck fr, zéro fichier généré) + 2 scénarios Cucumber.
US-2 : Regex `buildOutputFile` `_[a-z]{2}$` → `_[a-zA-Z]{2}$` (segment langue
uppercase `_ZH`). US-3 : `RagManager.buildStore` propriétés système
`ssl`/`sslmode` scopées try/finally + `restoreSystemProperty` (plus de mutation
globale JVM). `./gradlew :slider:check` VERT 0 FAIL / 1 SKIP. Publication slider
**0.0.13** Maven Central (bundle `ec715b09`, AUTOMATIC), confirmé repo1.maven.org.

== Session 044 (2026-08-04) — SLD-CR3 Revue CR-GLOBALE-003 ✅ TERMINÉ

EPIC SLD-CR3 TERMINÉ (2/2 US). SLD-CR3-1 : Dépréciation `translate/` (doublon de
`translation/`). `TranslationServiceAdapter.kt` supprimé (dead code). Import mort
`getTranslatePromptMessage` retiré de `OllamaLanguageModelAdapter.kt`.
`TranslatorManager` + `TranslatorPlugin` marqués `@Deprecated`. SLD-CR3-2 :
`CapsulePlugin.kt` (capsule-gradle) — `catch (_: Exception)` → `catch (e:
UnknownPluginException)`. `./gradlew :slider:check` VERT 0 FAIL / 1 SKIP.
`./gradlew test` capsule-gradle VERT.

== Session 042 (2026-08-01) — SLD-6.9 + SLD-6.10 — EPIC SLD-6 TERMINÉ ✅

EPIC SLD-6 TERMINÉ (11/11 US, ~20 pts). SLD-6.9 : Consolidation
`registerPublishSlidesTask` + `Git` object → `slider.repository.PublishTaskRegistrar`
+ `SlidePublisher`. `SlidePublisher.kt` (thin Gradle adapter,
ex-`SliderManager.Git.pushSlides`) + `PublishTaskRegistrar.kt` (task registrar,
ex-`SliderManager.Tasks.registerPublishSlidesTask`). Cucumber
`20_publish_task_decomposition.feature` 3/3 PASS. SLD-6.10 : `SliderManager`
supprimé, `SliderPlugin` 30 lignes délègue directement à 5 registrars
(SliderConfig, SliderPrerequisiteRegistrar, SliderScaffoldRegistrar,
SliderWiringRegistrar, SliderTasks). 15 domaines DDD. `./gradlew :slider:check`
VERT — 421 unit (+2 Playwright préexistants) + functional OK + Cucumber OK.

== EPICs — Statut final

[cols="2,3,2,2"]
|===
| EPIC | Description | Statut | US
| SLD-0 | Bootstrap gouvernance | ✅ | 0/0
| SLD-1 | Upgrade Reveal.js 5.2.1 | ✅ | 5/5
| SLD-2 | Playwright E2E + Capsule feed | ✅ | 5/5
| SLD-3 | i18n 10 langues | ✅ | 7/7
| SLD-4 | Refactor DDD slider.repository | ✅ | 8/8
| SLD-5 | Deck Translation Pipeline | ✅ | 7/7
| SLD-6 | SliderManager decomposition | ✅ | 11/11
| SLD-7 | Câblage translateDeck → Capsule | ✅ | 3/3
| CNV-6/7/8 | Conventions plugins | ✅ | 3/3
| PUB | Publications Maven Central 0.0.1→0.0.16 | ✅ | 10/10
| SLD-CR3 | Revue CR-GLOBALE-003 — Déprécier `translate/` + resserrer `catch (_: Exception)` CapsulePlugin | ✅ TERMINÉ (S-044) | 2/2
| CR-GLOBALE-002 | Fix 3 bugs Option A — guard plan vide `translateDeck`, regex `buildOutputFile` uppercase, SSL propriétés système scopées | ✅ TERMINÉ (S-045) | 3/3
| SLD-8 | Consommation socle N1 codebase — unifier les appels LLM slider (graphe koog pipeline deck) | ✅ TERMINÉ (S-065, US-8.0/8.1/8.2/8.3 ✅, US-8.4 caduque fusionné dans 8.3a-d) | 3.5/5 US
| SLD-9 | i18n Completion via LLM — AssistantManager + tests de traduction 10 langues | ✅ TERMINÉ (S-057, 5/5 US — 9.0/9.1/9.2/9.3/9.4) | 5 US ~10 pts
| SLD-10 | Slide Content Model & Rendering Validation — domaine DDD `slider.content` + assertions Playwright | ✅ TERMINÉ (S-054, 7/7 US — 10.0→10.6) | 7 US ~18 pts
| SLD-11 | Expulsion `slider.capsule` — realignment boundary slider ↔ capsule. Domaine `slider.capsule` (4 fichiers prod + 8 fichiers test, 1251l) = fuite de responsabilite (preparation script video dans slider). Expulsion vers capsule-side. | ✅ TERMINÉ (S-058, 6/6 US — 11.0 cadrage S-056 + 11.1→11.6) | 6 US ~14 pts
|===

_Tous les EPICs de feature sont terminés. EPIC SLD-11 (expulsion slider.capsule) TERMINÉ S-058 (6/6 US). EPIC CAP-MODEL (unification modèle capsule.feed) TERMINÉ capsule S-069 (7/7 US). Publication slider 0.0.16 + BOM 0.0.17 (S-066). EPIC SLD-8 (slider consomme codebase) TERMINÉ S-065 (US-8.0/8.1/8.2/8.3 ✅, US-8.4 caduque). EPIC SLD-9 (i18n Completion) TERMINÉ — SLD-9.0→9.4 ✅ (5/5 US). EPIC SLD-10 (Slide Content Model) TERMINÉ — SLD-10.0→10.6 ✅ (7/7 US)._

'''
== ✅ EPIC SLD-11 — Expulsion slider.capsule : realignment boundary slider ↔ capsule (Session 058 — TERMINÉ)

_Vision_ : Le domaine `slider.capsule` (4 fichiers production + 8 fichiers test,
1251 lignes) est une *fuite de responsabilite* — il fait du travail de capsule
(preparation de script video) a l'interieur de slider. Slider doit s'arreter a
produire le HTML Reveal.js ; l'extraction des notes orateur et la serialisation
en `*-script.txt` sont des preoccupations de capsule, pas de slider.

_Symptomes_ :
* Doublon de modele divergent : `slider.capsule.SlideSegment`/`CapsuleScript`
  vs `capsule.SlideSegment`/`CapsuleScript` (champs differents — capsule ajoute
  `type`/`manimScene`, renomme `segments`→`slides`).
* Format intermediaire proprietaire `=== CAPSULE SCRIPT ===` maintenu des deux
  cotes pour zero valeur ajoutee.
* 3 features Cucumber slider + 5 classes test unitaire (672l) + 3 steps (289l)
  testent une logique qui n'est pas slider.

_Boundary cible_ :
- slider produit : HTML Reveal.js seulement (plus de `*-script.txt`)
- capsule consomme : `.adoc` source directement (+ HTML existant)
- Format intermediaire : supprime (capsule parse `.adoc` directement)
- `generateCapsule`/`translateAndGenerateCapsule` : migrent vers capsule-side

[cols="2,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité

| SLD-11.0
| *Cadrage EPIC* — vision, inventaire a expulser, boundary cible, non-regression. MàJ INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine MIAMI
| ✅ TERMINÉ (S-056) | 1

| SLD-11.1
| *Capsule-side : nouveau parseur `.adoc`* — domaine `capsule.feed` (nouveau) : `AsciidocSpeakerNoteParser` (port depuis `slider.capsule`), `CapsuleScript` modele unifie (modele capsule existant avec `type`/`manimScene`), `CapsuleScriptWriter`. Unit tests purs (port des 239l `AsciidocSpeakerNoteParserTest` + 166l `CapsuleScriptWriterTest` + 102l `CapsuleFeedDemoDeckTest`). Baby-step TDD : RED (tests portes) → GREEN (parseur porte) → REFACTOR
| ✅ TERMINÉ (S-058) | 3

| SLD-11.2
| *Capsule-side : tache `extractSpeakerNotes`* — `CapsuleTaskRegistrar` (nouveau, cote capsule) register `extractSpeakerNotes` task (group `capsule`, lit `slides/misc/*.adoc` → extrait notes → `CapsuleScript` modele capsule). Remplace `generateCapsule` slider-side. Functional tests GradleTestKit
| ✅ TERMINÉ (S-058) | 3

| SLD-11.3
| *Capsule-side : Cucumber BDD* — `capsule_speaker_notes.feature` (nouveau, port des 13 scenarios `9_capsule_feed.feature` cote capsule) + steps portes dans `capsule.scenarios`. Tags `@capsule @feed`. BDD end-to-end
| ✅ TERMINÉ (S-058) | 2

| SLD-11.4
| *Capsule-side : pipeline `translateAndExtract`* — tache composite `translateAndExtractSpeakerNotes` (dependsOn `translateDeck` → `extractSpeakerNotes`). Remplace `translateAndGenerateCapsule` slider-side. Functional + Cucumber (port de `23_translate_and_generate_capsule.feature`)
| ✅ TERMINÉ (S-058) | 2

| SLD-11.5
| *Slider-side : expulsion `slider.capsule`* — suppression des 4 fichiers production, 5 classes test unitaire (672l), 3 steps Cucumber (289l), 3 features Cucumber (290l). Suppression câblage `SliderTasks.kt:26-27`. Mise a jour KDoc `AsciidocContentParser.kt`. Verification : `./gradlew :slider:check` VERT, zero regression
| ✅ TERMINÉ (S-058) | 2

| SLD-11.6
| *Capsule-side : integration `generateCapsuleVideo`* — `CapsuleBuildTask`/`CapsuleVideoTask` consomment directement `extractSpeakerNotes` output. Suppression `CapsuleManager.parseScript` (devenu mort). Verification non-regression : `./gradlew :capsule:check` VERT, pipeline complet produit la meme sortie video. Cucumber `capsule.feature` enrichi
| ✅ TERMINÉ (S-058) | 3
|===

_Total_ : 6 US, ~14 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux tests/US
(unit → functional → cucumber), English only code/Gherkin. _Pattern de référence_ :
SLD-6 (expulsion SliderManager → domaines DDD). _Prérequis_ : SLD-6/7/10 terminés
(✅), capsule-gradle stable (✅). _Risque_ : cross-borough (2 repos modifies en
coordination) — mitigé par non-regression stricte (tests portes avant
suppression source).

'''

== ✅ EPIC SLD-10 — Slide Content Model & Rendering Validation (Session 054 — TERMINÉ)

_Vision_ : slider génère des slides AsciiDoc → HTML via Reveal.js, mais n'a
**aucun modèle de contenu de slide** qui permette d'évaluer le rendu. Les
validations Playwright existantes (LTR/RTL) vérifient la structure globale
(titre, slides, bounding box) mais pas le contenu textuel individuel.

SLD-10 crée le domaine DDD `slider.content` modélisant le contenu d'une slide
et des assertions de rendu Playwright pour valider que le texte est dans
l'écran, que les notes orateur sont bien rendues, et que la lisibilité est
correcte.

_Périmètre_ :
- `SlideContent` (aggregate root : titre, sous-titres, paragraphes, listes, notes)
- `TextBlock` (value object : texte, position, bounding box estimée)
- `SlideLayout` (value object : dimensions viewport, marges, font sizes)
- `OverflowDetector` (domain service : prédit si le texte dépasse l'écran)
- `AsciidocContentParser` (parseur .adoc → SlideContent, incluant `[NOTE.speaker]`)
- Assertions Playwright : TextOverflow, NotesRendering, FontSize, Contrast

_Patterns de référence_ : `slider.rtl.RtlSlideAssertion` (assertions pures +
Playwright adapter) + `slider.capsule.AsciidocSpeakerNoteParser` (parseur .adoc).

[cols="2,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité
| SLD-10.0 | Cadrage EPIC — vision, domaine `slider.content`, découpage US, dépendances. MàJ INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine MIAMI | 1 | P0
| SLD-10.1 | Value object `TextBlock` (texte, position, bounding box) + `SlideLayout` (viewport, marges, font sizes). Unit tests purs | 2 | P0
| SLD-10.2 | `OverflowDetector` (domain service) — prédit si un `TextBlock` dépasse le viewport en fonction du `SlideLayout`. Unit tests purs | 3 | P0
| SLD-10.3 | `AsciidocContentParser` (parseur .adoc → `SlideContent`) — étend `AsciidocSpeakerNoteParser` aux titres, paragraphes, listes, notes `[NOTE.speaker]`. Unit tests purs | 3 | P0
| SLD-10.4 | `SlideContent` aggregate root + `ContentAssertionCode`/`Result`/`Failure` (codes P0/P1) + `ContentSlideAssertion` object (`assertAll`). Pattern `RtlSlideAssertion`. Unit tests purs | 3 | P1
| SLD-10.5 | Assertions Playwright enrichies — `ContentRenderData` (snapshot DTO pur, mirroring `SlideRenderData` RTL) + `ContentRenderAssertion` (object pur, mesure a posteriori) + `ContentPlaywrightAdapter` (extraction Playwright → snapshot, port 4337) + `ContentPlaywrightFunctionalTest` (LTR fixture). Réutilise les 4 codes `ContentAssertionCode` existants. Nouvelle constante `MIN_CONTRAST_RATIO = 4.5` (WCAG AA). 3 baby-steps TDD : unit (8+9) → functional (1 Playwright). | ✅ TERMINÉ (S-053, 17 unit + 1 functional) | 3 pts
| SLD-10.6 | Cucumber `24_slide_content_validation.feature` — scénarios BDD : overflow, notes rendues, font size, contraste WCAG, slide sans note. BDD end-to-end | ✅ TERMINÉ (S-054, 11 scénarios) | 3 pts
|===

_Total_ : 7 US, ~18 pts. _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux tests/US
(unit → functional → cucumber), English only code/Gherkin. _Pattern de référence_ :
`slider.rtl` (S-021) + `slider.repository` (S-025/027) — value objects purs +
assertions pures + adaptateur infrastructure. _Prérequis_ : SLD-2/3/6 terminés
(✅), build vert 660 PASS (✅).

'''
== ✅ EPIC SLD-8 — Consommation socle N1 codebase : unifier les appels LLM slider (Session 065 — TERMINÉ)

_Vision_ : les appels LLM de slider sont éparpillés dans sa logique métier au lieu
de reposer sur un socle unifié. `AssistantManager.kt` (648 lignes) fabrique des
modèles pour 4 providers (Ollama/Gemini/Mistral/HF) + dispatch `resolveModel` +
pools ; `RagManager.kt` (282 lignes) refait un RAG pgvector+all-minilm ;
`OllamaLanguageModelAdapter` refait un client de traduction. codebase-gradle (N1)
**unifie déjà tout cela** (langchain4j : `LlmProviderResolver`/`OllamaPool` ;
koog : graphes d'états `VibecodingGraph`/`KoogAugmentedContextGraph`). SLD-8 fait
de slider le **premier consommateur** de la couche LLM codebase, par injection de
services (`BuildService`/`Provider`, DI native Gradle — Decision 001), au lieu
de réimplémenter le socle.

_Prérequis côté codebase (N1)_ : codebase n'expose aujourd'hui **aucune API de
consommation de services** (zéro `BuildService`, zéro `Provider<ChatModel>`, deps
langchain4j/koog en `implementation`). L'US-8.1 lève ce gap — contrat de service
+ deps exposées en `api` — tracé ici comme dépendance cross-borough (Queens).

[cols="2,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité
| SLD-8.0 | Cadrage contrat de consommation — périmètre slider (`AssistantManager` 4 factories → câblage, `RagManager`, `OllamaLanguageModelAdapter`) + contrat service codebase consommable (N0 `llm-pool-contracts`/`codebase-contracts`) | 1 | P0 | ✅ TERMINÉ (S-046)
| SLD-8.1 | **Côté codebase (N1, Queens)** — exposer un `BuildService` LLM consommable : `Provider<ChatModel>` via `LlmProviderResolver`/`OllamaPool` ; koog-agents/langchain4j en `api` → slider n'ajoute pas koog via BOM, il le consomme transitivement | 3 | P0 | ✅ TERMINÉ (S-176)
| SLD-8.2 | slider consomme le service codebase via `@Inject` — `AssistantManager` réduit à du câblage, suppression factories 4 providers + dispatch `ai.provider` + pools | 2 | P0 | ✅ TERMINÉ (S-060)
| SLD-8.3 | Orchestration koog du pipeline deck — `DeckPipelineGraph` (nodes : propose-context, validate-context, generate-deck) sur les services codebase, DSL koog transitif. 4 baby-steps : 8.3a `DeckState`+`DeckContextValidator` (20-25 unit) → 8.3b `DeckPipelineGraph` graphe koog (15-20 unit mock LLM) → 8.3c `GenerateDeckPipelineTask` (5-8 functional) → 8.3d Cucumber `26_deck_pipeline_koog.feature` (8-12 scénarios). Domaine `slider.pipeline`. | 3 | P1 | ✅ TERMINÉ (S-065, 4/4 baby-steps 8.3a-d, 71 tests 3 niveaux)
| SLD-8.4 | Tests 3 niveaux — unit (graphe koog, services mockés), functional, Cucumber pipeline koog deck (2 appels orchestrés). Fusionné dans baby-steps 8.3a-d (US-8.3 intègre les 3 niveaux par baby-step). | 3 | P1 | ✅ CADUQUE (fusionné dans 8.3a-d)
|===

_Total_ : 5 US, ~12 pts. _Dépendances_ : US-8.1 = travail cross-borough codebase (Queens),
cadré ici comme contrat côté consommateur ; `proposeDeckContext`/`generateDeck` existants
(AssistantManager). _Source_ : Session 046 (refondu post-046 — slider consomme codebase
plutôt que réimplémenter koog). _Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux tests/US
(unit → functional → cucumber), English only code/Gherkin.
_Alignement vision_ : `doc/agentic_gradle_1.adoc` §1 (koog) + `doc/agentic_gradle_5.adoc` (R2DBC).
_Décisions_ : `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc` Decision 001 (PicoContainer
rejeté, DI native Gradle) + Decision 002 (consommer codebase comme socle IA unifié).

'''

== ✅ EPIC SLD-9 — i18n Completion via LLM : AssistantManager + tests de traduction 10 langues (Session 057 — TERMINÉ)

_Vision_ : SLD-3 (7/7 US) a posé le socle i18n — `SliderMessages`, 10 fichiers
`Messages_*.properties` peuplés, 5 registrars i18nisés. Mais `AssistantManager.kt`
(648 lignes) est resté en anglais dur : ~13 strings hardcodées (descriptions RAG,
groupes de tâches, smoke-tests 4 providers). SLD-9 comble ce gap avec une
approche **LLM-assisted** : les traductions sont générées par LLM (prompt
"traduis ces descriptions techniques Gradle en {langue}"), puis vérifiées par
des tests automatisés (pattern plantuml-gradle).

_Périmètre restrictif_ : que de l'i18n — pas de refacto, pas de koog, pas de
codebase. Les strings hardcodées de `AssistantManager` sont remplacées par des
appels `SliderMessages.get()`, les clés sont ajoutées aux 10 fichiers properties,
les traductions sont générées par LLM et validées par tests.

[cols="2,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité
| SLD-9.0 | Cadrage — inventaire exhaustif des strings non i18nisées + contrat de complétion | 1 | P0 | ✅ TERMINÉ (S-047)
| SLD-9.1 | i18niser `AssistantManager` — 13 clés → `Messages_*.properties` 10 langues (générées par LLM), remplacer strings hardcodées par `SliderMessages.get()` | 3 | P0 | ✅ TERMINÉ (S-047, +47 tests)
| SLD-9.2 | Tests de traduction par langue — 9 classes paramétrées (zh/hi/es/ar/bn/pt/ru/ur + en référence), pattern `MessagesFrTranslationTest`, vérification que chaque clé diffère de l'anglais | 2 | P1 | ✅ TERMINÉ (S-057, +366 unit)
| SLD-9.3 | `TaskDescriptionsI18nTest` — test fonctionnel GradleTestKit vérifiant que TOUTES les tâches enregistrées ont un groupe + description i18nisés pour chaque langue | 2 | P1 | ✅ TERMINÉ (S-057, +20 functional)
| SLD-9.4 | `SliderMessagesTest` unitaire (get/format/fallback/missing key) + Cucumber BDD message resolution (pattern plantuml `I18nSteps.kt`) | 2 | P1 | ✅ TERMINÉ (S-057, +26 unit +31 cucumber)
|===

_Total_ : 5 US, ~10 pts. _Dépendances_ : SLD-3 (socle i18n) ✅. _Source_ : Session 047.
_Méthode_ : baby-step DDD/TDD/BDD, 3 niveaux tests/US (unit → functional → cucumber),
English only code/Gherkin. _Pattern de référence_ : plantuml-gradle i18n
(`PlantumlMessagesTest`, `TaskDescriptionsI18nTest`, `LifecycleMessagesI18nTest`,
9 classes de traduction par langue, Cucumber `I18nSteps.kt`).

'''

'''
== 🟠 EPIC SLD-CR3 — Revue CR-GLOBALE-003 : Création de Contenu Solide

_Source_ : `GLOBAL_CODE_REVIEW.adoc` (workspace root) — session CR-GLOBALE-003 (2026-08-04).
_Focus_ : slider est la source des decks consommés par capsule ; nettoyer la chaîne amont.

[cols="1,3,2,2"]
|===
| EPIC | Description | Statut | US

| SLD-CR3-1
| **Déprécier/expulser `translate/` (doublon de `translation/`)** — Deux implémentations parallèles
de traduction coexistent (`translate/` vs `translation/`). `translation/` est le DDD pur (DeckTranslator).
Converger : déprécier `translate/` puis l'expulser, ou fusionner.
| ✅ TERMINÉ (S-044)
| 1

| SLD-CR3-2
| **Resserrer le `catch (_: Exception)` de `CapsulePlugin` auto-apply slider** — `CapsulePlugin.kt:10`
avale toute exception quand il auto-applique `education.cccp.slider`. Rendre l'échec visible
(logger.error + rethrow sur erreurs non « plugin absent ») pour ne pas masquer les soucis de chaîne.
| ✅ TERMINÉ (S-044)
| 1
|===

_Note_ : les tests Playwright E2E `@Disabled` (GradleRunner 9.6.1 / gem JRuby) restent un bloqueur
de validation réelle — à rouvrir avec la migration Gradle 9.6.1 résolue.

'''
== Session 041 (2026-07-31) — SLD-7 Câblage Explicite translateDeck → Capsule ✅ TERMINÉ

EPIC SLD-7 TERMINÉ (3/3 US, ~6 pts). US SLD-7.0 cadrage (contrat d'interface
slider↔capsule formalisé). US SLD-7.1 tâche composite `translateAndGenerateCapsule`
(dependsOn translateDeck → generateCapsule, domaine `slider.capsule` enrichi :
`CapsuleTaskNames.TRANSLATE_AND_GENERATE_CAPSULE` + `CapsuleTaskRegistrar.
registerTranslateAndGenerateCapsule`). US SLD-7.2 Cucumber BDD pipeline complet
slider→capsule (feature `23_translate_and_generate_capsule.feature` +
`TranslateAndGenerateCapsuleSteps`). Baby-step TDD/BDD 3 niveaux : 2 unit
(CapsuleTaskDomainTest) + 2 functional (TranslateAndGenerateCapsuleFunctionalTest)
+ 3 cucumber (23_translate_and_generate_capsule.feature). `./gradlew :slider:check`
VERT — 660 PASS / 0 FAIL / 1 SKIP (+6 vs 654 S-040). `SliderManager.kt` 552→553
lignes (registerTranslateAndGenerateCapsuleTask ajouté dans registerTasks).

'''
== Session 040 (2026-07-22) — SLD-6.8 Capsule task decomposition ✅ TERMINÉ

US SLD-6.8 livrée. Domaine DDD `slider.capsule` consolidé (2 nouveaux fichiers,
~138 lignes) : `CapsuleTaskDomain.kt` (78l value objects purs `CapsuleAdocDir`
résout `<projectDir>/slides/misc` + `adocFiles()` triés, `CapsuleScriptDir`
résout `<buildDir>/capsule` + `ensureCreated()` idempotent + `scriptFileFor(name)`,
`CapsuleTaskNames` constantes `generateCapsule`/`slider`/description) +
`CapsuleTaskRegistrar.kt` (60l thin Gradle adapter `register(project)` —
comportement bit-for-bit préservé : `outputs.upToDateWhen { false }`, skip empty
scripts, log slide count). `SliderManager.Tasks.registerAsciidocCapsuleTask`
(~30 lignes) supprimé, délègue à `slider.capsule.CapsuleTaskRegistrar.register(this)`.
Imports morts nettoyés (`AsciidocSpeakerNoteParser`/`CapsuleScriptWriter` retirés
de SliderManager). `SliderManager.kt` 570→552 lignes (was 968 S-035). Baby-step
TDD/BDD 3 niveaux : 12 unit (`CapsuleTaskDomainTest`) + 13 scénarios Cucumber
(`22_capsule_task_decomposition.feature` + `CapsuleTaskDecompositionSteps`,
steps préfixés `capsule`). `./gradlew :slider:check` VERT — **654 PASS / 0 FAIL /
1 SKIP** (+21 vs 633 S-039).

'''
== Session 039 (2026-07-21) — SLD-6.5 + SLD-6.6 + SLD-6.7 ✅ TERMINÉ

Trois US de l'EPIC SLD-6 livrées en une session. Domaine DDD `slider.wiring`
créé (2 fichiers : `WiringSpec.kt` value objects GroupRouting/RepositoryKind/
RepositorySpec/PluginSpec/GemDependency/WiringSpec.DEFAULT 6 repos+3 plugins+1 gem
+ `GradleWiring.kt` adapter configureRepositories/applyPlugins/configureDependencies).
Domaine DDD `slider.revealjs` créé (2 fichiers : `RevealJsDomain.kt`
RevealJsAttributesSpec/RevealJsOutputDir/SlideSourceDir/SlideMetadata/
SlideMetadataScanner/DashboardJsonSerializer/CleanBuildTarget +
`RevealJsTaskRegistrar.kt` register cleanBuild+asciidoctorRevealJs+generateDashboard).
Domaine DDD `slider.playwright` créé (2 fichiers : `PlaywrightDomain.kt`
PlaywrightDir/ServeCommand/InstallCommand DEFAULT chromium/VisualTestCommand/
PlaywrightTaskNames + `PlaywrightTaskRegistrar.kt` register serveSlides+
installPlaywright+visualTest). `SliderManager.kt` ~844→~570 lignes, 3 nested
objects → thin adapters, 6 méthodes Tasks supprimées, imports morts nettoyés.
Baby-step TDD/BDD 3 niveaux : +68 unit + 2 functional + 53 scénarios Cucumber
(features 19+20+21). `./gradlew :slider:check` VERT 633 PASS / 0 FAIL / 1 SKIP
(+117 vs 516 S-038).

'''
== Session 038 (2026-07-22) — SLD-6.4 Extensions → slider.extension ✅ TERMINÉ

SLD-6.4 — Extraction `Extensions` → `slider.extension` (nouveau domaine DDD,
3 fichiers, ~110 lignes) : `RevealJsPin.kt` value object pin Reveal.js 5.2.1,
`SliderExtension.kt` DSL déménagée depuis SliderPlugin, `SliderExtensionRegistrar.kt`
`configure(project, pin)` enregistre DSL + pin. `SliderManager.Extensions` →
thin adapter (délègue à `SliderExtensionRegistrar.configure(this)`).
`SliderPlugin.SliderExtension` → `typealias slider.extension.SliderExtension`
(préserve référence `RagTasks.kt`). Imports morts nettoyés. Baby-step TDD/BDD
3 niveaux : 9 unit (`RevealJsPinTest`) + 9 scénarios Cucumber
(`18_extension_decomposition.feature`). `./gradlew :slider:check` VERT 516 PASS /
0 FAIL / 1 SKIP (+18 vs 498 S-037).

'''
== Session 037 (2026-07-21) — SLD-6.3 Prerequisites → slider.prerequisite ✅ TERMINÉ

SLD-6.3 — Extraction `Prerequisites` → `slider.prerequisite` (nouveau domaine
DDD, 1 fichier, ~45 lignes) : `JavaVersionGuard.kt` object pur.
`requireJava23(currentMajor: Int)` validation pure + `requireJava23FromMajor(majorVersionString: String)`
parse String (issue de `JavaVersion.current().majorVersion`, zéro dépendance Gradle).
`SliderManager.Prerequisites` → thin adapter 11 lignes (passe
`JavaVersion.current().majorVersion` au guard). Signature publique
`SliderPlugin.apply()` préservée, zéro régression. Baby-step TDD/BDD 3 niveaux :
9 unit (`JavaVersionGuardTest`) + 9 scénarios Cucumber
(`17_prerequisite_decomposition.feature`). `./gradlew :slider:check` VERT 498 PASS /
0 FAIL / 1 SKIP (+32 vs 466 S-036).

'''
== Session 036 (2026-07-21) — SLD-6.2 Configuration → slider.config ✅ TERMINÉ

SLD-6.2 — Extraction `Configuration` → `slider.config` (nouveau domaine DDD, 3
fichiers : `ConfigPath.kt` value object, `YamlMapperFactory.kt` factory ObjectMapper,
`SlidesConfigLoader.kt` domain service). `SliderManager.Configuration` → thin
adapter. Fix deprecation `properties[...]` lignes 80 + 638 → `findProperty(...)`.
`registerPublishSlidesTask` réutilise `localConf` au lieu de re-désérialiser. Imports
morts nettoyés (5 imports Jackson/Arrow retirés). Baby-step TDD/BDD 3 niveaux :
16 unit + 15 scénarios Cucumber. `./gradlew :slider:check` VERT 466 PASS / 0 FAIL /
1 SKIP (+11 vs 455).

'''
== Session 035 (2026-07-20) — Cadrage SLD-6 + US-6.1 ✅ TERMINÉ

Cadrage EPIC SLD-6 — Refactor DDD SliderManager decomposition. `SliderManager.kt`
est un god-object de 968 lignes contenant 9 nested objects (Configuration,
Prerequisites, Scaffold, Repositories, Plugins, Dependencies, Extensions, Tasks,
Git). Alors que les 6 domaines métier DDD (`slider.repository`, `slider.capsule`,
`slider.rtl`, `slider.i18n`, `slider.translate`, `slider.translation`) ont déjà
été extraits proprement, le `SliderManager` reste le point d'agglomération
résiduel. SLD-6 applique le même pattern DDD au `SliderManager` lui-même.

11 US, ~20 pts. Cible : `SliderManager` thin orchestrator < 100 lignes. Fix
deprecation `properties[...]` (lignes 82, 744). Baby-step TDD/BDD 3 niveaux,
English only.

US-6.1 terminée : extraction `Scaffold` → `slider.scaffold` (3 fichiers, 169
lignes domaine pur). `SliderManager.Scaffold` réduit ~184→65 lignes (thin
adapter). Imports morts nettoyés. Baby-step TDD/BDD : 12 unit + 15 scénarios
Cucumber. `./gradlew :slider:check` VERT 455 PASS / 0 FAIL / 1 SKIP (+27 vs 428).

'''
== EPIC SLD-6 — Refactor DDD SliderManager decomposition ✅ TERMINÉ

_Vision_ : `SliderManager.kt` (968 lignes, 9 nested objects) est un god-object.
Extraction de chaque responsabilité dans un domaine dédié, baby-step TDD/BDD 3
niveaux, jusqu'à ce que `SliderManager` devienne un thin orchestrator (< 100
lignes). Fix deprecation `properties[...]` (lignes 82, 744).

_Domaines DDD extraits_ : `slider.scaffold`, `slider.config`, `slider.prerequisite`,
`slider.extension`, `slider.wiring`, `slider.revealjs`, `slider.playwright`,
`slider.capsule` (consolidé) + consolidation `slider.repository` existant restant.

|===
| US | Description | Statut | Estimation
| SLD-6.0 | Cadrage EPIC — vision, découpage US, dépendances. MàJ INDEX/BACKLOG/PROMPT_REPRISE | ✅ TERMINÉ (S-035) | 1 pt
| SLD-6.1 | Extraction `Scaffold` → `slider.scaffold` (3 méthodes, value object `ScaffoldResult`). Unit + Cucumber `15_scaffold_decomposition.feature` | ✅ TERMINÉ (S-035, 12 unit + 15 cucumber) | 3 pts
| SLD-6.2 | Extraction `Configuration` → `slider.config` + fix deprecation `properties[...]` (lignes 80, 638). `ConfigPath` value object, `SlidesConfigLoader`, `YamlMapperFactory`. Unit + Cucumber `16_config_decomposition.feature` | ✅ TERMINÉ (S-036, 16 unit + 15 cucumber) | 3 pts
| SLD-6.3 | Extraction `Prerequisites` → `slider.prerequisite` (`JavaVersionGuard`). Unit + Cucumber `17_prerequisite_decomposition.feature` | ✅ TERMINÉ (S-037, 9 unit + 9 cucumber) | 1 pt
| SLD-6.4 | Extraction `Extensions` → `slider.extension` (`SliderExtensionRegistrar`). Unit + Cucumber `18_extension_decomposition.feature` | ✅ TERMINÉ (S-038, 9 unit + 9 cucumber) | 2 pts
| SLD-6.5 | Extraction `Repositories`+`Plugins`+`Dependencies` → `slider.wiring` (`GradleWiring`). Functional GradleTestKit | ✅ TERMINÉ (S-039, 28 unit + 2 functional + 17 cucumber) | 2 pts
| SLD-6.6 | Extraction `registerAsciidoctorRevealJsTask`+clean+dashboard → `slider.revealjs` (`RevealJsTaskRegistrar`). Functional GradleTestKit | ✅ TERMINÉ (S-039, 24 unit + 17 cucumber) | 3 pts
| SLD-6.7 | Extraction `serveSlides`+`visualTest`+`installPlaywright` → `slider.playwright` (`PlaywrightTaskRegistrar`). Functional GradleTestKit | ✅ TERMINÉ (S-039, 16 unit + 19 cucumber) | 2 pts
| SLD-6.8 | Extraction `registerAsciidocCapsuleTask` → `slider.capsule.CapsuleTaskRegistrar`. Functional + Cucumber `22_capsule_task_decomposition.feature` | ✅ TERMINÉ (S-040, 12 unit + 13 cucumber) | 1 pt
| SLD-6.9 | Consolidation `registerPublishSlidesTask`+`Git` → `slider.repository.PublishTaskRegistrar`+`SlidePublisher`. Functional + Cucumber `20_publish_task_decomposition.feature` | ✅ TERMINÉ (S-042, 3 cucumber) | 2 pts
| SLD-6.10 | `SliderManager` → thin orchestrator < 100 lignes. `SliderPlugin.apply()` délègue à tous les registrars. Vérification 428 PASS maintenus | ✅ TERMINÉ (S-042, `SliderManager` supprimé) | 2 pts
|===

_Total_ : 11 US, ~20 pts (11/11 terminées — S-035→S-042). _Méthode_ : baby-step TDD/BDD, 3 niveaux tests/US (unit → functional → cucumber), English only code/Gherkin. _Prérequis_ : SLD-0→5 + CNV-6/7/8 terminés, build vert 654 PASS.

'''
== EPIC PUB — Publication Maven Central 0.0.13 ✅ TERMINE (Session 045, 2026-08-04)

Bump 0.0.12 → 0.0.13 (fix CR-GLOBALE-002 S-045 : guard plan vide + regex
uppercase + SSL scopé). Bundle nmcp `ec715b09-9587-49e5-9b9b-5f46a579c870`
AUTOMATIC, confirmé repo1.maven.org.

| Artefact | URL |
|---|---|
| slider 0.0.13 | https://central.sonatype.com/artifact/education.cccp/slider/0.0.13 |

'''
== EPIC PUB — Publication Maven Central 0.0.12 ✅ TERMINE (Session 040, 2026-07-22)

Bump 0.0.11 → 0.0.12 sur 3 fichiers (SLD-6.8 capsule task decomposition S-040).
Bundle nmcp `842a5a72-fded-4856-b4f6-e3a6793f7fd1` AUTOMATIC, 3 artefacts signés
(slider:0.0.12 + 2 plugin markers). BOM 0.0.11 publiée (bundle
`261de995-7767-4e11-83e4-9c62196ab43a`), slider constraint 0.0.11 → 0.0.12.

| Artefact | URL |
|---|---|
| slider 0.0.12 | https://central.sonatype.com/artifact/education.cccp/slider/0.0.12 |
| workspace-bom 0.0.11 | https://central.sonatype.com/artifact/education.cccp/workspace-bom/0.0.11 |

'''
== EPIC PUB — Publication Maven Central 0.0.11 ✅ TERMINE (Session 034, 2026-07-15)

Bump 0.0.10 → 0.0.11 sur 4 fichiers (Playwright @Disabled fix). Bundle nmcp
`b18431e5-a8a1-473e-a6a9-337fd6bfd664` AUTOMATIC, 3 artefacts signés
(slider:0.0.11 + 2 plugin markers). BOM 0.0.10 publiée (bundle `61519263`),
slider constraint 0.0.10 → 0.0.11.

| Artefact | URL |
|---|---|
| slider 0.0.11 | https://central.sonatype.com/artifact/education.cccp/slider/0.0.11 |
| workspace-bom 0.0.10 | https://central.sonatype.com/artifact/education.cccp/workspace-bom/0.0.10 |

'''
== Session 033 (2026-07-14) — Audit i18n ✅

Audit i18n slider-gradle. Confirmation que le support i18n est entièrement
couvert et fonctionnel. SLD-3 (config + UI + RTL, 7/7 US) et SLD-5 (pipeline
deck one-to-many, 7/7 US) TERMINÉS. 10 langues, RTL ar/ur, fallback, pipeline
`translateDeck`, validation Playwright, dogfooding. 3 domaines DDD i18n
inventoriés (slider.i18n 5 fichiers, slider.translate 2 fichiers,
slider.translation 8 fichiers). 8 features Cucumber i18n. Nettoyage 3
répertoires vides `com/cheroliv/slider/{ai,translate}/` (héritage S-005).
Zéro fichier de production modifié. `./gradlew :slider:check` VERT — 425 PASS.

'''
== EPIC PUB — Publication Maven Central 0.0.10 ✅ TERMINE (Session 032, 2026-07-14)

Bump 0.0.9 → 0.0.10 sur 3 fichiers (SLD-5 translation + README i18n S-031). Bundle
nmcp `ea117827-531a-4d3c-8a45-c58bc9d34eb1` AUTOMATIC, 3 artefacts signés
(slider:0.0.10 + 2 plugin markers). BOM 0.0.9 publiée (bundle `79395cf5`), slider
constraint 0.0.1 → 0.0.10 + correction module name (`slider-plugin` → `slider`).

| Artefact | URL |
|---|---|
| slider 0.0.10 | https://central.sonatype.com/artifact/education.cccp/slider/0.0.10 |
| workspace-bom 0.0.9 | https://central.sonatype.com/artifact/education.cccp/workspace-bom/0.0.9 |

'''
== EPIC SLD-5 — Deck Translation Pipeline ✅ TERMINÉ (Session 029-030, 2026-07-14)

_Vision_ : Pipeline de traduction de deck one-to-many. Partant d'un deck source
(deck-context.yml + .adoc) dans une langue, génère les 10 variantes localisées
couvrant le top 10 des langues les plus parlées (source :
`10 langues les plus parlés.csv` — aligné sur `LanguageCatalog.supportedCodes()` :
en, zh, hi, es, fr, ar, bn, pt, ru, ur).

_Domaine DDD_ : `slider.translation` (S-029) — 8 fichiers : value objects
purs (TranslationRequest, DeckTranslationPlan, TranslationResult sealed 3 variants,
TranslationOutcome) + port (LanguageModelAdapter) + domain service (DeckTranslator)
+ adaptateur LLM (OllamaLanguageModelAdapter) + tâche Gradle thin (TranslateDeckTask).
`DeckTranslator.ensureRtlDirection` (S-030) : injecte/supprime `:revealjs_direction: rtl`
post-traduction selon langue cible.

_Dogfooding_ (S-030) : 3 decks demo fr/en/ar étendus à 10 langues via `translateDeck`
avec LLM réel `gpt-oss:120b-cloud` (port 11439). 54 fichiers générés (27 adoc + 27
context). Validation Playwright LTR (fr→en) + RTL (fr→ar). README master EN mis à
jour.

|===
| US | Description | Statut | Estimation
| SLD-5.0 | Cadrage EPIC — vision, domaine, découpage US, dépendances. MàJ INDEX/BACKLOG/PROMPT_REPRISE + AGENTS.adoc racine | ✅ TERMINÉ (S-029) | 1 pt
| SLD-5.1 | Value object `TranslationRequest` (source DeckContext + targetLanguages validée LanguageCatalog) + `DeckTranslationPlan` (permutations source→target). Unit tests purs | ✅ TERMINÉ (S-029, 19 unit) | 3 pts
| SLD-5.2 | Value object `TranslationResult` (per-language DeckContext+adoc+status) + `TranslationOutcome` (agrégation). Unit tests purs | ✅ TERMINÉ (S-029, 25 unit) | 2 pts
| SLD-5.3 | `DeckTranslator` (domain service) — orchestre LLM par langue cible, port `LanguageModelAdapter` mockable, zéro leak langchain4j. Unit tests mock LLM | ✅ TERMINÉ (S-029, 10 unit) | 3 pts
| SLD-5.4 | Tâche Gradle `translateDeck` — `-Pdeck.context` + `-Ptarget.languages` (défaut 10), outputs 20 fichiers (10 context + 10 adoc). Functional tests GradleTestKit | ✅ TERMINÉ (S-029, 2 functional) | 3 pts
| SLD-5.5 | Cucumber `13_deck_translation.feature` — BDD end-to-end (one-to-many, langue invalide, fallback, idempotence). BDD | ✅ TERMINÉ (S-029, 13 cucumber) | 2 pts
| SLD-5.6 | Dogfooding — 3 decks demo S-020 étendus à 10 langues + validation visuelle Playwright (1 LTR + 1 RTL) + README i18n | ✅ TERMINÉ (S-030) | 2 pts
|===

_Total_ : 7 US, ~16 pts (7/7 terminées, 16 pts livrés). _Méthode_ : baby-step TDD/BDD, 3 niveaux tests/US (unit → functional → cucumber), English only code/Gherkin. _Tests_ : 425 PASS (293 unit + 15 functional + 117 cucumber), was 270. +155 tests, zéro régression.

'''
== EPIC PUB — Publication Maven Central 0.0.9 ✅ TERMINE (Session 028, 2026-07-11)

README i18n (20 fichiers) mis à jour avec domaine DDD `slider.repository` + 2
autres domaines (`slider.capsule`, `slider.rtl`). Bump 0.0.8 → 0.0.9. Bundle
nmcp `63048fec` AUTOMATIC, 3 artefacts signés (slider:0.0.9 + 2 plugin markers).

| Artefact | URL |
|---|---|
| slider 0.0.9 | https://central.sonatype.com/artifact/education.cccp/slider/0.0.9 |

'''
== EPIC SLD-4 — Refactor DDD slider.repository ✅ TERMINÉ

_Vision_ : Extraction du domaine `slider.repository` depuis `SliderManager.Git`.
Value objects purs (SlideDeploymentRequest, GitCredentials) + SlideDeployer
(createRepoDir/copySlides/cleanup, typed results) + JGitSlidePusher (adaptateur
JGit init/commit/push, types domaine CommitResult/SlidePushResult, zéro leak JGit).

_Session 025 (2026-07-11)_ : Baby-step TDD/BDD 3 niveaux. `SlideDeploymentRequest`
+ `GitCredentials` (value objects) + `SlideDeployer` (createRepoDir/copySlides/
cleanup, typed results RepoDirResult/CopyResult) depuis `SliderManager.Git`.
`FileOps` + `FileOperationResult` supprimés (dead code). `Git.pushSlides` délègue
au domaine. 16 unit + 2 functional + 9 cucumber BDD.

_Session 027 (2026-07-11)_ : Extraction `JGitSlidePusher` (adaptateur JGit) —
`initAndCommit` + `push` avec types domaine `CommitResult`/`SlidePushResult`.
`SliderManager.Git` réduit ~120→55 lignes. Tests intégration pipeline avec bare
repo local. Suppression `GitOperationResult` dead code. 9 unit + 3 cucumber BDD.
211 PASS / 0 FAIL / 3 SKIP. **EPIC SLD-4 TERMINÉ** 8/8 US.

|===
| US | Description | Statut | Estimation
| SLD-4.1 | Value objects (SlideDeploymentRequest + GitCredentials) + validation | ✅ TERMINÉ (S-025) | 2 pts
| SLD-4.2 | SlideDeployer (createRepoDir + copySlides + cleanup, typed results) | ✅ TERMINÉ (S-025) | 2 pts
| SLD-4.3 | Refactor Git.pushSlides → délègue au domaine + suppression FileOps | ✅ TERMINÉ (S-025) | 1 pt
| SLD-4.4 | Tests fonctionnels deploySlides (GradleTestKit) | ✅ TERMINÉ (S-025) | 1 pt
| SLD-4.5 | Tests Cucumber BDD deployment (11_deployment_request.feature) | ✅ TERMINÉ (S-025) | 1 pt
| SLD-4.6 | Extraction JGitSlidePusher (adaptateur JGit) — initAndCommit + push | ✅ TERMINÉ (S-027) | 3 pts
| SLD-4.7 | Tests intégration pipeline avec mock Git remote (bare repo local) | ✅ TERMINÉ (S-027) | 2 pts
| SLD-4.8 | Suppression GitOperationResult dead code + cleanup imports JGit | ✅ TERMINÉ (S-027) | 1 pt
|===

_Total_ : 8 US, ~13 pts (8/8 terminées).

'''
== EPIC PUB — Publication Maven Central ✅ TERMINE

Plugin N2 de generation de presentations Reveal.js (AsciiDoc → HTML). VAGUE 4 (plugins metier).

=== PUB-3.12 — slider-plugin 0.0.1 ✅ TERMINE (Session 009, 2026-06-11)
**Priorite** : P0
**Estimation** : 1 pt

. Artefact : `education.cccp:slider-plugin:0.0.1`
. URL : https://central.sonatype.com/artifact/education.cccp/slider-plugin
. Methode : nmcp, POM sans guard, version 0.0.1 hardcodee

=== PUB-3.13 — slider 0.0.8 ✅ TERMINE (Session 022, 2026-07-09)
**Priorite** : P2
**Estimation** : 1 pt

. Artefacts : `education.cccp:slider:0.0.8` + 2 plugin markers
  (`education.cccp.slider` + `education.cccp.slider.translator`)
. URL : https://central.sonatype.com/artifact/education.cccp/slider/0.0.8
. Methode : nmcp, bundle 4684447b, publishingType AUTOMATIC
. Bump strictement montant : 0.0.1→0.0.8 (slider), 0.0.1→0.0.5 (codebase),
  0.0.0→0.0.1 (readme)
. Correction namespace : plugin ID `slider.translator`→
  `education.cccp.slider.translator` (Central education.cccp.* seulement)
. Zéro régression : 94 unit + 65 cucumber + 11 functional identiques pré/post

=== PUB-5.1 — Verifier Maven Central ✅ TERMINE (2026-07-09)

| Artefact | URL |
|---|---|
| slider-plugin 0.0.1 | https://central.sonatype.com/artifact/education.cccp/slider-plugin |
| slider 0.0.8 | https://central.sonatype.com/artifact/education.cccp/slider/0.0.8 |

'''
== EPIC CNV-6/7/8 — Conventions plugins ✅ TERMINE

Migration vers les convention plugins education.cccp.build.* v0.0.2
(gradle-plugin + publishing + functional-test + cucumber + logback-exclusion)
publiées par le borough conventions-gradle (ABIDJAN).

|===
| US | Description | Statut
| CNV-6 | gradle-plugin + publishing (session 013, -96 lignes) | ✅ TERMINE
| CNV-7 | functional-test + cucumber (session 016, -114 lignes) | ✅ TERMINE
| CNV-8 | logback-exclusion (session 017, -13 lignes slider, conventions-gradle TDD) | ✅ TERMINE
|===

Build.gradle.kts slider : 301 → 145 lignes (-156 net, -52%).
Test results : functionalTest ✅, cucumberTest 16 PASS / 0 FAIL / 2 SKIP, test 22 PASS.

'''
== EPIC SLD-2 — Rendu visuel theme pro + Playwright ✅ TERMINE

_Vision_ : Rendu visuel professionnel validé par Playwright, thème reveal.js pro,
transitions fluides, feed capsule pour capsule-gradle. 5/5 US terminées.

_Session 020 (2026-07-07)_ : US-2.5 TERMINÉ — Capsule Feed recadré sur contrat réel
`*-script.txt` (audit cross-borough capsule-gradle). Domaine DDD `slider.capsule`
(CapsuleScript + SlideSegment + CapsuleScriptWriter + AsciidocSpeakerNoteParser).
Refactor generateCapsule task (délègue domaine, group slider, dependsOn asciidoctor
supprimé). Tests 3 niveaux : 24 unit + 4 functional + 9 cucumber.

|===
| US | Description | Statut
| US-2.1 | Playwright via node-gradle | ✅ TERMINE
| US-2.2 | Tache `slider:visualTest` | ✅ TERMINE
| US-2.3 | Theme CSS reveal.js pro (Talaria) | ✅ TERMINE
| US-2.4 | Tests validation visuelle Playwright | ✅ TERMINE
| US-2.5 | Capsule Feed — extraction notes orateur → `*-script.txt` contrat capsule-gradle | ✅ TERMINÉ (S-020)
|===

_Detail complet_ : voir link:slider-plugin/.agents/INDEX.adoc#epic-sld-2-detail-us[INDEX → EPIC SLD-2].

'''
== EPIC SLD-3 — i18n 10 langues ✅ TERMINE

_Vision_ : Internationalisation 10 langues via contrats N0 `education.cccp:i18n-contracts:0.0.1`.
Pattern aligne sur bakery BKY-I18N. RTL pour arabe/ourdou. 7/7 US terminées.

_Dependance_ : i18n-contracts N0 (workspace-bom MEM-3, mavenLocal, publication Central en attente).

_Session 020 (2026-07-07)_ : US-3.6 TERMINÉ — 3 decks de démo multilingue
`slides/misc/capsule-feed-demo-{fr,en,ar}-deck.adoc` (4 slides chacun, notes orateur
FR/EN/AR, AR deck RTL via `:revealjs_direction: rtl`). Tests dogfood : 4 unit + 4 cucumber.

_Session 019 (2026-07-06)_ : US-3.3/3.4/3.5 TERMINÉS — Reveal.js UI i18n
(RevealUiMessages + RevealUiMessageCatalog 10 langues + RevealUiMessagesWriter
messages_{code}.js + generateRevealUiMessages task) + RTL support (RevealRtlResolver
+ câblage asciidoctorRevealJs setRightToLeft -Planguage=ar|ur) + Cucumber BDD
i18n fallback (8_i18n_fallback.feature 6 scenarios). 57 unit + 3 functional +
46 cucumber PASS.

|===
| US | Description | Statut | Estimation
| SLD-3.0 | Dependance i18n-contracts + DeckContext.language ISO 639-1 | ✅ TERMINÉ (S-014) | 2 pts
| SLD-3.1 | Configuration layer DSL + cascade 5 couches | ✅ TERMINÉ (S-015) | 3 pts
| SLD-3.2 | TranslatorManager 2→10 langues + LLM prompt adaptatif | ✅ TERMINÉ (S-018) | 3 pts
| SLD-3.3 | Reveal.js UI i18n — messages_{code}.js 10 langues + lang:{code} config | ✅ TERMINÉ (S-019) | 2 pts
| SLD-3.4 | RTL support arabe/ourdou + validation Playwright | ✅ TERMINÉ (S-019, resolver+câblage; validation Playwright deck complète en US-3.6) | 2 pts
| SLD-3.5 | Tests Cucumber BDD i18n | ✅ TERMINÉ (S-019) | 2 pts
| SLD-3.6 | Documentation + deck multilingue demo | ✅ TERMINÉ (S-020) | 1 pt
|===

_Total_ : 7 US, ~15 pts.
_Detail complet_ : voir link:slider-plugin/.agents/INDEX.adoc#epic-sld-3-detail-us[INDEX → EPIC SLD-3].

'''
== ✅ SLD-7 — Câblage Explicite translateDeck → Capsule ✅ TERMINÉ (S-041, 2026-07-31)

[cols="2,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité
| SLD-7.0 | Cadrage EPIC — contrat d'interface slider↔capsule pour pipeline multi-langue | 1 | P0
| SLD-7.1 | Tâche composite `translateAndGenerateCapsule` — dependsOn translateDeck → generateCapsule | 2 | P0
| SLD-7.2 | Tests Cucumber — scénario BDD pipeline complet slider→capsule | 3 | P1
|===

_Total_ : 3 US, ~6 pts (3/3 terminées, S-041). _Dépendance_ : SLD-6 terminé. _Source_ : Session 097 (cadrage initial), livré Session 041.