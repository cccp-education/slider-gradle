= PROMPT_REPRISE — slider-gradle — Session 022
:session: 022
:date: 2026-07-09

== Session 021 terminée — US-3.4-bis Validation visuelle Playwright RTL ✅

US-3.4-bis TERMINÉ (validation visuelle Playwright reportée depuis S-019) :
- Domaine DDD `slider.rtl` posé : `RtlAssertionCode` (enum 3 P0 + 1 P1),
  `RtlAssertionResult`/`RtlAssertionFailure` (résultat), `SlideRenderData`
  (snapshot Playwright-agnostic), `RtlSlideAssertion` (4 contraintes pure logic)
- Test functional `RtlPlaywrightFunctionalTest` — GradleRunner génère deck arabe
  `-Planguage=ar` (mavenLocal 0.0.7), Playwright-jvm ouvre HTML, extrait
  `SlideRenderData`, délègue à `RtlSlideAssertion.assertAll()`
- Cucumber BDD `10_rtl_visual_validation.feature` (6 scénarios)
- Révélation : Reveal.js gère RTL via `rtl: true` config JS + `.rtl` classe
  runtime, PAS via `<html dir="rtl">`. Assertions adaptées au modèle réel.
- Tests : 36 unit (+8) + 5 functional (+1) + 64 cucumber (+6) PASS

== Prochaine session — 022

EPICs SLD-0/1/2/3 + CNV-6/7/8 + Publication + US-3.4-bis tous TERMINÉS.
Slider-gradle en maintenance. Pistes possibles :

. Migration version plugin 0.0.1 → 0.0.2 (nouvelles tasks generateCapsule +
  generateRevealUiMessages + domaine capsule + slider.rtl) — publication
  Maven Central
. Exploration nouveaux EPICs (slides interactives, export PPTX, etc.)
. Mise à jour README i18n 10 langues avec nouvelles classes `slider.rtl`
  + `slider.capsule`

== Rappels

- GroupId : `education.cccp`, Plugin ID : `education.cccp.slider`
- Version publiée Central : 0.0.1 (build.gradle.kts hardcodé 0.0.1)
- mavenLocal : 0.0.7 (publié temporairement S-021 pour GradleRunner)
- conventions-gradle : mavenLocal 0.0.2 (logback-exclusion non publié Central)
- i18n-contracts : mavenLocal 0.0.2
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Domaine `slider.rtl` : 4 classes (RtlAssertionCode, RtlAssertionResult,
  SlideRenderData, RtlSlideAssertion)
- Domaine `slider.capsule` : 4 classes (CapsuleScript, SlideSegment,
  CapsuleScriptWriter, AsciidocSpeakerNoteParser)
- Reveal.js RTL = `rtl: true` config JS + `.rtl` classe runtime (PAS `<html dir>`)