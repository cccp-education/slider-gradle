= PROMPT_REPRISE — slider-gradle — Session 029
:session: 029
:date: 2026-07-12

== Session 028 terminée

README i18n (20 fichiers — 2 EN masters + 9 consommateurs + 9 plugins) mis à
jour avec le domaine DDD `slider.repository` (3 fichiers : SlideDeploymentRequest,
SlideDeployer, JGitSlidePusher) + 2 autres domaines DDD (`slider.capsule`,
`slider.rtl`). Nouvelle section "Domain model (DDD)" ajoutée dans les 10 README
consommateurs. Module layout enrichi dans les 10 README plugin. EPIC status
mis à jour dans toutes les langues (SLD-2/3/4 ✅, CNV-6/7/8 ✅, PUB 0.0.8 +
0.0.9 ✅).

Bump 0.0.8 → 0.0.9 (build.gradle.kts + 2 catalogues libs.versions.toml).
Publication Maven Central via nmcp — bundle `63048fec-9f69-4cfa-812c-7adb63fa0fef`,
publishingType AUTOMATIC, 3 artefacts signés (slider:0.0.9 + 2 plugin markers).
`./gradlew :slider:check` VERT — 211 PASS / 0 FAIL / 3 SKIP (inchangé).

== Prochaine session — 029

Pistes (par priorité) :
. Définir prochain EPIC — maintenance ou nouvelle fonctionnalité (vision
  produit à définir). Tous les EPICs SLD-0→4 + CNV-6/7/8 + PUB sont clos.
. Fix 2 tests Playwright @Disabled (gem JRuby isolé GradleRunner —
  investigation S-023, non fixable sans modification API GradleRunner).
  Piste bloquée techniquement.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.9 (build.gradle.kts hardcodé 0.0.9)
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps` (renommé S-026)
- Domaine `slider.repository` : 3 fichiers — SlideDeploymentRequest,
  SlideDeployer, JGitSlidePusher (zéro leak JGit hors adaptateur)
- Domaine `slider.rtl` : 4 classes (RtlAssertionCode, RtlAssertionResult,
  SlideRenderData, RtlSlideAssertion)
- Domaine `slider.capsule` : 4 classes (CapsuleScript, SlideSegment,
  CapsuleScriptWriter, AsciidocSpeakerNoteParser)
- 2 tests Playwright @Disabled (gem JRuby isolé GradleRunner)
- `./gradlew :slider:check` est VERT (depuis S-024)
- Tâche Gradle : `deploySlides` (constante TASK_PUBLISH_SLIDES =
  "deploySlides"), group "deploy"
- Exclusion locale `slider.steps.**` dans `test` task (le plugin
  conventions n'exclut que `*.scenarios.*` par défaut)
- Tous les EPICs SLD-0→4 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9 CLOS