= PROMPT_REPRISE — slider-gradle — Session 048
:session: 048
:date: 2026-08-04

== Session 047 terminée

SLD-9.1 — i18n AssistantManager via LLM (baby-step TDD/BDD 3 niveaux).

- **SLD-9.0** : cadrage EPIC SLD-9 (5 US ~10 pts) dans BACKLOG + INDEX.
- **SLD-9.1 RED** : `SliderMessagesTest.kt` créé (29 tests, 13 clés × EN+FR).
  Échec confirmé — clés absentes.
- **SLD-9.1 GREEN** : 13 clés ajoutées à `Messages_en.properties` (reindexRag,
  proposeDeckContext, generateDeck, helloOllama/OllamaStreaming/Gemini/
  GeminiStreaming/Mistral/MistralStreaming/HuggingFace/HuggingFaceStreaming
  descriptions + task.group.collect/slider-ai). 9 fichiers `Messages_*.properties` :
  traductions générées par LLM (`gpt-oss:120b-cloud` port 11437, 99 lignes).
  `AssistantManager.kt` : 15 strings hardcodées → `SliderMessages.get()`/`format()`
  avec cascade `resolveLanguage()`.
- **SLD-9.1 REFACTOR** : `MessagesFrTranslationTest.kt` +11 clés (38 tests).
  `AssistantManagerI18nFunctionalTest.kt` créé (6 tests GradleTestKit EN+FR).
  `9_assistant_manager_i18n.feature` créé (12 scénarios Cucumber) + Steps.
- `./gradlew :slider:check` BUILD SUCCESSFUL 0 FAIL / 1 SKIP (+47 tests).

== Prochaine session — 048

*Objectif* : SLD-9.2 — 9 tests de traduction par langue (zh/hi/es/ar/bn/pt/ru/ur + en).
Pattern `MessagesFrTranslationTest` : test paramétré vérifiant que chaque clé
diffère de l'anglais pour chaque langue. Génération LLM si des traductions
manquent.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator` (déprécié)
- Version publiée Central : 0.0.13 (build.gradle.kts hardcodé 0.0.13)
- BOM workspace-bom : Central 0.0.11
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5 (koog 1.0.0, R2DBC — socle N1, service LLM à exposer US-8.1)
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD (15) : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate` (déprécié), `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`,
  `slider.revealjs`, `slider.playwright` + `SliderConfig` + `SliderTasks`
- `SliderManager.kt` supprimé. `SliderPlugin.kt` 30 lignes.
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playwright)
- `./gradlew :slider:check` est VERT (0 FAIL, 1 SKIP)
- EPICs clos : SLD-0→7 + SLD-CR3 + CNV-6/7/8 + PUB 0.0.1→0.0.13 + CR-GLOBALE-002
- EPIC cadré : SLD-8 slider consomme codebase (5 US ~12 pts, à planifier)
- EPIC en cours : SLD-9 i18n Completion (SLD-9.1 ✅, reste 9.2/9.3/9.4)
- Build se lance depuis `slider-plugin/` (project `:slider`)
- Décisions : `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc` (001 PicoContainer
  rejeté, 002 slider consomme codebase comme socle LLM unifié)
