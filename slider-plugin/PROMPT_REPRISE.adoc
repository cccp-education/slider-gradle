= PROMPT_REPRISE — slider-gradle — Session 033
:session: 033
:date: 2026-07-14

== Session 032 terminée

Publication slider 0.0.10 sur Maven Central + mise à jour BOM (workspace-bom 0.0.9).
Bump 0.0.9 → 0.0.10 sur 3 fichiers (build.gradle.kts + 2 catalogues libs.versions.toml).
Bundle nmcp `ea117827-531a-4d3c-8a45-c58bc9d34eb1` AUTOMATIC, 3 artefacts signés
(slider:0.0.10 + 2 plugin markers). BOM 0.0.9 publiée (bundle `79395cf5`), slider
constraint 0.0.1 → 0.0.10 + correction module name (`slider-plugin` → `slider`).

`./gradlew :slider:check` VERT — 425 PASS / 0 FAIL / 2 SKIP (UP-TO-DATE, zéro
régression). 5 fichiers modifiés, zéro secret.

== Prochaine session — 033

*Objectif* : À définir. SLD-0→5 terminés, CNV-6/7/8 terminés, PUB 0.0.10 publié.
Pistes possibles :
- SLD-6 cadrage (nouvel EPIC feature)
- Rattrapage i18n autre borough
- Mise à jour BOM pour autres plugins (bakery, codex, etc.)

== Rappels

- GroupId : `education.ccp`, Plugin IDs : `education.ccp.slider` +
  `education.ccp.slider.translator`
- Version publiée Central : 0.0.10 (build.gradle.kts hardcodé 0.0.10)
- BOM workspace-bom : Central 0.0.9
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps` (renommé S-026)
- Domaines DDD : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.ai`, `slider.translation` (S-029)
- Domaine `slider.translation` : 8 fichiers — TranslationRequest,
  DeckTranslationPlan, TranslationResult (sealed 3 variants), TranslationOutcome,
  LanguageModelAdapter (port), DeckTranslator (domain service),
  OllamaLanguageModelAdapter (adaptateur langchain4j), TranslateDeckTask
- Tâche Gradle : `translateDeck` (group "translator"), inputs `-Pdeck.context`
  + `-Pdeck.adoc` (optionnel) + `-Ptarget.languages` (défaut 10)
- 2 tests Playwright @Disabled (gem JRuby isolé GradleRunner — piste bloquée)
- `./gradlew :slider:check` est VERT (425 PASS depuis S-030)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10