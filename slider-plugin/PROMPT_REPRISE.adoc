= PROMPT_REPRISE — slider-gradle — Session 031
:session: 031
:date: 2026-07-14

== Session 030 terminée

EPIC SLD-5 Deck Translation Pipeline TERMINÉ (7/7 US, 16 pts). US-5.6
Dogfooding livrée : 3 decks demo fr/en/ar étendus à 10 langues via
`translateDeck` avec LLM réel `gpt-oss:120b-cloud` (port 11439). 54 fichiers
générés (27 `.adoc` + 27 `deck-context.yml`).

`DeckTranslator.kt` enrichi : `ensureRtlDirection` post-traduction injecte
`:revealjs_direction: rtl` pour cibles RTL (ar, ur) et supprime l'attribut
pour cibles LTR (source RTL → cible LTR).

Validation Playwright : LTR (fr→en) `slides=5, rtl=false` ✅, RTL (fr→ar)
`slides=5, rtl=true, screenshot=32KB` ✅.

`./gradlew :slider:check` VERT — 425 PASS (293 unit + 15 functional + 117
cucumber), was 270. +155 tests, zéro régression. 2 SKIP Playwright @Disabled
inchangés.

README.md master EN mis à jour avec domaine `slider.translation` (8 fichiers)
+ EPIC SLD-5 ✅ DONE. 9 README.plugin/ traduits en attente.

== Prochaine session — 031

*Objectif* : Traduction README i18n (9 fichiers `README.plugin/README.*.md`)
avec domaine `slider.translation` (8 fichiers) + EPIC SLD-5 statut. Aligner
sur le master EN mis à jour en S-030.

_Etapes_ :
. Lire `README.md` master EN (section module layout + EPIC status).
. Traduire la section `slider.translation` dans les 9 README.plugin/
  (ar, bn, es, fr, hi, pt, ru, ur, zh).
. Mettre à jour le statut EPIC SLD-5 ✅ DONE dans les 9 fichiers.
. Vérifier `./gradlew :slider:check` VERT.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.9 (build.gradle.kts hardcodé 0.0.9)
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps` (renommé S-026)
- Domaines DDD : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.ai`, `slider.translation` (S-029)
- Domaine `slider.translation` : 8 fichiers — TranslationRequest,
  DeckTranslationPlan, TranslationResult (sealed 3 variants), TranslationOutcome,
  LanguageModelAdapter (port), DeckTranslator (domain service),
  OllamaLanguageModelAdapter (adaptateur langchain4j), TranslateDeckTask
- Tâche Gradle : `translateDeck` (group "translator"), inputs `-Pdeck.context`
  + `-Pdeck.adoc` (optionnel) + `-Ptarget.languages` (défaut 10)
- 2 tests Playwright @Disabled (gem JRuby isolé GradleRunner — piste bloquée)
- `./gradlew :slider:check` est VERT (425 PASS depuis S-030)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9
- EPIC SLD-5 TERMINÉ : 7/7 US (S-029 + S-030)