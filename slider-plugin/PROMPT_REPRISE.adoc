= PROMPT_REPRISE — slider-gradle — Session 017
:session: 017
:date: 2026-07-06

== Session 016 terminée — CNV-6/7 Migration functional-test + cucumber

CNV-6/7 ✅ TERMINÉ : 4/4 conventions plugins appliquées (gradle-plugin + publishing +
functional-test + cucumber). `build.gradle.kts` 251→157 lignes (-114, -45%).

. `functionalTest` : BUILD SUCCESSFUL (convention + failOnNoDiscoveredTests=false, tests commentés)
. `cucumberTest` : 17 tests, 10 FAIL préexistants (proposeDeckContext), 1 skip, 6 PASS — stable
. `test` : 22 PASS, 0 FAIL — **bug filtre package résolu** (effet de bord convention cucumber :
  `excludeTestsMatching("*.scenarios.*")` remplace l'ancien `com.cheroliv.slider.scenarios.**` S-007)

Variants préservées : buildscript annotations force, exclusions logback,
additionalDependencies mockito/coroutines/slf4j, compatibility configurationCache=false.

== Prochaine session — 017 : SLD-3 i18n US-3.2

. TranslatorManager modernisation — remplacer `setOf(FRENCH, ENGLISH)` par
  `LanguageCatalog.ALL` (2→10 langues)
. System prompt LLM adaptatif par langue (prompt traduction localisé)
. Utiliser `I18nConfig.supportedLanguages` pour générer les tâches de traduction

== Pattern de référence

- bakery-gradle BKY-I18N (TranslatorManager équivalent)
- i18n-contracts N0 : `LanguageCatalog.ALL`, `SupportedLanguage`, `TranslationRequest`

== Rappels

- GroupId : `education.cccp`, Plugin ID : `education.cccp.slider`
- Version publiée Central : 0.0.1
- i18n-contracts : mavenLocal 0.0.2
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED (test TranslatorManager 10 langues) → GREEN → REFACTOR
- CNV-6/7 complet : 4/4 conventions (ne plus toucher le build.gradle.kts sauf nécessité)