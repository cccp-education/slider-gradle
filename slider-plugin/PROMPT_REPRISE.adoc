= PROMPT_REPRISE — slider-gradle — Session 039
:session: 039
:date: 2026-07-22

== Session 038 terminée

SLD-6.4 — Extraction `Extensions` → `slider.extension` (nouveau domaine DDD,
3 fichiers, ~110 lignes) :

- `RevealJsPin.kt` (37 lignes) — value object data class. Pin Reveal.js
  (version gem "5.2.0" + organisation "hakimel" + repository "reveal.js" +
  tag "5.2.1"). Validation non-blanc sur les 4 champs. Pure — zéro Gradle.
- `SliderExtension.kt` (30 lignes) — DSL `slider {}` déménagée depuis
  `SliderPlugin.kt`. Propriétés `configPath`, `language`, `supportedLanguages`.
  `@Inject constructor(objects: ObjectFactory)`. Contrat API public préservé.
- `SliderExtensionRegistrar.kt` (53 lignes) — `configure(project, pin)`
  enregistre DSL + pin Reveal.js. Méthodes publiques `registerSliderExtension`
  + `pinRevealJsTemplate`.

`SliderManager.Extensions` → thin adapter (12 lignes, délègue à
`SliderExtensionRegistrar.configure(this)`). `SliderPlugin.SliderExtension`
→ `typealias slider.extension.SliderExtension` (préserve référence
`RagTasks.kt`). Imports morts nettoyés (SliderExtension + RevealJSExtension
retirés de `SliderManager.kt` ; ObjectFactory/Property/Inject retirés de
`SliderPlugin.kt`).

Baby-step TDD/BDD 3 niveaux : 9 unit (`RevealJsPinTest`) + 9 scénarios Cucumber
(`18_extension_decomposition.feature` + `ExtensionDecompositionSteps.kt`).
`./gradlew :slider:check` VERT — 516 PASS / 0 FAIL / 1 SKIP (+18 vs 498 S-037).

== Prochaine session — 039

*Objectif* : SLD-6.5 — Extraction `Repositories` + `Plugins` + `Dependencies`
→ `slider.wiring` (Gradle plumbing groupé). `GradleWiring` facade
(`configureRepositories`, `applyPlugins`, `configureDependencies`). Les 3
nested objects de `SliderManager` deviennent thin adapters qui délèguent au
domaine.

Enchaîner SLD-6.6 (Reveal.js tasks), SLD-6.7 (Playwright tasks), etc. jusqu'à
SLD-6.10 (`SliderManager` thin orchestrator < 100 lignes).

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.11 (build.gradle.kts hardcodé 0.0.11)
- BOM workspace-bom : Central 0.0.10
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD existants : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`
- Domaines DDD SLD-6 à créer : `slider.wiring`, `slider.revealjs`,
  `slider.playwright`
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playright)
- `./gradlew :slider:check` est VERT (516 PASS depuis S-038)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10/0.0.11
- EPIC en cours : SLD-6 (SLD-6.0 ✅, SLD-6.1 ✅, SLD-6.2 ✅, SLD-6.3 ✅,
  SLD-6.4 ✅, SLD-6.5→6.10 ⏳)
- Environnement : daemon Gradle reçoit parfois des signaux stop externes (autres
  sessions Gradle parallèles). Lancer `./gradlew :slider:test` en arrière-plan
  `nohup ... &` si blocage, puis inspecter `build/test-results/`.
- `SliderPlugin.SliderExtension` est désormais un `typealias` vers
  `slider.extension.SliderExtension` — préserve la compat API
  (`RagTasks.kt` référence `slider.SliderPlugin.SliderExtension`).