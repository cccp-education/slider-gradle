= PROMPT_REPRISE — slider-gradle — Session 035
:session: 035
:date: 2026-07-15

== Session 034 terminée

Fix des 2 tests Playwright `@Disabled` (S-023). Découplage architecturel :
les HTML fixtures sont pré-générés par le build racine via
`generatePlaywrightFixtures` (vrai Gradle home, gem JRuby présente). Les
tests chargent ces fixtures via `python3 -m http.server` + Playwright-jvm
headless Chromium — sans GradleRunner, sans JRuby isolé.

`PlaywrightFixtureSupport.kt` (nouveau) mutualise Browser lifecycle + HTTP
server. `SliderPlaywrightE2eTest` (LTR) + `RtlPlaywrightFunctionalTest` (RTL)
réécrits — 0 `@Disabled` restant.

`./gradlew :slider:check` VERT — 428 PASS / 0 FAIL / 1 SKIP (+3 vs 425).

== Prochaine session — 035

*Objectif* : À définir. SLD-0→5 terminés, CNV-6/7/8 terminés, PUB 0.0.10
publié, i18n audité complet, Playwright @Disabled fixé. Pistes :
- SLD-6 cadrage (nouvel EPIC feature)
- Rattrapage i18n autre borough
- Mise à jour BOM pour autres plugins (bakery, codex, etc.)
- Publication 0.0.11 (Playwright fix)

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.11 (build.gradle.kts hardcodé 0.0.11)
- BOM workspace-bom : Central 0.0.10
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps` (renommé S-026)
- Domaines DDD : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.ai`, `slider.translation` (S-029)
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié Playwright)
- `./gradlew :slider:check` est VERT (428 PASS depuis S-034)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10/0.0.11