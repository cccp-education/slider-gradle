= PROMPT_REPRISE — slider-gradle — Session 027
:session: 027
:date: 2026-07-11

== Sessions 025-026 terminées

S-025 : Refactor DDD `slider.repository` — extraction `SlideDeploymentRequest`
+ `GitCredentials` (value objects) + `SlideDeployer` (createRepoDir/copySlides/
cleanup, typed results). `FileOps` + `FileOperationResult` supprimés.
`Git.pushSlides` délègue au domaine. 16 unit + 2 functional + 9 cucumber BDD.

S-026 : Rename package `slider.scenarios` → `slider.steps`. Fix double
`scenarios` dans chemin `src/test/scenarios/slider/scenarios/` →
`src/test/scenarios/slider/steps/`. 12 fichiers renommés, glue Cucumber
mise à jour, exclusion locale `slider.steps.**` ajoutée au `test` task.

`./gradlew :slider:check` VERT — 196 PASS, 0 FAIL, 4 SKIP.

== Prochaine session — 027

Pistes (par priorité) :
. Extraction `JGitSlidePusher` (adaptateur) — `initAddCommitToSlides` +
  `pushSlide` sortis de `SliderManager.Git` vers `slider.repository`
. Tests fonctionnels `deploySlides` avec mock Git remote (bypass push)
. Mise à jour README i18n 10 langues avec domaine `slider.repository`
. Refactor `GitOperationResult` (models.kt) → `slider.repository`

== Rappels

- GroupId : `education.ccp`, Plugin IDs : `education.ccp.slider` +
  `education.ccp.slider.translator`
- Version publiée Central : 0.0.8 (build.gradle.kts hardcodé 0.0.8)
- conventions-gradle : mavenLocal 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps` (renommé S-026, was `slider.scenarios`)
- Domaine `slider.repository` : SlideDeploymentRequest, GitCredentials,
  SlideDeployer, RepoDirResult, CopyResult
- Domaine `slider.rtl` : 4 classes (RtlAssertionCode, RtlAssertionResult,
  SlideRenderData, RtlSlideAssertion)
- Domaine `slider.capsule` : 4 classes (CapsuleScript, SlideSegment,
  CapsuleScriptWriter, AsciidocSpeakerNoteParser)
- 2 tests Playwright @Disabled (gem JRuby isolé GradleRunner)
- `./gradlew :slider:check` est VERT (depuis S-024)
- Tâche Gradle : `deploySlides` (constante TASK_PUBLISH_SLIDES =
  "deploySlides"), group "deploy"
- Exclusion locale `slider.steps.**` dans `test` task (le plugin
  conventions n'exclut que `*.scenarios.*` par défaut)