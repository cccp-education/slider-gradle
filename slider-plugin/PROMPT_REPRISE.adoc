= PROMPT_REPRISE — slider-gradle — Session 018
:session: 018
:date: 2026-07-06

== Session 017 terminée — Baseline A + CNV-8 logback-exclusion ✅

A — Baseline restaurée :
- `AssistantManager.kt` rupture propre noms tâches RAG :
  `collectRagIndex` → `reindexRag`, `generateDeckContext` → `proposeDeckContext`
- `slider/build.gradle.kts` : conventions `0.0.2` (publiée Central, fix SCM `.git`)
- `cucumberTest` : 16 PASS / 0 FAIL / 2 SKIP (vs 6 PASS / 10 FAIL avant)
- `./gradlew :slider:check` vert

B — CNV-8 logback-exclusion convention :
- Nouveau plugin `education.cccp.build.logback-exclusion` (conventions-gradle)
- TDD : feature Gherkin français + steps + LogbackExclusionConventionsPlugin (24 lignes)
- `conventions-gradle cucumberTest` 19/19 PASS, `publishToMavenLocal` 0.0.2
- Migration slider : -13 lignes (bloc exclusion inline supprimé)

== Prochaine session — 018

. Option C reportée : publication Maven Central conventions-gradle 0.0.2
   avec nouveau plugin logback-exclusion (nécessite permission publish)
. Migration des 6 autres boroughs (bakery, capsule, plantuml, readme, flow,
   workspace-bom) vers `education.cccp.build.logback-exclusion` (~84 lignes)
. SLD-3 i18n US-3.2 (TranslatorManager 10 langues) — reprendre roadmap

== Rappels

- GroupId : `education.cccp`, Plugin ID : `education.cccp.slider`
- Version publiée Central : 0.0.1
- conventions-gradle : mavenLocal 0.0.2 (logback-exclusion non publié Central)
- i18n-contracts : mavenLocal 0.0.2
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR
- Gherkin en français, code en anglais, pas de commentaires