= PROMPT_REPRISE — slider-gradle — Session 019
:session: 019
:date: 2026-07-06

== Session 018 terminée — SLD-3 US-3.2 + English Only governance ✅

A — SLD-3 i18n US-3.2 TERMINÉ :
- `TranslatorManager.kt` : `setOf(FRENCH, ENGLISH)` → `LanguageCatalog.supportedCodes()` (2→10 langues)
- `getTranslatePromptMessage()` : adaptive prompt with native names via `LanguageCatalog.findByCode()`
- `createDisplaySupportedLanguagesTask` : native name display (Français, العربية, 中文, اردو…)
- `slider.translator` plugin registered in `gradlePlugin` block
- Baby-step TDD: 11 unit tests (TranslatorManagerLanguageTest + TranslatorPromptTest),
  4 functional tests (TranslatorFunctionalTest), 7 Cucumber scenarios (5_translator_i18n.feature)
- `./gradlew :slider:check` green (33 unit + 4 functional + 23 cucumber PASS / 1 SKIP)

B — English Only governance fix :
- Rule 7 added to `AGENT.adoc` (slider + conventions-gradle)
- Transverse rule added to workspace `AGENTS.adoc` (all boroughs)
- `logback-exclusion-conventions.feature` migrated FR→EN
- `SignUp.feature` (plantuml) migrated FR→EN
- Zero `# language: fr` remaining in foundry

== Prochaine session — 019

. SLD-3 US-3.3: Reveal.js UI i18n — `messages_{code}.js` for 10 languages + `lang: {code}` in config
. SLD-3 US-3.4: RTL support arabe/ourdou + Playwright validation
. SLD-3 US-3.5: Cucumber BDD i18n scenarios (generation per language, RTL, fallback)
. SLD-2 US-2.5: Feed capsule (Playwright screenshots → JSON for capsule-gradle)

== Rappels

- GroupId : `education.cccp`, Plugin ID : `education.cccp.slider`
- Version publiée Central : 0.0.1
- conventions-gradle : mavenLocal 0.0.2 (logback-exclusion non publié Central)
- i18n-contracts : mavenLocal 0.0.2
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR
- English only: code, Gherkin, comments — no French