= PROMPT_REPRISE — slider-gradle — Session 047
:session: 047
:date: 2026-08-04

== Session 046 terminée

Analyse vision + gouvernance + cadrage (zéro code).

- **Audit PicoContainer** : zéro implémentation `org.picocontainer` dans le
  workspace. `cucumber-picocontainer` (15 boroughs) = DI Cucumber de test (World/
  Steps), pas une DI de production. **Decision 001** : rejet assumé de PicoContainer
  comme conteneur DI — DI native Gradle (`BuildService`/`Provider`) suffit, jusqu'à
  réévaluation (critères listés).
- **Audit koog + R2DBC** : socle N1 **déjà publié** (codebase-gradle Central 0.0.5) —
  koog `ai.koog:koog-agents:1.0.0` (4 graphes : VibecodingGraph, KoogPlanningOrchestrator,
  KoogAugmentedContextGraph, MultiChannelContextGraph) + document-gradle
  (DocumentGenerationGraph). R2DBC implémenté (R2dbcAgenticChunkRepository). EPIC L ✅
  TERMINÉ (034-049). **Decision 002** : adopter koog+R2DBC comme socle IA slider.
- **EPIC SLD-8 cadré** (BACKLOG) : koog Orchestration du pipeline deck
  (`proposeDeckContext` → `generateDeck`), 5 US ~12 pts, méthode baby-step
  DDD/TDD/BDD 3 niveaux.
- **Doc pilotage** : `KOOG_AGENTIC_PATTERNS.adoc` → koog 1.0.0, EPIC L ✅, note DI.
- **Nouveau** : `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc` (Decision 001 + 002).

== Prochaine session — 047

*Objectif* : Planifier EPIC SLD-8 koog Orchestration.
- **US-8.0** : cadrage contrat koog — graphe `proposeDeckContext` → `generateDeck`
  sur la base RAG pgvector existante.
- **US-8.1** : domaine DDD `slider.koog` — dépendance `ai.koog:koog-agents:1.0.0`
  (via BOM workspace-bom) + `DeckPipelineGraph` (nodes : propose-context,
  validate-context, generate-deck).
- Nettoyage optionnel : sections obsolètes SLD-6 ("EN COURS 9/11") et SLD-7
  ("🔲 ... Session 097") du BACKLOG — les deux EPICs sont en fait terminés.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator` (déprécié)
- Version publiée Central : 0.0.13 (build.gradle.kts hardcodé 0.0.13)
- BOM workspace-bom : Central 0.0.11
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5 (koog 1.0.0, R2DBC — socle N1 consommable)
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD (15) : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate` (déprécié), `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`,
  `slider.revealjs`, `slider.playwright` + `SliderConfig` + `SliderTasks`
- `SliderManager.kt` supprimé. `SliderPlugin.kt` 30 lignes.
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playwright)
- `./gradlew :slider:check` est VERT (0 FAIL, 1 SKIP)
- EPICs clos : SLD-0→7 + SLD-CR3 + CNV-6/7/8 + PUB 0.0.1→0.0.13 + CR-GLOBALE-002
- EPIC cadré : SLD-8 koog Orchestration (5 US ~12 pts, à planifier)
- Build se lance depuis `slider-plugin/` (project `:slider`)
- Décisions : `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc` (001 PicoContainer
  rejeté, 002 koog+R2DBC socle N1)
