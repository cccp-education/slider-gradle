= PROMPT_REPRISE — slider-gradle — Session 041
:session: 041
:date: 2026-07-23

== Session 040 terminée

US SLD-6.8 livrée — Extraction `registerAsciidocCapsuleTask` (~30 lignes) depuis
`SliderManager.Tasks` vers une consolidation du domaine `slider.capsule` existant
(déjà extracted S-020 : `CapsuleScript`/`SlideSegment`/`CapsuleScriptWriter`/
`AsciidocSpeakerNoteParser`). La tâche Gradle `generateCapsule` déménage dans un
`CapsuleTaskRegistrar` thin adapter.

- **SLD-6.8 Capsule** → `slider.capsule` consolidé (2 nouveaux fichiers, ~138
  lignes) :
  - `CapsuleTaskDomain.kt` (78l) — value objects purs, zéro dépendance Gradle :
    `CapsuleAdocDir(projectDir)` résout `<projectDir>/slides/misc` + `adocFiles()`
    (`.adoc` triés, empty si dir absent), `CapsuleScriptDir(buildDir)` résout
    `<buildDir>/capsule` + `ensureCreated()` idempotent + `scriptFileFor(name)`
    (`<name>-script.txt`), `CapsuleTaskNames` (`GENERATE_CAPSULE`, `GROUP`,
    `DESCRIPTION`).
  - `CapsuleTaskRegistrar.kt` (60l) — thin Gradle adapter `register(project)` :
    `DefaultTask` `generateCapsule` group=slider, `outputs.upToDateWhen { false }`,
    pour chaque `.adoc` parse via `AsciidocSpeakerNoteParser.parse`, skip si
    `script.isEmpty` + log warning, sinon write `CapsuleScriptWriter.write` +
    log slide count. Comportement bit-for-bit préservé.

`SliderManager.Tasks.registerAsciidocCapsuleTask` (~30 lignes) supprimé,
remplacé par `slider.capsule.CapsuleTaskRegistrar.register(this)`. Imports morts
nettoyés : `AsciidocSpeakerNoteParser` + `CapsuleScriptWriter` retirés de
`SliderManager.kt`. `SliderManager.kt` 570→**552 lignes** (was 968 S-035).

Baby-step TDD/BDD 3 niveaux : 12 unit (`CapsuleTaskDomainTest`) + 13 scénarios
Cucumber (`22_capsule_task_decomposition.feature` + `CapsuleTaskDecompositionSteps`,
steps préfixés `capsule`). `./gradlew :slider:check` VERT — **654 PASS / 0 FAIL /
1 SKIP** (+21 vs 633 S-039).

== Prochaine session — 041

*Objectif* : SLD-6.9 — Consolidation `registerPublishSlidesTask` + `Git` object
→ `slider.repository.PublishTaskRegistrar` + `SlidePublisher`. Le nested object
`Git` (~55 lignes, déjà thin adapter vers `slider.repository` depuis S-025/027)
déménage dans le domaine `slider.repository`. Fix deprecation
`properties[CONFIG_PATH_KEY]` ligne 744 (déjà traité US-6.2 pour ligne 82, ici
pour 744). Functional GradleTestKit + Cucumber `23_publish_task_decomposition.feature`.

Enchaîner SLD-6.10 (SliderManager → thin orchestrator < 100 lignes,
vérification 654 PASS maintenus). EPIC SLD-6 se termine à 11/11.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.11 (build.gradle.kts hardcodé 0.0.11)
- BOM workspace-bom : Central 0.0.10
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD existants (14) : `slider.repository`, `slider.capsule` (consolidé),
  `slider.rtl`, `slider.i18n`, `slider.translate`, `slider.translation`,
  `slider.scaffold`, `slider.config`, `slider.prerequisite`, `slider.extension`,
  `slider.wiring`, `slider.revealjs`, `slider.playwright` + `SliderManager`
  résiduel (552 lignes)
- Domaines DDD SLD-6 restants : `slider.repository` (consolidation
  PublishTaskRegistrar + SlidePublisher + Git object déménagement)
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playright)
- `./gradlew :slider:check` est VERT (654 PASS depuis S-040)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10/0.0.11
- EPIC en cours : SLD-6 (SLD-6.0 ✅, 6.1 ✅, 6.2 ✅, 6.3 ✅, 6.4 ✅, 6.5 ✅,
  6.6 ✅, 6.7 ✅, 6.8 ✅, SLD-6.9→6.10 ⏳)
- Environnement : daemon Gradle reçoit parfois des signaux stop externes (autres
  sessions Gradle parallèles). Lancer `./gradlew :slider:check` en arrière-plan
  `nohup ... &` si blocage, puis inspecter `build/test-results/`.
- `SliderManager.kt` est désormais ~552 lignes (was 968 S-035). Cible SLD-6.10 :
  thin orchestrator < 100 lignes. Restent à extraire : registerPublishSlidesTask
  (~30 lignes) + Git object (~55 lignes) + registerReportTestsTask/
  registerReportFunctionalTestsTask (~30 lignes) +
  registerGenerateRevealUiMessagesTask (~15 lignes) + registerTranslateDeckTask
  (délègue déjà à slider.translation) + registerAsciidoctorTask (~7 lignes).
- Build se lance depuis `slider-plugin/` (project `:slider`).