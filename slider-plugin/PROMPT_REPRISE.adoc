= PROMPT_REPRISE — slider-gradle — Session 040
:session: 040
:date: 2026-07-22

== Session 039 terminée

Trois US de l'EPIC SLD-6 livrées en une session :

- **SLD-6.5 Wiring** → `slider.wiring` (2 fichiers, ~210 lignes) :
  `WiringSpec.kt` (GroupRouting sealed, RepositoryKind sealed MAVEN/MAVEN_CENTRAL/IVY,
  RepositorySpec, PluginSpec, GemDependency, WiringSpec.DEFAULT 6 repos + 3 plugins + 1 gem)
  + `GradleWiring.kt` (adapter `configure(project, spec)`). 28 unit + 2 functional
  + 17 cucumber (`19_wiring_decomposition.feature`).
- **SLD-6.6 Reveal.js** → `slider.revealjs` (2 fichiers, ~240 lignes) :
  `RevealJsDomain.kt` (RevealJsAttributesSpec 14 attrs, RevealJsOutputDir,
  SlideSourceDir, SlideMetadata, SlideMetadataScanner, DashboardJsonSerializer,
  CleanBuildTarget/CleanReport) + `RevealJsTaskRegistrar.kt` (register cleanBuild
  + asciidoctorRevealJs + generateDashboard, const task names). 24 unit + 17 cucumber
  (`20_revealjs_decomposition.feature`).
- **SLD-6.7 Playwright** → `slider.playwright` (2 fichiers, ~170 lignes) :
  `PlaywrightDomain.kt` (PlaywrightDir, ServeCommand, InstallCommand DEFAULT chromium,
  VisualTestCommand, PlaywrightTaskNames) + `PlaywrightTaskRegistrar.kt` (register
  serveSlides + installPlaywright + visualTest). 16 unit + 19 cucumber
  (`21_playwright_decomposition.feature`).

`SliderManager.kt` : 3 nested objects (Repositories/Plugins/Dependencies) → thin
adapters 1-ligne. 6 méthodes privées Tasks supprimées (cleanSlidesBuild,
asciidoctorRevealJs, dashboard, serveSlides, installPlaywright, visualTest) +
helper playwrightDir(). Imports morts nettoyés. SliderManager.kt ~844→~570
lignes. 6 nouveaux fichiers domaine pur, ~620 lignes extraites.

Baby-step TDD/BDD 3 niveaux : +68 unit + 2 functional + 53 scénarios Cucumber.
`./gradlew :slider:check` VERT — **633 PASS / 0 FAIL / 1 SKIP** (+117 vs 516 S-038).

== Prochaine session — 040

*Objectif* : SLD-6.8 — Extraction `registerAsciidocCapsuleTask` →
`slider.capsule.CapsuleTaskRegistrar` (consolidation avec le domaine
`slider.capsule` existant — CapsuleScript/SlideSegment/CapsuleScriptWriter/
AsciidocSpeakerNoteParser déjà extraits S-020). La tâche Gradle
`generateCapsule` déménage dans un `CapsuleTaskRegistrar` adapter.
Functional GradleTestKit + Cucumber `22_capsule_task_decomposition.feature`.

Enchaîner SLD-6.9 (registerPublishSlidesTask + Git → slider.repository.PublishTaskRegistrar +
SlidePublisher consolidation), SLD-6.10 (SliderManager → thin orchestrator < 100 lignes,
vérification 633 PASS maintenus). EPIC SLD-6 se termine à 11/11.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.11 (build.gradle.kts hardcodé 0.0.11)
- BOM workspace-bom : Central 0.0.10
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD existants : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`,
  `slider.revealjs`, `slider.playwright`
- Domaines DDD SLD-6 restants : `slider.capsule` (consolidation CapsuleTaskRegistrar),
  `slider.repository` (consolidation PublishTaskRegistrar)
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playright)
- `./gradlew :slider:check` est VERT (633 PASS depuis S-039)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10/0.0.11
- EPIC en cours : SLD-6 (SLD-6.0 ✅, 6.1 ✅, 6.2 ✅, 6.3 ✅, 6.4 ✅, 6.5 ✅,
  6.6 ✅, 6.7 ✅, SLD-6.8→6.10 ⏳)
- Environnement : daemon Gradle reçoit parfois des signaux stop externes (autres
  sessions Gradle parallèles). Lancer `./gradlew :slider:check` en arrière-plan
  `nohup ... &` si blocage, puis inspecter `build/test-results/`.
- `SliderManager.kt` est désormais ~570 lignes (was 968 S-035). Cible SLD-6.10 :
  thin orchestrator < 100 lignes. Restent à extraire : registerPublishSlidesTask
  (~30 lignes) + Git object (~55 lignes) + registerAsciidocCapsuleTask (~25 lignes)
  + registerReportTestsTask/registerReportFunctionalTestsTask (~30 lignes) +
  registerGenerateRevealUiMessagesTask (~15 lignes) + registerTranslateDeckTask
  (délègue déjà à slider.translation) + registerAsciidoctorTask (~7 lignes).