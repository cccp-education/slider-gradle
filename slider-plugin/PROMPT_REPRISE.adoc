= PROMPT_REPRISE — slider-gradle — Session 043
:session: 043
:date: 2026-08-01

== Session 042 terminée

EPIC SLD-6 TERMINÉ (11/11 US, ~20 pts) — SliderManager decomposition.

- **SLD-6.9** (2 pts) : Consolidation `registerPublishSlidesTask` + `Git` object
  → `slider.repository.PublishTaskRegistrar` + `SlidePublisher`. `SlidePublisher.kt`
  (thin Gradle adapter, ex-`SliderManager.Git.pushSlides`) + `PublishTaskRegistrar.kt`
  (task registrar, ex-`SliderManager.Tasks.registerPublishSlidesTask`). Cucumber
  `20_publish_task_decomposition.feature` 3/3 PASS. Deprecation
  `properties[CONFIG_PATH_KEY]` déjà résolue US-6.2 — `localConf` utilise
  `findProperty()`.
- **SLD-6.10** (2 pts) : `SliderManager` supprimé, `SliderPlugin` 30 lignes délègue
  directement à 5 registrars (SliderConfig, SliderPrerequisiteRegistrar,
  SliderScaffoldRegistrar, SliderWiringRegistrar, SliderTasks). 15 domaines DDD.

Baby-step TDD/BDD 3 niveaux : 3 Cucumber (20_publish_task_decomposition.feature).
`./gradlew :slider:check` VERT — 421 unit (+2 Playwright préexistants) + functional
OK + Cucumber OK.

== Prochaine session — 043

*Objectif* : À définir. Tous les EPICs SLD-0→7 sont terminés. Backlog vide.
Prochaines pistes : SLD-8 (nouvel EPIC à cadrer), publication 0.0.13, ou
maintenance.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.12 (build.gradle.kts hardcodé 0.0.12)
- BOM workspace-bom : Central 0.0.11
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD (15) : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`,
  `slider.revealjs`, `slider.playwright` + `SliderConfig` + `SliderTasks`
- `SliderManager.kt` supprimé. `SliderPlugin.kt` 30 lignes.
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playwright)
- `./gradlew :slider:check` est VERT (421 unit + functional OK + Cucumber OK)
- EPICs clos : SLD-0→7 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10/0.0.11/0.0.12
- Build se lance depuis `slider-plugin/` (project `:slider`)
