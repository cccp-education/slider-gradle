= PROMPT_REPRISE — slider-gradle — Session 070
:session: 070
:date: 2026-09-07

== Sessions 066-069 terminées — EPIC PUB 0.0.18 clôturé, borough DORMANT

* S-066 : Publication slider 0.0.16 + BOM 0.0.17 + sync capsule.
* S-067 : Aplatissement structure `slider-plugin/slider/` → `slider-plugin/`
  (fix contrat d'architecture, 211 fichiers déplacés `git mv`, build
  mono-projet autonome, `./gradlew check` VERT).
* S-068 : Audit version + EPIC PUB bump backlog (gouvernance pure).
* S-069 : Publication slider **0.0.18** + BOM **0.0.19** sur Maven Central
  (bundles `3720936a` / `2160075f`). EPIC PUB 0.0.18 TERMINÉ 3/3 US.
  Session pure ops (zéro code production hors bump version).

== Prochaine session — 070

[IMPORTANT]
Borough DORMANT — tous EPICs feature clos (SLD-0→11, SLD-CR3, CR-GLOBALE-002,
CNV-6/7/8, PUB 0.0.1→0.0.18). Aucun travail planifié.

=== ⚠️ État git post-S-069 à consigner (à faire en ouverture)

Commits post-S-069 non consignés dans un rapport de session :

* `1237a2c` — bump codebase-plugin 0.0.7 → 0.0.11 (sync Central)
* `f279468` — remove opencode.json + update Gradle & Kotlin
* `6d1d8cb` — align conventions-plugin 0.0.4

Action d'ouverture : `git log --oneline -10`, confirmer intentionnels,
conserver consignation pour un futur rapport si absent.

=== Option A — Maintenance

* Dette technique ciblée, bumps de dépendances, code review.
* Hygiene : 3 backups orphelins à la racine (`build.gradle.kts.backup`,
  `.bak3`, `.backup4`) — purge à valider par le pilote.

=== Option B — Cross-borough

* MEM-CAT-ROLLOUT (MEMPHIS) : le catalogue workspace référence déjà
  `slider-plugin` (fix EPIC MEMPHIS-BOM-FIX). Rien à faire côté slider.
* Assister bakery/capsule si demandé.

=== Option C — Nouvel EPIC

* Cadrer si besoin identifié.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator` (déprécié)
- Version locale : 0.0.18 (Central : 0.0.18). Conventions 0.0.4.
- BOM workspace-bom : Central 0.0.31 (MEM-CAT-ROLLOUT S-032, constraint
  slider-plugin 0.0.18)
- codebase-plugin : Central 0.0.10 (local 0.0.11)
- conventions-gradle : Central 0.0.3 (local 0.0.4)
- i18n-contracts : Central 0.0.2
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD (16) : `slider.repository`, `slider.rtl`, `slider.i18n`,
  `slider.translate` (déprécié), `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`,
  `slider.revealjs`, `slider.playwright`, `slider.content`,
  `slider.pipeline` (clos S-065 — 10 prod + 4 test),
  `SliderConfig` + `SliderTasks` + `slider.ai` (adapter N1 codebase, S-073
  LLM-ADAPTER-EXTRACT : la copie locale `LlmProviderChatModelAdapter` est
  supprimée, le borough consomme `codebase.koog.llm.adapter`).
- `SliderManager.kt` supprimé. `SliderPlugin.kt` 30 lignes.
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playwright)
- `./gradlew check` VERT (959 unit + 45 functional + 265 cucumber,
  0 FAIL, 1 SKIP) — baseline S-065/S-069 préservée
- EPICs clos : SLD-0→11 + SLD-CR3 + CNV-6/7/8 + PUB 0.0.1→0.0.18 + CR-GLOBALE-002
- Build se lance depuis `slider-plugin/` (project `:slider`)
- Décisions : `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc`
  (001 PicoContainer rejeté, 002 slider consomme codebase)
- Patterns koog : `office/pilotage/codebase/KOOG_AGENTIC_PATTERNS.adoc`