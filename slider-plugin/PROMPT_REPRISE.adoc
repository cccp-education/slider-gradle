= PROMPT_REPRISE — slider-gradle — Session 037
:session: 037
:date: 2026-07-21

== Session 036 terminée

SLD-6.2 — Extraction `Configuration` → `slider.config` (nouveau domaine DDD, 3
fichiers, ~110 lignes) :

- `ConfigPath.kt` (21 lignes) — value object `@JvmInline value class`, validation
  non-blanc, résolution relative/absolue contre base dir. Pure, zéro Gradle.
- `YamlMapperFactory.kt` (34 lignes) — factory du `ObjectMapper` partagé
  (YAMLFactory + Kotlin + Arrow + WRITE_DATES_AS_TIMESTAMPS disabled). Remplace
  l'extension property `SliderManager.Configuration.yamlMapper`.
- `SlidesConfigLoader.kt` (70 lignes) — domain service. `load(File, mapper)` +
  `load(configPath, baseDir, mapper)` + `emptyConfiguration()` fallback. Remplace
  `SliderManager.Configuration.readSlidesConfigurationFile`.

`SliderManager.Configuration` → thin adapter. `Project.localConf` :
`properties[CONFIG_PATH_KEY]` → `findProperty(CONFIG_PATH_KEY)` (zéro warning).
`registerPublishSlidesTask` (ligne 638) : re-désérialisation `YAMLMapper().readValue`
supprimée — réutilise `localConf`. Imports morts nettoyés (5 imports Jackson/Arrow
retirés de `SliderManager.kt`). Baby-step TDD/BDD 3 niveaux : 16 unit
(ConfigPathTest 4 + YamlMapperFactoryTest 5 + SlidesConfigLoaderTest 7) + 15
scénarios Cucumber (`16_config_decomposition.feature` + `ConfigDecompositionSteps.kt`).
`./gradlew :slider:check` VERT 466 PASS / 0 FAIL / 1 SKIP (+11 vs 455).

== Prochaine session — 037

*Objectif* : SLD-6.3 — extraction `Prerequisites` → `slider.prerequisite`.
`JavaVersionGuard` (value object + `requireJava23()`). Extraction de la logique
pure (parse version + guard) hors du contexte Gradle. `SliderManager.Prerequisites`
(lignes ~126-141) contient :

- `checkJavaVersion()` — `JavaVersion.current().majorVersion.toInt()` + `require(...)
  Java 23+` avec message d'erreur.

Migration : `JavaVersionGuard` domaine pur (prend `JavaVersion` en input, zéro
dépendance Gradle), `SliderManager.Prerequisites` devient thin adapter qui passe
`JavaVersion.current()` au guard.

Enchaîner SLD-6.4 (Extensions → `slider.extension`), SLD-6.5 (Wiring), etc.

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.11 (build.gradle.kts hardcodé 0.0.11)
- BOM workspace-bom : Central 0.0.10
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD existants : `slider.repository`, `slider.capsule`, `slider.rtl`,
  `slider.i18n`, `slider.translate`, `slider.translation`, `slider.scaffold`,
  `slider.config`
- Domaines DDD SLD-6 à créer : `slider.prerequisite`, `slider.extension`,
  `slider.wiring`, `slider.revealjs`, `slider.playwright`
- Playwright fixtures : `./gradlew generatePlaywrightFixtures` (racine)
  → `build/playwright-fixtures/{ltr,rtl}/`
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playright)
- `./gradlew :slider:check` est VERT (466 PASS depuis S-036)
- EPICs clos : SLD-0→5 + CNV-6/7/8 + PUB 0.0.1/0.0.8/0.0.9/0.0.10/0.0.11
- EPIC en cours : SLD-6 (SLD-6.0 ✅, SLD-6.1 ✅, SLD-6.2 ✅, SLD-6.3→6.10 ⏳)