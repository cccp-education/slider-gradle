= PROMPT_REPRISE — slider-gradle — Session 026
:session: 026
:date: 2026-07-11

== Session 025 terminée — Refactor DDD slider.repository

Extraction du domaine `slider.repository` depuis `SliderManager.Git`.
Baby-step DDD/TDD/BDD 3 niveaux (unit → functional → cucumber).

Domaine `slider.repository` posé (2 fichiers production) :
- `SlideDeploymentRequest` + `GitCredentials` — value objects validés
  à la construction (slidesDir exists+isDirectory, tous String non-blank)
- `SlideDeployer` — `createRepoDir`/`copySlides`/`cleanupRepoDir`/
  `cleanupSlidesDir` avec typed results (`RepoDirResult`/`CopyResult`)

Refactor `SliderManager.Git.pushSlides` : délègue file-system au domaine.
`FileOps` + `FileOperationResult` supprimés (dead code).

Tests ajoutés : 16 unit (10+6) + 2 functional + 9 cucumber BDD.
`./gradlew :slider:check` VERT — 196 PASS, 0 FAIL, 4 SKIP.

== Prochaine session — 026

Pistes (par priorité) :
. Extraction `JGitSlidePusher` (adaptateur) — `initAddCommitToSlides` +
  `pushSlide` sortis de `SliderManager.Git` vers `slider.repository`
. Tests fonctionnels `deploySlides` avec mock Git remote (bypass push)
. Mise à jour README i18n 10 langues avec domaine `slider.repository`
. Refactor `GitOperationResult` (models.kt) → `slider.repository`

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.8 (build.gradle.kts hardcodé 0.0.8)
- conventions-gradle : mavenLocal 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Domaine `slider.repository` : 2 classes (SlideDeploymentRequest,
  SlideDeployer) + 2 value objects (GitCredentials, typed results)
- Domaine `slider.rtl` : 4 classes (RtlAssertionCode, RtlAssertionResult,
  SlideRenderData, RtlSlideAssertion)
- Domaine `slider.capsule` : 4 classes (CapsuleScript, SlideSegment,
  CapsuleScriptWriter, AsciidocSpeakerNoteParser)
- 2 tests Playwright @Disabled (gem JRuby isolé GradleRunner, non fixable
  sans withGradleUserHomeDir dans l'API GradleRunner 9.6.1)
- `./gradlew :slider:check` est VERT (depuis S-024)
- Tâche Gradle : `deploySlides` (constante TASK_PUBLISH_SLIDES =
  "deploySlides"), group "deploy"