= PROMPT_REPRISE — slider-gradle — Session 062
:session: 062
:date: 2026-08-05

== Session 061 terminée — Cadrage formel SLD-8.3 DeckPipelineGraph ✅

Cadrage formel US-8.3 `DeckPipelineGraph` (koog orchestration). Zéro code modifié —
session de vision et découpage baby-step. Patterns koog 1.0.0 consommés depuis
`office/pilotage/codebase/KOOG_AGENTIC_PATTERNS.adoc` (15 patterns, architecture
"koog orchestre / langchain4j exécute"). Audit codebase : `KoogAugmentedContextGraph`
(pattern hybride graphe koog + `execute()` séquentiel), `AugmentedState` (data class
immutable), `LlmProvider` (fun interface suspend), `LlmBuildService` (BuildService Gradle).

Domaine cible : `slider.pipeline` (nouveau), `DeckState` (data class immutable, 11 champs),
`DeckContextValidator` (validation pure, sealed Valid/Invalid), `DeckPipelineGraph`
(graphe koog `strategy<DeckState, DeckState>` — 3 nœuds propose-context/validate-context/
generate-deck + edges conditionnels), `GenerateDeckPipelineTask` (tâche Gradle thin
`generateDeckPipeline`).

4 baby-steps TDD/BDD prévus (~9 pts) : 8.3a `DeckState`+`DeckContextValidator` (20-25 unit)
→ 8.3b `DeckPipelineGraph` (15-20 unit mock LLM) → 8.3c `GenerateDeckPipelineTask`
(5-8 functional) → 8.3d Cucumber `26_deck_pipeline_koog.feature` (8-12 scénarios).

Rapport : `slider-plugin/.agents/sessions/061-cadrage-sld-8.3.adoc`.

== Prochaine session — 062

. SLD-8.3 baby-step 8.3a : `DeckState` + `DeckContextValidator` (value objects purs,
   domaine `slider.pipeline`). RED → GREEN → REFACTOR. 20-25 unit tests purs (zéro
   Gradle, zéro LLM, zéro koog). Valider `./gradlew :slider:compileTestKotlin` VERT.
. Publication BOM 0.0.16 + codebase-plugin 0.0.5 sur Maven Central (si permission) —
   prérequis pour publication slider 0.0.16 future.

== Rappels
- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` + `education.cccp.slider.translator` (déprécié)
- Version publiée Central : 0.0.15 (build.gradle.kts hardcodé 0.0.15)
- BOM workspace-bom : Central 0.0.15 (mavenLocal 0.0.16 post-S-176)
- codebase-plugin : Central 0.0.5 (mavenLocal 0.0.5 republié S-060)
- conventions-gradle : Central 0.0.2
- i18n-contracts : Central 0.0.2
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Package Cucumber steps : `slider.steps`
- Domaines DDD (15) : `slider.repository`, `slider.rtl`, `slider.i18n`,
  `slider.translate` (déprécié), `slider.translation`, `slider.scaffold`,
  `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`,
  `slider.revealjs`, `slider.playwright`, `slider.content` + `SliderConfig` + `SliderTasks`
  + `slider.ai` (LlmProviderChatModelAdapter, AssistantManager)
- Domaine cible SLD-8.3 : `slider.pipeline` (nouveau — `DeckState`, `DeckContextValidator`,
  `DeckPipelineGraph`, `GenerateDeckPipelineTask`)
- `SliderManager.kt` supprimé. `SliderPlugin.kt` 30 lignes.
- 1 test cucumber @Skip préexistant (JRuby gem isolé — non lié playwright)
- `./gradlew :slider:check` VERT (0 FAIL, 900 unit, 38 functional, 254 cucumber)
- EPICs clos : SLD-0→7 + SLD-9 + SLD-10 + SLD-11 + SLD-CR3 + CNV-6/7/8 + PUB 0.0.1→0.0.15 + CR-GLOBALE-002
- EPIC en cours : SLD-8 (US-8.0/8.1/8.2 ✅, US-8.3 cadré S-061, US-8.4 à planifier)
- Build se lance depuis `slider-plugin/` (project `:slider`)
- Décisions : `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc`
  (001 PicoContainer rejeté, 002 slider consomme codebase)
- Rapport 061 : `slider-plugin/.agents/sessions/061-cadrage-sld-8.3.adoc`
- Patterns koog : `office/pilotage/codebase/KOOG_AGENTIC_PATTERNS.adoc`
- Graphe cible SLD-8.3 : propose-context → validate-context → generate-deck (edges conditionnels)
- Pattern référence codebase : `codebase.koog.KoogAugmentedContextGraph` (graphe + execute séquentiel)