= PROMPT_REPRISE — slider-gradle — Session 024
:session: 024
:date: 2026-07-09

== Session 023 terminée — Investigation Playwright Gem JRuby 🔍

Investigation des 2 tests Playwright échouant (`RtlPlaywrightFunctionalTest` +
`SliderPlaywrightE2eTest`) — `LoadError: no such file to load -- asciidoctor-revealjs`.

Root cause : `GradleRunner.create()` crée un Gradle user home isolé dans `/tmp`.
JRuby ne trouve pas la gem `asciidoctor-revealjs` dans cet environnement vierge.
5 approches testées, aucune applicable dans l'état actuel de l'API GradleRunner 9.6.1.

Solutions possibles :
- `gem install asciidoctor-revealjs` système-wide
- Attendre que GradleRunner expose `withGradleUserHomeDir`
- `@Disabled` avec raison documentée

== Prochaine session — 024

EPICs SLD-0/1/2/3 + CNV-6/7/8 + Publication 0.0.1 + US-3.4-bis +
Publication 0.0.8 tous TERMINÉS. Slider-gradle en maintenance. Pistes :

. `@Disabled` les 2 tests Playwright avec raison documentée (gem JRuby isolé)
. Exploration nouveaux EPICs (slides interactives, export PPTX, etc.)
. Mise à jour README i18n 10 langues avec nouvelles classes `slider.rtl`
  + `slider.capsule` + version 0.0.8

== Rappels

- GroupId : `education.cccp`, Plugin IDs : `education.cccp.slider` +
  `education.cccp.slider.translator`
- Version publiée Central : 0.0.8 (build.gradle.kts hardcodé 0.0.8)
- mavenLocal : 0.0.8 (aligné)
- conventions-gradle : mavenLocal 0.0.2
- i18n-contracts : Central 0.0.2
- codebase-plugin : Central 0.0.5
- 10 langues cibles : en, zh, hi, es, fr, ar, bn, pt, ru, ur
- Baby-step TDD : RED → GREEN → REFACTOR (unit → functional → cucumber)
- English only: code, Gherkin, comments — no French
- Domaine `slider.rtl` : 4 classes (RtlAssertionCode, RtlAssertionResult,
  SlideRenderData, RtlSlideAssertion)
- Domaine `slider.capsule` : 4 classes (CapsuleScript, SlideSegment,
  CapsuleScriptWriter, AsciidocSpeakerNoteParser)
- Reveal.js RTL = `rtl: true` config JS + `.rtl` classe runtime (PAS `<html dir>`)
- 2 tests Playwright échouent (gem JRuby isolé, préexistant, non fixable sans
  `withGradleUserHomeDir` dans GradleRunner API)
