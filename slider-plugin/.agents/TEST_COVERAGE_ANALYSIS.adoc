= Test Coverage Analysis — Slider Gradle

_Statut session 022_ : Bump 0.0.8 + Publication Maven Central. Aucun test
modifié — bumps de versions uniquement (slider 0.0.1→0.0.8, codebase 0.0.1→0.0.5,
readme 0.0.0→0.0.1) + correction namespace plugin ID `slider.translator`→
`education.ccp.slider.translator` (build.gradle.kts + TranslatorFunctionalTest.kt).
Zéro régression confirmée : 94 unit (2 fail Playwright env préexistant) +
65 cucumber + 11 functional identiques pré/post bump (clean check comparé).

_Statut session 006_ : SLD-3a Playwright E2E fix. 3 bugs corrigés, test unitaire Playwright PASSED (49s). 1 fichier modifié : `SliderPlaywrightE2eTest.kt`.

| Session | Fichiers testés | Couverture | Notes
| 006 | `SliderPlaywrightE2eTest.kt` (242 lignes, 1 test + 1 helper) — GradleRunner mavenLocal → asciidoctorRevealJs BUILD SUCCESSFUL, HTTP local (npx serve) navigate, Playwright assertions title/slides/h2/bounding box/screenshot PASS | 1/1 Gradle ✅, 1/1 Playwright ✅ | 3 bugs corrigés : (1) file:// → HTTP npx serve + startHttpServer polling, (2) chemin output build/docs/asciidocRevealJs/intro.html, (3) strict mode .first() sur 4 sections. Screenshot > 10 KB, bbox viewport contrainte OK.
| 005 | `SliderPlaywrightE2eTest.kt` (177 lignes, 1 test) — GradleRunner mavenLocal → asciidoctorRevealJs BUILD SUCCESSFUL, HTML généré ✅, Playwright navigate ⚠️ file:// + reveal.js CDN ne charge pas en headless | 1/1 Gradle ✅, 0/1 Playwright ⚠️ | TODO session 006 : servir HTML via HTTP local (npx serve) avant Playwright navigate. Assertions UX/UI viewport déjà codées. |
| 002 | `RevealJsTransitionSteps.kt`, `MinimalSteps.kt` — Cucumber | 17/17 PASS | Cucumber BDD — parsing transitions + auto-animate model validation |

== Session 009 — Publication Maven Central (2026-06-11)

Inchangé — aucun test modifié. Publication uniquement (build -x cucumberTest -x functionalTest).

== Session 014 — SLD-3 i18n US-3.0 (2026-07-06)

Nouveau test unitaire `DeckContextLanguageTest.kt` (5 tests JUnit5 + AssertJ) :
. `DeckContext default languageCode should be fr` — default ISO 639-1
. `DeckContext languageCode should accept all LanguageCatalog supported codes` — 10 codes
. `DeckContext languageCode should reject legacy display name 'French'` — migration safety
. `DeckContext languageCode should reject unknown ISO code 'xx'` — validation
. `DeckContext requireValidLanguage should pass for fr` — happy path

Résultat : 5/5 PASS. Couverture `DeckContext.languageCode` + `requireValidLanguage()`.
Tests Cucumber modifiés (RevealJsTransitionSteps, ProposeDeckContextSteps, feature) —
10 échecs préexistants (infra GradleRunner `proposeDeckContext not found`, S007).

== Session 018 — SLD-3 i18n US-3.2 + English Only governance (2026-07-06)

New unit tests:

`TranslatorManagerLanguageTest.kt` (5 tests JUnit5 + AssertJ) :
. `supportedLanguages should contain all 10 LanguageCatalog ISO codes` — 10 codes
. `supportedLanguages should not contain legacy display names` — no "French"/"English"
. `supportedLanguages size should match LanguageCatalog ALL size` — size 10
. `translationTasks should exclude identity permutations` — no frToFr
. `translationTasks should produce N times N minus 1 permutations for 10 languages` — 90 tasks

`TranslatorPromptTest.kt` (6 tests JUnit5 + AssertJ) :
. `prompt should contain the source text to translate` — text preserved
. `prompt should mention target language native name for Arabic` — العربية
. `prompt should mention target language native name for Chinese` — 中文
. `prompt should mention source language native name` — Français
. `prompt should contain both source and target native names for fr to ur` — Français + اردو
. `prompt should contain target ISO code for clarity` — code in prompt

New functional test:

`TranslatorFunctionalTest.kt` (4 tests GradleRunner + AssertJ) :
. `displaySupportedLanguages should list all 10 ISO codes` — en, zh, hi, es, fr, ar, bn, pt, ru, ur
. `displaySupportedLanguages should list native names not English display names` — Français, العربية, 中文
. `displaySupportedLanguages should not contain legacy English-only display names` — no "French(fr)"
. `translate task should be registered for all 10 languages permutations` — translateFrToEn, translateEnToZh, etc.

New Cucumber BDD:

`5_translator_i18n.feature` (7 scenarios, English Gherkin) + `TranslatorI18nSteps.kt` (8 step defs) :
. `Supported languages match LanguageCatalog ISO codes`
. `Supported languages do not contain legacy display names`
. `Translation tasks exclude identity permutations`
. `Translation tasks produce N times N minus 1 permutations`
. `Translation prompt mentions target language native name`
. `Translation prompt mentions source language native name`
. `Translation prompt contains the source text`

Result: 33 unit PASS + 4 functional PASS + 23 cucumber PASS / 1 SKIP (Ollama).
Coverage: TranslatorManager.supportedLanguages, translationTasks, getTranslatePromptMessage,
createDisplaySupportedLanguagesTask, TranslatorPlugin task registration.

== Session 019 — SLD-3 i18n US-3.3/3.4/3.5 (2026-07-06)

New unit tests (30 total, +9 over session 018):

`RevealUiMessagesTest.kt` (4 tests JUnit5 + AssertJ) :
. `RevealUiMessages should expose the language ISO code` — languageCode field
. `RevealUiMessages for Arabic should expose the ISO code and rtl flag` — ar rtl true
. `RevealUiMessages for French should not be rtl` — fr rtl false
. `RevealUiMessages isRtl should derive from LanguageCatalog` — all 10 codes match

`RevealUiMessageCatalogTest.kt` (9 tests JUnit5 + AssertJ) :
. `catalog should expose one RevealUiMessages per LanguageCatalog entry` — size 10
. `catalog should cover all 10 ISO codes from LanguageCatalog` — all codes present
. `catalog findByCode should return the matching RevealUiMessages` — fr lookup
. `catalog findByCode should return null for unknown code` — xx null
. `catalog French messages should use French tooltips for navigation` — Diapositive précédente
. `catalog English messages should use English tooltips for navigation` — Previous slide
. `catalog Arabic messages should be rtl` — ar isRtl true
. `catalog Urdu messages should be rtl` — ur isRtl true
. `catalog all messages should have non blank nav and controls labels` — no blanks

`RevealUiMessagesWriterTest.kt` (8 tests JUnit5 + AssertJ + TempDir) :
. `writeAll should create one messages_{code} js file per supported language` — 10 files
. `written file should contain RevealI18n messages assignment for the matching code` — RevealI18n + fr
. `written file for Arabic should embed rtl true flag` — rtl: true
. `written file for English should not embed rtl flag set to true` — no rtl: true
. `written file should expose nav messages as a nested nav object` — nav/prev/next/up/help
. `written file should expose controls messages as a nested controls object` — controls/overview/speakerNotes/fullscreen
. `writeAll should return the list of written files` — 10 files
. `written files should be valid JavaScript opening with a statement` — starts with RevealI18n

`RevealRtlResolverTest.kt` (9 tests JUnit5 + AssertJ) :
. `resolveRtl should return true for Arabic` — ar true
. `resolveRtl should return true for Urdu` — ur true
. `resolveRtl should return false for French` — fr false
. `resolveRtl should return false for English` — en false
. `resolveRtl should return false for every LTR LanguageCatalog entry` — all LTR
. `resolveRtl should return true for every RTL LanguageCatalog entry` — all RTL
. `resolveRtl should return false for unknown language code` — xx false
. `rtlLanguages should contain exactly Arabic and Urdu` — {ar, ur}
. `rtlLanguages size should match LanguageCatalog RTL count` — size 2

New functional test:

`RevealUiMessagesFunctionalTest.kt` (3 tests GradleRunner + AssertJ) :
. `generateRevealUiMessages task should be registered and run successfully` — task SUCCESS
. `generateRevealUiMessages should produce one messages_{code} js file per supported language` — 10 files in build/reveal-i18n
. `generateRevealUiMessages task should belong to the slider group` — tasks --group slider

New Cucumber BDD (22 scenarios across 3 features):

`6_reveal_ui_i18n.feature` (10 scenarios, English Gherkin) + `RevealUiI18nSteps.kt` (11 step defs) :
. RevealUiMessageCatalog exposes one entry per LanguageCatalog language
. RevealUiMessageCatalog covers all 10 ISO codes
. Reveal UI messages for Arabic should be RTL
. Reveal UI messages for Urdu should be RTL
. Reveal UI messages for French should not be RTL
. Reveal UI messages writer produces one file per supported language
. Written messages_fr js file contains French navigation labels
. Written messages_ar js file embeds the RTL flag
. Written messages_en js file does not embed the RTL flag
. Each written file starts with the RevealI18n assignment

`7_rtl_layout.feature` (6 scenarios, English Gherkin) + `RtlLayoutSteps.kt` (5 step defs) :
. Arabic language code resolves to RTL layout
. Urdu language code resolves to RTL layout
. French language code resolves to LTR layout
. English language code resolves to LTR layout
. Unknown language code defaults to LTR layout
. RTL languages are exactly Arabic and Urdu

`8_i18n_fallback.feature` (6 scenarios, English Gherkin) + `I18nFallbackSteps.kt` (6 step defs) :
. I18nConfig fallback language should always be the default
. Invalid CLI language code should fall back to default
. Unknown CLI language code falls back to default when gradle properties are empty
. Empty CLI language code falls back to gradle properties
. Supported languages fall back to active language singleton when empty
. Resolved i18n config for supported code should validate successfully

Result: 57 unit PASS (0 fail, 0 skip) + 3 functional PASS + 46 cucumber PASS / 0 FAIL / 1 SKIP.
Coverage: RevealUiMessages (value object), RevealUiMessageCatalog (10 languages),
RevealUiMessagesWriter (JS generation), RevealRtlResolver (RTL decision),
generateRevealUiMessages task, I18nConfigResolver fallback cascade,
asciidoctorRevealJs RTL wiring (setRightToLeft).

== Session 015 — SLD-3 i18n US-3.1 (2026-07-06)

Nouveau test unitaire `I18nConfigResolverTest.kt` (9 tests JUnit5 + AssertJ) :
. `cascade layer 1 CLI -Planguage wins` — CLI bat toutes les couches
. `cascade layer 2 gradle properties wins over DSL YAML default`
. `cascade layer 3 DSL wins over YAML and default`
. `cascade layer 4 YAML languageCode wins over default`
. `cascade layer 5 default fr when nothing is set`
. `resolved I18nConfig should validate successfully` — I18nValidationResult.Valid
. `supportedLanguages falls back to activeLanguage singleton when empty`
. `invalid CLI language code should fall back to default`
. `fallback language should always be default`

Résultat : 9/9 PASS. Couverture cascade 5 couches + validation I18nConfig.
Total : 22 tests PASS (5 DeckContextLanguageTest + 9 I18nConfigResolverTest + 6 Cucumber
transition/auto-animate + 1 SliderPluginTest + 1 PlaywrightE2E).

== Session 020 — SLD-2 US-2.5 Capsule Feed + SLD-3 US-3.6 Dogfood (2026-07-07)

Nouveaux tests (package `slider.capsule`) :

**Unit (28 PASS, was 57) :**
- `CapsuleScriptWriterTest` (12 tests) — header deckName, segments count, note placement,
  blank line separation, multi-line, trim whitespace, invariants construction
- `AsciidocSpeakerNoteParserTest` (11 tests) — extraction multi-slides, skip sans note,
  multi-lignes, attributes bracket, attribute lines preceding heading, level-2 only,
  reindexation, blank deckName rejeté, deck vide sans notes
- `CapsuleSmokeRealDeckTest` (1 test) — round-trip sur `kotlin-intro-deck.adoc` réel
- `CapsuleFeedDemoDeckTest` (4 tests) — dogfood decks fr/en/ar parse+write round-trip

**Functional (4 PASS, was 3) :**
- `GenerateCapsuleFunctionalTest` (4 tests) — task registered in slider group,
  produit `<deck>-script.txt` conforme contrat, skip decks sans notes, 1-based indices

**Cucumber BDD (58 PASS, was 46) :**
- `9_capsule_feed.feature` (13 scénarios) — 9 contractuels (1 segment, multi, skip,
  plain-text, multi-lignes, deck vide, blank name, attributes, level-2) + 4 dogfood
  (fr/en/ar decks + plain-text contract)

Nouveaux fichiers de production : 2 (`CapsuleScript.kt` + `AsciidocSpeakerNoteParser.kt`)
Nouveaux fichiers de test : 6 (3 unit + 1 functional + 1 feature + 1 steps)

Total : 28 unit + 4 functional + 58 cucumber = 90 PASS.