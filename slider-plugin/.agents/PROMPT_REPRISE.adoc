= PROMPT_REPRISE — Slider Gradle

_Session terminee_ : 071 — BKY-LANG-22-8 (dogfooding 22 langues) + publication 0.0.19
_Prochaine session_ : 072 — à arbitrer par le pilote (tous EPICs terminés, borough en maintenance)

== Mission session 071 (TERMINÉE) — EPIC transverse BKY-LANG-22 (piloté bakery S-226)

. *BKY-LANG-22-8 — dogfooding slider corpus 22 langues* : 3 sources fr/en/ar ×
   12 nouvelles langues = **36 paires** via `translateDeck` réel (`gemma4:31b-cloud`,
   `gpt-oss:120b-cloud` à sa limite mensuelle, ports 11439-11444 via
   `OLLAMA_POOL_PORTS`). Corpus `slides/misc/` : **63 adoc + 63 context = 126
   fichiers** (21 cibles/source).
. *Pin levé* : `TranslatedDemoDeckTest.dogfoodTargetCodes` dérivé de
   `LanguageCatalog.supportedCodes()` (RED 8 assertions → GREEN 261/0).
. *Drift préexistant corrigé* : `7_rtl_layout.feature` (`fa` ajouté S-217 sans
   maj de l'assertion) → catalog-driven.
. *Feature 14 étendue* : 9→21 adoc/source, totaux 27→63, RTL `fa`.
. *BKY-LANG-22-10 — publication slider 0.0.19* : bundle
   `03289d70-443d-4ee2-9f02-1a06b850522c` AUTOMATIC, propagation repo1 HTTP 200.
. Gates : 261 UT, cucumber 270/0, FT 45/0, `test` global 1099 (3 FAIL Playwright
   préexistants, fixtures HTML non générées).

== Contexte reprise session 072 — à arbitrer par le pilote

- *Tous les EPICs sont terminés* (SLD-0→11 + CR + PUB). Aucun nouvel EPIC cadré.
- *Structure* : `slider-plugin/` est un build autonome (`./gradlew check`).
- *Versions publiées Central* : slider 0.0.19, BOM 0.0.48, i18n-contracts 0.0.4.
- *Rapport 071* : `.agents/sessions/071-bky-lang-22-8-dogfooding.adoc`.
- *Dette* : rendu `asciidoctorRevealJs` Gradle cassé (gems JRuby
  `jar-dependencies 0.5.4 vs 0.4.1`, préexistant S-023/S-034) — bloque le
  dogfooding HTML automatisé. EPIC candidat.

== Rappels méthodologie (pilote)

- *Baby-step DDD/TDD/BDD* : RED → GREEN → REFACTOR, 3 niveaux tests/US
  (unit → functional → cucumber), non-régression stricte (`./gradlew check`).
- *English only* : code, Gherkin, commentaires. Gouvernance (.adoc) en français.
- *Modèle LLM* : `gpt-oss:120b-cloud` épuisé (limite mensuelle) → utiliser
  `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
- *Git autorisé* (décision pilote 2026-09-17) — zéro secret. Aucun publish sans
  permission (publishSlides inclus).
- *Domaines DDD* (16) : `slider.pipeline` clos. `slider.capsule` expulsé (S-058).
