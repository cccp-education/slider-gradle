= PROMPT_REPRISE — Slider Gradle

_Session terminee_ : 067 — Aplatissement structure `slider-plugin/slider/` → `slider-plugin/`
_Prochaine session_ : 068 — à arbitrer par le pilote (tous EPICs terminés)

== Mission session 067 (TERMINÉE)

. Fix structure repo : le dossier `slider-plugin/slider/` (sous-projet
  Gradle `include("slider")`) violait le contrat d'architecture
  « Plugin Indépendant + Racine Consommateur » (article 0124) — slider
  était le SEUL borough avec cette indirection
  (`slider-plugin/slider/src/` au lieu de `slider-plugin/src/`).
  Héritage du tout premier commit (`d3aacdd`).
. Aplatissement complet via `git mv` (historique préservé, 211 fichiers
  déplacés) : `slider-plugin/slider/{build.gradle.kts, src, gradle.properties}`
  → `slider-plugin/` + fusion `.gitignore`.
. `slider-plugin/settings.gradle.kts` : suppression `include("slider")`
  — build mono-projet autonome conforme au pattern.
. Chemins fixtures Playwright corrigés (`../../build/` → `../build/`).
. 5 refs `:slider:` nettoyées (3 tests Playwright, PROMPT_REPRISE,
  SESSION_CHECKLIST). Path Gradle : `:slider` → `:`.
. `./gradlew check` BUILD SUCCESSFUL 10m36s (unit + functional +
  cucumber), zéro régression.
. Commit + push autorisés par le pilote (clôture session 067).

== Contexte reprise session 068 — à arbitrer par le pilote

- *Tous les EPICs de feature sont terminés* (SLD-0→11 + CR-GLOBALE-002 +
  CR3 + PUB 0.0.1→0.0.16). Aucun nouvel EPIC cadré.
- *Structure* : `slider-plugin/` est maintenant un build autonome avec
  `src/` à sa racine. Commandes : `./gradlew check` (plus de `:slider:`
  prefix).
- *Backlog en attente de décision* :
  - Optionnel : rattrapage i18n plantuml (196 lignes × 9 langues —
    backlog cross-borough, borough HTOWN DORMANT).
  - Optionnel : nouveaux EPICs à cadrer (le pilote arbitre la prochaine
    direction — aucun EPIC n'est imposé).
- *Baseline* : session 067 committée + pushée (clôture pilote).
- *Versions publiées Central* : slider 0.0.16, BOM 0.0.17.
- *Règles* : Règle 1 — AUCUN commit sans permission. Règle 2 — AUCUN
  publish sans permission (publishSlides inclus).
- *Rapport 067* :
  `slider-plugin/.agents/sessions/067-aplatir-slider-plugin-structure.adoc`.

== Rappels méthodologie (pilote)

- *Baby-step DDD/TDD/BDD* : RED → GREEN → REFACTOR, 3 niveaux tests/US
  (unit → functional → cucumber), non-regression stricte
  (`./gradlew check` VERT avant de clore une US).
- *English only* : code, Gherkin, commentaires. Gouvernance (.adoc) en
  français.
- *Domaines DDD* (16) : `slider.pipeline` clos (10 fichiers prod + 4
  test). `slider.capsule` expulsé (S-058, cross-borough capsule).
- *Décisions architecture* : Decision 001 (PicoContainer rejeté, DI
  native Gradle), Decision 002 (slider consomme codebase N1 comme socle
  LLM unifié).