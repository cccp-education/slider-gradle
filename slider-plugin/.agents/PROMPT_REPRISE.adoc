= PROMPT_REPRISE — Slider Gradle

_Session terminee_ : 072 — `asciidoctorRevealJs` reparé (dette S-023/S-034) + bump 0.0.20
_Prochaine session_ : 073 — a arbitrer par le pilote

== Mission session 072 (TERMINÉE) — dette locale actionnable (zéro coût LLM)

*`asciidoctorRevealJs` cassé* : reproduction réelle → cause racine **double
JRuby** sur le worker OUT_OF_PROCESS. La tâche résout le défaut
asciidoctor-gradle `asciidoctorj:3.0.0` (`jruby-complete:9.4.8.0`,
`jar-dependencies 0.4.1`) ; le classpath plugin (via workspace-bom /
codebase-plugin) contraint `asciidoctorj:3.0.1` (`jruby-stdlib:9.4.14.0`,
`jar-dependencies 0.5.4`). Gemini JRuby activait 0.4.1 puis échouait sur
0.5.4 → `Gem::LoadError`, jar de gems vide → `LoadError: no such file to
load -- asciidoctor`.

Correctif (baby-step DDD/TDD/BDD strict) :
. *US-1* `slider.extension.AsciidoctorJPin` value object pur (défaut 3.0.1,
   3 UT RED→GREEN).
. *US-2* `SliderExtensionRegistrar.pinAsciidoctorJ` (pose
   `AsciidoctorJExtension.setVersion`).
. *US-3* `AsciidoctorJPinFunctionalTest` (TestKit, RED prouvé par stash).
. *US-4* feature 18 : 9 → 12 scénarios (3 AsciidoctorJPin).

*Preuve dogfooding from-scratch* : `Successfully installed
asciidoctor-revealjs-5.2.0` + **80 decks HTML** ; jar de gems 261 o → 10,9 Mo.
*Bonus* : `generatePlaywrightFixtures` débloqué → les 3 tests Playwright
(échecs préexistants S-217) **passent**.

*Bump* : 0.0.19 (déjà Central S-226, immuable) → **0.0.20**.

Gates : `test` 1102/0, `functionalTest` 46/0, `cucumberTest` 273/0 (1 skip),
`check` VERT.

== Contexte reprise session 073

- *Tous les EPICs slider terminés* (SLD-0→11 + CR + PUB).
- *Versions* : slider local **0.0.20** (non publié Central — accord pilote
  requis Règle 2) ; Central 0.0.19 ; BOM 0.0.48 ; i18n-contracts 0.0.4.
- *Publication 0.0.20* : EPIC PUB candidat (ordre DAG : publier slider 0.0.20
  puis bump MEMPHIS/catalog + consommateurs). `publishToMavenLocal` OK.
- *Rapport 072* : `.agents/sessions/072-asciidoctorj-pin-revealjs-repare.adoc`.
- *Dette* : `slider-plugin/gradle/libs.versions.toml` `slider = "0.0.14"` (résidu
  stale, version inerte — seul `.pluginId` est lu). Nettoyage candidat.
- *Dogfooding HTML désormais automatisable* : plus besoin du Ruby système.

== Rappels méthodologie (pilote)

- *Baby-step DDD/TDD/BDD* : RED → GREEN → REFACTOR, 3 niveaux tests/US
  (unit → functional → cucumber), non-régression stricte (`./gradlew check`).
- *English only* : code, Gherkin, commentaires. Gouvernance (.adoc) en français.
- *Modèle LLM* : `gpt-oss:120b-cloud` épuisé (limite mensuelle) → utiliser
  `gemma4:31b-cloud` + `OLLAMA_POOL_PORTS=11439,11440,11441,11442`.
- *Git autorisé* (décision pilote 2026-09-17) — zéro secret. Aucun publish
  Central sans permission explicite de l'utilisateur.
