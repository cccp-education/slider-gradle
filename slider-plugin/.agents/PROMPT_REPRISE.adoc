= PROMPT_REPRISE — Slider Gradle

_Session terminee_ : 068 — Audit version + EPIC PUB bump 0.0.17 (gouvernance pure)
_Prochaine session_ : 069 — à arbitrer par le pilote (tous EPICs de feature terminés)

== Mission session 068 (TERMINÉE)

. Audit version slider : `slider-plugin/build.gradle.kts:26` = 0.0.16 ;
  dernière publication Central 0.0.16 (bundle `106d110f`, S-066) ;
  commit post-pub `822c9e9` (LLM-ADAPTER-EXTRACT US-2 — consommation
  adapter N1 `codebase.koog.llm.adapter`, langchain4j 1.14.1→1.16.3,
  codebase 0.0.5→0.0.7) non publié.
. Décision : bump 0.0.16 → 0.0.17 recadré EPIC PUB dédié (3 US
  PUB-0.0.17-1/2/3 — bump + publication nmcp + BOM 0.0.19 + sync capsule).
. BACKLOG.adoc synchronisé (Section Session 068 + EPIC PUB 0.0.17
  `🟡 À FAIRE`). Gouvernance pure — zéro code de production modifié.
. Baseline `./gradlew check` VERT préservée (959 unit + 45 functional +
  265 cucumber, 0 FAIL, 1 SKIP préexistant).
. Commit + push autorisés par le pilote (clôture session 068).

== Contexte reprise session 069 — à arbitrer par le pilote

- *EPIC PUB 0.0.17 au backlog* (`🟡 À FAIRE`) : à exécuter à la prochaine
  session de publication — bump 0.0.16→0.0.17 + `./gradlew check` VERT
  + publication nmcp Central (3 artefacts signés GPG) + bump BOM
  workspace-bom 0.0.18→0.0.19 (constraint slider 0.0.17) + publication
  + sync capsule-gradle (catalogue slider). Accord pilote requis pour
  publish et commit.
- *Tous les EPICs de feature sont terminés* (SLD-0→11 + CR-GLOBALE-002 +
  CR3 + PUB 0.0.1→0.0.16). Aucun nouvel EPIC cadré.
- *Structure* : `slider-plugin/` est un build autonome avec `src/` à sa
  racine. Commandes : `./gradlew check` (plus de `:slider:` prefix).
- *Backlog en attente de décision* :
  - Optionnel : rattrapage i18n plantuml (196 lignes × 9 langues —
    backlog cross-borough, borough HTOWN DORMANT).
  - Optionnel : nouveaux EPICs à cadrer (le pilote arbitre la prochaine
    direction — aucun EPIC n'est imposé).
- *Baseline* : session 068 committée + pushée (clôture pilote).
- *Versions publiées Central* : slider 0.0.16, BOM 0.0.18. Version cible
  backlog : slider 0.0.17.
- *Règles* : Règle 1 — AUCUN commit sans permission. Règle 2 — AUCUN
  publish sans permission (publishSlides inclus).
- *Rapport 068* :
  `slider-plugin/.agents/sessions/068-version-bump-0.0.17.adoc`.

== Rappels méthodologie (pilote)

- *Baby-step DDD/TDD/BDD* : RED → GREEN → REFACTOR, 3 niveaux tests/US
  (unit → functional → cucumber), non-regression stricte
  (`./gradlew check` VERT avant de clore une US).
- *English only* : code, Gherkin, commentaires. Gouvernance (.adoc) en
  français.
- *Domaines DDD* (16) : `slider.pipeline` clos (10 fichiers prod + 4
  test). `slider.capsule` expulsé (S-058, cross-borough capsule).
- *Décisions architecture* : Decision 001 (PicoContainer rejeté, DI
  native Gradle), Decision 002 (slider consomme codebase N1 comme socle
  LLM unifié).
