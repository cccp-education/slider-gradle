= INDEX — Slider Gradle
:toc:

_Regles absolues_ : voir `AGENT.adoc`
_DAG_ : N2 — Plugin Gradle de generation de presentations (Regle 4bis)

_Derniere mise a jour_ : 2026-08-05
_Session terminee_ : 060 — SLD-8 US-8.2 AssistantManager câblage codebase LlmBuildService. `AssistantManager.kt` réécrit (652 → 300 lignes, -352) : suppression 3 factories gemini/mistral/huggingface + 4 streaming + 8 hello* tasks + catalogues. `resolveModel(provider, serviceProvider)` délègue à `LlmBuildService` (codebase N1) wrappé dans `LlmProviderChatModelAdapter` (nouveau, `LlmProvider` suspend → `ChatModel` langchain4j via `runBlocking`). Injection `@ServiceReference llmService` sur `RagTask` + `TranslateDeckTask`. Mock-LLM fallback (`-Pollama.baseUrl` → `createOllamaChatModel` legacy) préserve 9 scénarios Cucumber `01_propose_deck_context`. `TranslatorManager` déprécié conservé. Cross-borough : `codebase-plugin:0.0.5` republié mavenLocal (JAR S-176 n'incluait pas LlmBuildService). `./gradlew :slider:check` VERT (900 unit +5, 38 functional -2, 254 cucumber = 1192 total, 0 FAIL).
_Session en cours_ : 061 — Cadrage formel SLD-8.3 `DeckPipelineGraph` (koog orchestration, nodes propose-context/validate-context/generate-deck). Zéro code modifié — session de vision et découpage baby-step. Patterns koog 1.0.0 consommés depuis `office/pilotage/codebase/KOOG_AGENTIC_PATTERNS.adoc` (15 patterns, architecture "koog orchestre / langchain4j exécute"). Domaine cible : `slider.pipeline` (nouveau), `DeckState` (data class immutable), 4 baby-steps TDD/BDD (DeckState → DeckPipelineGraph → DeckPipelineExecutor → Cucumber BDD). Publication BOM 0.0.16 + codebase 0.0.5 Central en attente (si permission).
_Resume public_ : link:../../BACKLOG.adoc[BACKLOG.adoc racine]

'''
== REGLES ABSOLUES (resume)

=== 1. COMMITS/GIT — INTERDICTION FORMELLE sans permission
=== 2. PUBLISH — INTERDICTION FORMELLE sans permission (publishSlides inclus)
=== 3. DAG N2 — importable par N3 runner-gradle, jamais importe N3
=== 4. PROCEDURE DE FIN DE SESSION — 6 ETAPES OBLIGATOIRES
=== 5. HOOK OUVERTURE — Checklist SESSION_CHECKLIST.adoc obligatoire
=== 6. ZERO secret/token/api key en dur dans le code source

'''
== Sessions

|===
| # | Date | Type | Sujet | Score

| 060 | 2026-08-05 | feat+ddd+tdd+archi cross-borough | SLD-8 US-8.2 — AssistantManager câblage codebase LlmBuildService. `AssistantManager.kt` réécrit (652 → 300 lignes, -352) : suppression 3 factories gemini/mistral/huggingface + 4 streaming + 8 hello* tasks + catalogues. `resolveModel(provider, serviceProvider)` délègue à `LlmBuildService` (codebase N1) wrappé dans `LlmProviderChatModelAdapter` (nouveau, 48l, `LlmProvider` suspend → `ChatModel` langchain4j via `runBlocking`, concatène system+user). Injection `@ServiceReference llmService` sur `RagTask` + `TranslateDeckTask` + `registerLlmBuildService()`. Mock-LLM fallback (`-Pollama.baseUrl` détecté → `createOllamaChatModel` legacy) préserve 9 scénarios Cucumber `01_propose_deck_context`. `TranslatorManager` déprécié conservé (expulsion future EPIC SLD-CR4). Cross-borough : `codebase-plugin:0.0.5` republié mavenLocal (JAR S-176 n'incluait pas LlmBuildService). US-8.2a (catalog + build), US-8.2b (LlmProviderChatModelAdapter TDD 5 unit), US-8.2c (AssistantManager réduit), US-8.2d (RagTasks + TranslateDeckTask câblés). `./gradlew :slider:check` VERT (900 unit +5, 38 functional -2, 254 cucumber = 1192 total, 0 FAIL, 1 SKIP préexistant). | ✅ 9/10

| 059 | 2026-08-05 | publish+ops+cross-borough | Publication slider 0.0.15 (post-SLD-11 expulsion `slider.capsule`) + BOM 0.0.15 + sync capsule. Bump 0.0.14 → 0.0.15 sur 4 fichiers (slider build, BOM build, BOM catalogue, capsule catalogue). Bundle nmcp slider `832cea2a-47b5-4e74-b677-0effcc94bed6` (AUTOMATIC, 3 artefacts signés GPG : slider:0.0.15 + 2 plugin markers) + BOM `02307b00-db40-44e7-9037-c7d293f6a26f` (AUTOMATIC). `./gradlew :slider:check` VERT pré-publication (895 unit + 40 functional + 254 cucumber). `./gradlew :slider:publishToMavenLocal` (sync capsule). Capsule 361 unit tests VERT avec slider 0.0.15 (mavenLocal). Zéro code production modifié côté slider. Coordination capsule 069 : EPIC CAP-MODEL TERMINÉ (unification modèle capsule.feed, côté capsule seul — 7/7 US, +124 unit tests, `CapsuleScriptReader` remplace `CapsuleManager.parseScript`, `capsule.CapsuleModels` purgé). | ✅ 9/10

| 058 | 2026-08-05 | refactor+ddd+tdd+bdd cross-borough | EPIC SLD-11 (expulsion `slider.capsule` → capsule) TERMINÉ — 6/6 US (11.0 cadrage S-056 + 11.1→11.6 cette session). Cross-borough (slider + capsule modifiés en coordination), non-regression stricte (tests portés côté capsule avant suppression côté slider). SLD-11.1 : domaine `capsule.feed` côté capsule (3 fichiers prod : AsciidocSpeakerNoteParser + CapsuleFeedModels invariants stricts + CapsuleScriptWriter + CapsuleFeedTaskDomain), 27 unit tests portés (RED → GREEN). SLD-11.2 : tâche `extractSpeakerNotes` côté capsule (remplace slider `generateCapsule`), CapsuleFeedTaskRegistrar + ExtractSpeakerNotesTask, 13 unit + 4 functional. SLD-11.3 : Cucumber `capsule_speaker_notes.feature` (13 scénarios, port `9_capsule_feed.feature`) + CapsuleSpeakerNotesSteps. SLD-11.4 : pipeline `translateAndExtractSpeakerNotes` (remplace `translateAndGenerateCapsule`), 2 unit + 2 functional + 3 cucumber. SLD-11.5 : expulsion côté slider — 4 prod + 8 test + 3 features + 3 steps + 2 functionalTest supprimés, câblage `SliderTasks.kt` + KDoc + 11 `.properties` + 10 `MessagesXxTranslationTest` nettoyés. SLD-11.6 : `generateCapsuleScript` dépend de `extractSpeakerNotes`, 2 functional + 1 cucumber. `parseScript` conservé (rétrocompatibilité pipeline vidéo). Tests slider : 895 unit (was 977, -82) / 40 functional (was 46, -6) / 254 cucumber (was 279, -25) = 1189 total (was 1302, -113). `./gradlew :slider:check` VERT 0 FAIL / 1 SKIP préexistant. Capsule-gradle : +42 unit +8 functional +16 cucumber = 66 nouveaux tests, 0 FAIL (1 failure préexistant burnin ffmpeg non lié). | ✅ 9/10
| 057 | 2026-08-05 | feat+tdd+bdd+i18n | EPIC SLD-9 (i18n Completion via LLM) TERMINÉ — 5/5 US (9.2/9.3/9.4 livrées). Périmètre restrictif : i18n only, zéro production modifié. SLD-9.2 : 9 classes traduction par langue (8 Messages{Zh,Hi,Es,Ar,Bn,Pt,Ru,Ur}TranslationTest + MessagesEnReferenceTest), pattern MessagesFrTranslationTest étendu (+`task.group.translator` PARTIAL +`task.translateDeck.noTargets`). 39 clés véritablement traduisibles (12 clés identiques cross-langue exclues : 10 task.group.* techniques + 3 placeholders purs `translated`/`failed`/`summary`). +366 tests unit. SLD-9.3 : `TaskDescriptionsI18nFunctionalTest` (GradleRunner, 20 tests) — 17 tâches i18nisées × 10 langues + 10 tests spécifiques EN/FR (cleanBuild, asciidoctorRevealJs, serveSlides, visualTest, translateDeck). +20 functional. SLD-9.4 : SliderMessagesTest étendu (29→55, +26 unit) + `25_slider_messages_resolution.feature` (31 scénarios BDD) + `SliderMessagesResolutionSteps` (Java8 En). Bug DuplicateStepDefinitionException résolu (renommage `the resolved message should contain`, args multi-values séparés par `|`). +31 cucumber. Tests : 977 unit / 46 functional / 279 cucumber = 1302 total (was 859, +443). `./gradlew :slider:check --rerun-tasks` VERT 0 FAIL / 1 SKIP préexistant. | ✅ 10/10
| 055 | 2026-08-05 | publish+deps | Publication slider 0.0.14 Maven Central + synchronisation consommateurs. Bump 0.0.13 → 0.0.14 (SLD-9.1 i18n AssistantManager + SLD-10 content domain complet). Bundle nmcp `6f7b84be-93db-4f31-a83f-bebe1b656c2a` (AUTOMATIC), 3 artefacts signés GPG (slider:0.0.14 POM+JAR+sources+module + 2 plugin markers education.cccp.slider + education.cccp.slider.translator). `./gradlew :slider:check` VERT pré-publication. Vérification repo1.maven.org : POM HTTP 200 à 03:52, markers HTTP 200 à 03:56. Synchronisation capsule-gradle (2 catalogues slider 0.0.6 → 0.0.14 + plugin ID `com.cheroliv.slider` → `education.cccp.slider` — ancien namespace inexistant sur Central). Publication workspace-bom 0.0.14 (bundle `c8bf911c`, constraint slider 0.0.14, POM HTTP 200 à 04:11). Commits + push 3 repos (slider-gradle `6b975ef`, capsule-gradle `993c59b`, workspace-bom `4d6f175`). Zéro code de production modifié. | ✅ 9/10
| 054 | 2026-08-05 | feat+bdd+tdd | US SLD-10.6 (Cucumber `24_slide_content_validation.feature`) livrée en baby-step TDD/BDD (RED → GREEN → REFACTOR). 2 fichiers test créés : feature `24_slide_content_validation.feature` (11 scénarios BDD end-to-end, tags `@content @validation`) + `ContentValidationSteps.kt` (141 lignes, package `slider.steps`, Java8 lambda style `En`). 3 groupes de steps : a priori (`ContentSlideAssertion` sur `SlideContent`+`SlideLayout` — 5 scénarios), a posteriori (`ContentRenderAssertion` sur `ContentRenderData` snapshot DTO — 5 scénarios), cross-validation (1 scénario). Zéro fichier de production modifié (pure BDD — logique métier déjà livrée S-051/053). 237 → 248 Cucumber (+11 scénarios : 5 a priori + 5 a posteriori + 1 cross-validation). 585 unitaires (inchangé). 0 FAIL, 1 SKIP préexistant. `./gradlew :slider:check` VERT. Zéro régression. **EPIC SLD-10 TERMINÉ** (7/7 US — 10.0/10.1/10.2/10.3/10.4/10.5/10.6 ✅). Domaine `slider.content` clos (9 fichiers prod + 8 fichiers test). | ✅ 9/10
| 053 | 2026-08-05 | feat+ddd+tdd+playwright | US SLD-10.5 (Assertions Playwright enrichies) livrée selon cadrage S-052. 3 baby-steps TDD (RED → GREEN → REFACTOR), 3 niveaux tests (unit → functional). Domaine `slider.content` enrichi (2 nouveaux fichiers prod) : `ContentRenderData.kt` (snapshot DTO pur, mirroring `SlideRenderData` RTL, 8 champs — slideTitle, realTextBlocks, computedTitleFontSize, computedBodyFontSize, computedContrastRatio, hasNotesInDom, viewportWidth, viewportHeight, invariants `init`, factory `EMPTY` avec bypass via paramètre privé `bypassInvariants`) + `ContentRenderAssertion.kt` (object pur, `assertAll(ContentRenderData): ContentAssertionResult`, réutilise les 4 codes `ContentAssertionCode` existants — pas de nouveau code, délègue overflow à `OverflowDetector.detect` sur les `TextBlock` réels, nouvelle constante `MIN_CONTRAST_RATIO = 4.5` WCAG AA, `MIN_BODY_FONT_SIZE` réutilisé depuis `ContentSlideAssertion`). Adaptateur Playwright `ContentPlaywrightAdapter.kt` (src/test, object, extrait snapshot depuis `Page` réelle — `extractTextBlocks` h2/h3/p/li + boundingBox, `computedFontSize` getComputedStyle, `computedContrast` JS injecté WCAG relative luminance). Functional test LTR `ContentPlaywrightFunctionalTest.kt` (port 4337, fixture `capsule-feed-demo-en-deck.html`, réutilise `PlaywrightFixtureSupport` S-034, lifecycle `@TestInstance(PER_CLASS)` + companion + `@BeforeAll/@AfterAll`). Deux objects d'assertion coexistent : `ContentSlideAssertion` (S-051, estimation a priori sur `SlideContent`+`SlideLayout`) et `ContentRenderAssertion` (S-053, mesure a posteriori sur `ContentRenderData`). 17 unit + 1 functional = +18 tests. 585 unitaires (was 567), 0 FAIL, 1 SKIP préexistant. `./gradlew :slider:check` VERT. Zéro régression. | ✅ 9/10
| 052 | 2026-08-05 | vision+cadrage+audit | Cadrage exhaustif US SLD-10.5 (Assertions Playwright enrichies). Zéro code modifié — session de vision et découpage. Audit Playwright cross-borough (6 boroughs : slider, bakery, capsule, container-provision, magic-stick, workspace-bom) confirmant l'absence de contrat Playwright partagé et le pattern snapshot DTO `SlideRenderData` unique à slider (gold standard du workspace). Décision architecturale clé : SLD-10.5 est l'**adaptateur Playwright** de SLD-10. Nouveaux types cadrés : `ContentRenderData` (snapshot DTO pur, mirroring `SlideRenderData` RTL — slideTitle, realTextBlocks, computedTitleFontSize, computedBodyFontSize, computedContrastRatio, hasNotesInDom, viewportWidth/Height), `ContentRenderAssertion` (object pur, mesure a posteriori, réutilise les 4 codes `ContentAssertionCode` existants — pas de nouveau code), `ContentPlaywrightAdapter` (extraction Playwright → snapshot via `page.evaluate`/`boundingBox`/`getComputedStyle`, port 4337), `ContentPlaywrightFunctionalTest` (LTR fixture, réutilise `PlaywrightFixtureSupport` S-034). Nouvelle constante `MIN_CONTRAST_RATIO = 4.5` (WCAG AA) — vit sur `ContentRenderAssertion` car le contraste réel n'est mesurable qu'après rendu. Deux objects d'assertion coexistent : `ContentSlideAssertion` (S-051, estimation a priori sur `SlideContent`+`SlideLayout`) et `ContentRenderAssertion` (S-052, mesure a posteriori sur `ContentRenderData`). 3 baby-steps TDD prévus : (1) `ContentRenderData` + 8 unit, (2) `ContentRenderAssertion` + 9 unit, (3) `ContentPlaywrightAdapter` + `ContentPlaywrightFunctionalTest` (1 functional Playwright). 17 unit + 1 functional attendus. SLD-10.6 (Cucumber `24_slide_content_validation.feature`) à planifier ensuite. Rapport : `slider-plugin/.agents/sessions/052-sld-10.5-cadrage.adoc`. | ✅ 9/10
| 051 | 2026-08-05 | feat+ddd+tdd | US SLD-10.4 `ContentSlideAssertion` + codes assertions P0/P1. Domaine `slider.content` enrichi (3 nouveaux fichiers) : `ContentAssertionCode.kt` (enum P0_OVERFLOW/P0_MISSING_NOTES/P1_FONT_SIZE/P1_CONTRAST), `ContentAssertionResult.kt` (sealed Passed/Failed + `ContentAssertionFailure`), `ContentSlideAssertion.kt` (object `assertAll(content, layout): ContentAssertionResult`, délègue l'estimation d'overflow à `OverflowDetector` via `TextBlock` synthétiques, constantes `MIN_BODY_FONT_SIZE=14.0` + `MIN_TITLE_BODY_RATIO=1.25`). Pattern de référence `slider.rtl.RtlSlideAssertion` (sealed result pour `when` exhaustif vs data class RTL). Baby-step TDD : RED (11 tests, compilation échec) → GREEN (3 fichiers prod, 1 test corrigé P1_CONTRAST ratio) → REFACTOR. 11 nouveaux tests unitaires. 567 unitaires (was 556), zéro régression. `./gradlew :slider:check` VERT. | ✅ 9/10
| 050 | 2026-08-05 | feat+ddd+tdd | US SLD-10.3 `AsciidocContentParser` (parseur `.adoc` → `SlideContent`). Domaine `slider.content` enrichi (2 nouveaux fichiers) : `SlideContent.kt` (aggregate root : title, subtitles, paragraphs, lists, speakerNote, validations `require`) + `AsciidocContentParser.kt` (parseur ligne par ligne `parseDeck(adoc): List<SlideContent>`, étend `AsciidocSpeakerNoteParser` aux titres `==`, sous-titres `===`, paragraphes, listes `* `, notes `[NOTE.speaker]`). Baby-step TDD : 34 nouveaux tests unitaires (12 SlideContent + 22 AsciidocContentParser). 556 unitaires (was 522), zéro régression. `./gradlew :slider:check` VERT. | ✅ 9/10
| 046 | 2026-08-04 | vision+governance+cadrage | Vision & backlog non implémenté + arbitrage horizons E-3PO. Audit PicoContainer : zéro implémentation `org.picocontainer` dans le workspace ; `cucumber-picocontainer` (15 boroughs) = DI Cucumber test (World/Steps), pas DI production. **Decision 001** (`.agents/DECISIONS_ARCHITECTURE.adoc` créé) : rejet assumé PicoContainer, DI native Gradle (`BuildService`/`Provider`) suffit jusqu'à réévaluation. Audit koog+R2DBC : socle N1 **déjà publié** (codebase-gradle Central 0.0.5 — koog `ai.koog:koog-agents:1.0.0` 4 graphes + document-gradle DocumentGenerationGraph ; R2DBC `R2dbcAgenticChunkRepository` ; EPIC L ✅ 034-049). **Decision 002** : adopter koog+R2DBC socle IA slider. **EPIC SLD-8 cadré** (BACKLOG, refondu post-046) : **slider consomme codebase** comme socle LLM unifié — `BuildService` LLM côté codebase (US-8.1), `AssistantManager` réduit à du câblage (US-8.2), `DeckPipelineGraph` sur services codebase (US-8.3). 5 US ~12 pts, méthode baby-step DDD/TDD/BDD 3 niveaux. Doc pilotage `KOOG_AGENTIC_PATTERNS.adoc` : koog 0.8.0→1.0.0, EPIC L ✅, note DI. Zéro code modifié. | ✅ 9/10
| 048 | 2026-08-05 | vision+cadrage | Vision SLD-10 Slide Content Model & Rendering Validation. Cadrage du besoin : domaine DDD `slider.content` (SlideContent, TextBlock, SlideLayout, OverflowDetector, AsciidocContentParser) + assertions Playwright (TextOverflow, NotesRendering, FontSize, Contrast). Zéro code modifié. SLD-9.2→9.4 mis en pause. | ✅ 8/10
| 049 | 2026-08-05 | cadrage+feat+ddd+tdd | Cadrage formel EPIC SLD-10 Slide Content Model & Rendering Validation + US SLD-10.1/10.2 livrées. Domaine DDD `slider.content` (SlideContent, TextBlock, SlideLayout, OverflowDetector, AsciidocContentParser) + assertions Playwright (TextOverflow, NotesRendering, FontSize, Contrast). 7 US ~18 pts. SLD-10.0 cadrage + SLD-10.1 `TextBlock`/`SlideLayout` (20 unit) + SLD-10.2 `OverflowDetector`/`OverflowResult` sealed (12 unit). Pattern de référence `slider.rtl.RtlSlideAssertion` + `slider.capsule.AsciidocSpeakerNoteParser`. 522 unitaires (+32), zéro régression. | ✅ 9/10
| 047 | 2026-08-04 | feat(sld-9.1) | SLD-9.1 i18n AssistantManager via LLM. 13 clés × 10 langues (générées par `gpt-oss:120b-cloud`). `AssistantManager.kt` : 15 strings hardcodées → `SliderMessages.get()`/`format()`. Tests 3 niveaux : 29 unit (`SliderMessagesTest`) + 6 functional (`AssistantManagerI18nFunctionalTest`) + 12 Cucumber (`9_assistant_manager_i18n.feature`) = +47 tests. `./gradlew :slider:check` VERT 0 FAIL / 1 SKIP. | ✅ 10/10
| 045 | 2026-08-04 | fix+tdd+bdd+pub | Fix CR-GLOBALE-002 (Option A) — 3/3 US + publication 0.0.13. US-1 : Guard plan vide pipeline translation. `DeckTranslator.translate` → `TranslationOutcome.EMPTY` si `plan.tasks.isEmpty()`. `TranslationOutcome.of()` accepte liste vide (constante `EMPTY`). `TranslateDeckTask` skip avant résolution LLM quand tous targets = langue source (clé i18n `task.translateDeck.noTargets` ajoutée dans 11 fichiers Messages_*.properties). Tests 3 niveaux : 2 unit (TranslationOutcomeTest accept-empty + DeckTranslatorTest empty-plan) + 1 functional (TranslateDeckFunctionalTest guard `-Ptarget.languages=fr` sur deck fr, aucun fichier généré, message noTargets) + 2 scénarios Cucumber (`13_deck_translation.feature` plan vide + outcome vide). US-2 : Regex `buildOutputFile` `_[a-z]{2}$` → `_[a-zA-Z]{2}$` (segment langue uppercase type `_ZH` remplacé) + 1 unit test RED→GREEN. US-3 : `RagManager.buildStore` propriétés système `ssl`/`sslmode` scopées try/finally avec restauration via `restoreSystemProperty` (plus de mutation globale JVM, le builder capture les valeurs avant restauration). Zéro test modifié. `./gradlew :slider:check` VERT 0 FAIL / 1 SKIP. Publication Maven Central slider **0.0.13** (bundle `ec715b09`, AUTOMATIC), confirmé repo1.maven.org. | ✅ 9/10
| 044 | 2026-08-04 | cleanup+deprecation+fix | SLD-CR3 Revue CR-GLOBALE-003 — 2/2 US. SLD-CR3-1 : Dépréciation `translate/` (doublon de `translation/`). `TranslationServiceAdapter.kt` supprimé (dead code). Import mort `getTranslatePromptMessage` retiré de `OllamaLanguageModelAdapter.kt`. `TranslatorManager` + `TranslatorPlugin` marqués `@Deprecated`. SLD-CR3-2 : `CapsulePlugin.kt` (capsule-gradle) — `catch (_: Exception)` → `catch (e: UnknownPluginException)`. `./gradlew :slider:check` VERT 0 FAIL / 1 SKIP. `./gradlew test` capsule-gradle VERT. | ✅ 9/10
| 042 | 2026-08-01 | refactor+ddd+tdd+bdd | SLD-6.9 + SLD-6.10 — EPIC SLD-6 TERMINÉ 11/11 US. SLD-6.9 : Consolidation `registerPublishSlidesTask` + `Git` object → `slider.repository.PublishTaskRegistrar` + `SlidePublisher`. `SlidePublisher.kt` (thin Gradle adapter, ex-`SliderManager.Git.pushSlides`) + `PublishTaskRegistrar.kt` (task registrar, ex-`SliderManager.Tasks.registerPublishSlidesTask`). Cucumber `20_publish_task_decomposition.feature` 3/3 PASS. SLD-6.10 : `SliderManager` supprimé, `SliderPlugin` 30 lignes délègue directement à 5 registrars (SliderConfig, SliderPrerequisiteRegistrar, SliderScaffoldRegistrar, SliderWiringRegistrar, SliderTasks). 15 domaines DDD. `./gradlew :slider:check` VERT — 421 unit (+2 Playwright préexistants) + functional OK + Cucumber OK. | ✅ 10/10
| 041 | 2026-07-31 | feat+ddd+tdd+bdd | SLD-7 Câblage Explicite translateDeck → Capsule. EPIC SLD-7 TERMINÉ (3/3 US, ~6 pts). US SLD-7.0 cadrage (contrat d'interface slider↔capsule formalisé). US SLD-7.1 tâche composite `translateAndGenerateCapsule` (dependsOn translateDeck → generateCapsule, domaine `slider.capsule` enrichi : `CapsuleTaskNames.TRANSLATE_AND_GENERATE_CAPSULE` + `CapsuleTaskRegistrar.registerTranslateAndGenerateCapsule`). US SLD-7.2 Cucumber BDD pipeline complet slider→capsule (feature `23_translate_and_generate_capsule.feature` + `TranslateAndGenerateCapsuleSteps`). Baby-step TDD/BDD 3 niveaux : 2 unit (CapsuleTaskDomainTest) + 2 functional (TranslateAndGenerateCapsuleFunctionalTest) + 3 cucumber (23_translate_and_generate_capsule.feature). `./gradlew :slider:check` VERT — 660 PASS / 0 FAIL / 1 SKIP (+6 vs 654 S-040). `SliderManager.kt` 552→553 lignes (registerTranslateAndGenerateCapsuleTask ajouté dans registerTasks). | ✅ 10/10
| 040 | 2026-07-22 | refactor+ddd+tdd+bdd+pub | SLD-6.8 Capsule task decomposition. Domaine `slider.capsule` consolidé (2 nouveaux fichiers 138l : `CapsuleTaskDomain.kt` value objects `CapsuleAdocDir`/`CapsuleScriptDir`/`CapsuleTaskNames` + `CapsuleTaskRegistrar.kt` thin adapter `register(project)`). `SliderManager.Tasks.registerAsciidocCapsuleTask` (~30 lignes) supprimé, délègue au registrar. Imports morts nettoyés. `SliderManager.kt` 570→552 lignes (was 968 S-035). Baby-step TDD/BDD : 12 unit (`CapsuleTaskDomainTest`) + 13 scénarios Cucumber (`22_capsule_task_decomposition.feature` + `CapsuleTaskDecompositionSteps`). `./gradlew :slider:check` VERT 654 PASS / 0 FAIL / 1 SKIP (+21 vs 633 S-039). 9/11 US SLD-6 terminées. Publication Maven Central slider 0.0.12 (bundle `842a5a72`) + BOM 0.0.11 (bundle `261de995`). | ✅ 10/10
| 039 | 2026-07-21 | refactor+ddd+tdd+bdd | SLD-6.5 + SLD-6.6 + SLD-6.7 — Trois US livrées. Domaine `slider.wiring` (2 fichiers 210l : WiringSpec value objects GroupRouting/RepositoryKind/RepositorySpec/PluginSpec/GemDependency/WiringSpec.DEFAULT + GradleWiring adapter) → 3 thin adapters Repositories/Plugins/Dependencies. Domaine `slider.revealjs` (2 fichiers 240l : RevealJsAttributesSpec/RevealJsOutputDir/SlideSourceDir/SlideMetadata/SlideMetadataScanner/DashboardJsonSerializer/CleanBuildTarget + RevealJsTaskRegistrar register cleanBuild+asciidoctorRevealJs+generateDashboard). Domaine `slider.playwright` (2 fichiers 170l : PlaywrightDir/ServeCommand/InstallCommand/VisualTestCommand/PlaywrightTaskNames + PlaywrightTaskRegistrar register serveSlides+installPlaywright+visualTest). SliderManager.kt ~844→~570 lignes, 6 méthodes Tasks supprimées, imports morts nettoyés. Baby-step TDD/BDD : +68 unit + 2 functional + 53 scénarios Cucumber (features 19+20+21). `./gradlew :slider:check` VERT 633 PASS / 0 FAIL / 1 SKIP (+117 vs 516 S-038). | ✅ 10/10
| 034 | 2026-07-15 | fix+tests+ddd+pub | Fix Playwright @Disabled (S-023). Découplage architecturel : HTML fixtures pré-générés par build racine via `generatePlaywrightFixtures` (vrai Gradle home, gem JRuby présente). Tests chargent fixtures via `python3 -m http.server` + Playwright-jvm headless Chromium — sans GradleRunner, sans JRuby isolé. `PlaywrightFixtureSupport.kt` (nouveau) mutualise Browser lifecycle + HTTP server. `SliderPlaywrightE2eTest` (LTR) + `RtlPlaywrightFunctionalTest` (RTL) réécrits — 0 `@Disabled` restant. 3 tâches Gradle créées (`copyPlaywrightLtrFixture`, `generateRtlFixture`, `generatePlaywrightFixtures`). Baby-step TDD : RED → GREEN → REFACTOR. Publication Maven Central slider 0.0.11 (bundle `b18431e5`) + BOM 0.0.10 (bundle `61519263`). `./gradlew :slider:check` VERT 428 PASS / 0 FAIL / 1 SKIP (+3 vs 425). | ✅ 10/10
| 036 | 2026-07-21 | refactor+ddd+tdd+bdd | SLD-6.2 Extraction `Configuration` → `slider.config` (nouveau domaine, 3 fichiers : `ConfigPath.kt` 21l value object, `YamlMapperFactory.kt` 34l factory ObjectMapper, `SlidesConfigLoader.kt` 70l domain service). `SliderManager.Configuration` → thin adapter (délègue à `slider.config`). Fix deprecation `properties[...]` lignes 80 + 638 → `findProperty(...)` + réutilisation `localConf` dans `registerPublishSlidesTask`. Imports morts nettoyés (5 imports Jackson/Arrow retirés de `SliderManager.kt`). Baby-step TDD/BDD 3 niveaux : 16 unit (ConfigPathTest 4 + YamlMapperFactoryTest 5 + SlidesConfigLoaderTest 7) + 15 scénarios Cucumber (`16_config_decomposition.feature` + `ConfigDecompositionSteps.kt`, steps préfixés `config` pour éviter collisions). `./gradlew :slider:check` VERT 466 PASS / 0 FAIL / 1 SKIP (+11 vs 455 S-035). | ✅ 9/10
| 037 | 2026-07-21 | refactor+ddd+tdd+bdd | SLD-6.3 Extraction `Prerequisites` → `slider.prerequisite` (nouveau domaine, 1 fichier : `JavaVersionGuard.kt` 45l object pur). `requireJava23(currentMajor: Int)` + `requireJava23FromMajor(majorVersionString: String)` (parse String issue de `JavaVersion.current().majorVersion`, zéro dépendance Gradle). `SliderManager.Prerequisites` → thin adapter 11 lignes (passe `JavaVersion.current().majorVersion` au guard). Signature publique `SliderPlugin.apply()` préservée, zéro régression. Baby-step TDD/BDD 3 niveaux : 9 unit (`JavaVersionGuardTest`) + 9 scénarios Cucumber (`17_prerequisite_decomposition.feature` + `PrerequisiteDecompositionSteps.kt`). `./gradlew :slider:check` VERT 498 PASS / 0 FAIL / 1 SKIP (+32 vs 466 S-036). | ✅ 9/10
| 038 | 2026-07-22 | refactor+ddd+tdd+bdd | SLD-6.4 Extraction `Extensions` → `slider.extension` (nouveau domaine, 3 fichiers : `RevealJsPin.kt` 37l value object pin Reveal.js 5.2.1, `SliderExtension.kt` 30l DSL déménagée depuis SliderPlugin, `SliderExtensionRegistrar.kt` 53l `configure(project, pin)` enregistre DSL + pin). `SliderManager.Extensions` → thin adapter 12 lignes (délègue à `SliderExtensionRegistrar.configure(this)`). `SliderPlugin.SliderExtension` → `typealias slider.extension.SliderExtension` (préserve référence `RagTasks.kt`). Imports morts nettoyés (SliderExtension + RevealJSExtension retirés de SliderManager ; ObjectFactory/Property/Inject retirés de SliderPlugin). Baby-step TDD/BDD 3 niveaux : 9 unit (`RevealJsPinTest`) + 9 scénarios Cucumber (`18_extension_decomposition.feature` + `ExtensionDecompositionSteps.kt`). `./gradlew :slider:check` VERT 516 PASS / 0 FAIL / 1 SKIP (+18 vs 498 S-037). | ✅ 9/10
| 035 | 2026-07-20 | cadrage+feat+ddd+tdd+bdd | Cadrage EPIC SLD-6 — Refactor DDD SliderManager decomposition (11 US, ~20 pts, cible thin orchestrator < 100 lignes). US-6.1 terminée : extraction `Scaffold` → `slider.scaffold` (3 fichiers : `ScaffoldResult.kt` 12l, `SlidesScaffolder.kt` 65l, `ScaffoldDefaults.kt` 92l). `SliderManager.Scaffold` réduit ~184→65 lignes (thin adapter). Imports morts nettoyés (`ZipInputStream`, `IOException`). Baby-step TDD/BDD 3 niveaux : 12 unit (SlidesScaffolderTest + ScaffoldDefaultsTest) + 15 scénarios Cucumber (`15_scaffold_decomposition.feature` + `ScaffoldDecompositionSteps.kt`). `./gradlew :slider:check` VERT 455 PASS / 0 FAIL / 1 SKIP (+27 vs 428 S-034). | ✅ 9/10
| 033 | 2026-07-14 | audit | Audit i18n slider-gradle — confirmation support i18n entièrement couvert et fonctionnel. SLD-3 (config + UI + RTL, 7/7 US) et SLD-5 (pipeline deck one-to-many, 7/7 US) TERMINÉS. 10 langues, RTL ar/ur, fallback cascade 5 couches, pipeline `translateDeck`, validation Playwright LTR+RTL, dogfooding 3 decks × 10 langues. 3 domaines DDD i18n inventoriés : `slider.i18n` (5 fichiers), `slider.translate` (2 fichiers), `slider.translation` (8 fichiers). Dépendance N0 `i18n-contracts:0.0.2`. 8 features Cucumber i18n. Nettoyage 3 répertoires vides `com/cheroliv/slider/{ai,translate}/` (héritage renommage S-005). Zéro fichier de production modifié. `./gradlew :slider:check` VERT — 425 PASS / 0 FAIL / 2 SKIP (UP-TO-DATE). | ✅ 8/10
| 032 | 2026-07-14 | ops+pub | Publication slider 0.0.10 sur Maven Central + mise à jour BOM (workspace-bom 0.0.9). Bump 0.0.9 → 0.0.10 sur 3 fichiers (build.gradle.kts + 2 catalogues). Bundle nmcp `ea117827-531a-4d3c-8a45-c58bc9d34eb1` AUTOMATIC, 3 artefacts signés (slider:0.0.10 + 2 plugin markers). BOM 0.0.9 publiée (bundle `79395cf5`), slider constraint 0.0.1 → 0.0.10 + correction module name (`slider-plugin` → `slider`). `./gradlew :slider:check` VERT — 425 PASS / 0 FAIL / 2 SKIP (UP-TO-DATE). | ✅ 9/10
| 031 | 2026-07-14 | docs+i18n | README i18n (9 fichiers `README.plugin/README.*.md` — ar, bn, es, fr, hi, pt, ru, ur, zh) alignés sur le master EN (S-030). 6 édits par langue (55 total) : module layout (4 domaines DDD `repository/`, `capsule/`, `rtl/`, `translation/` + SliderManager desc + test `scenarios/slider/steps/`), test matrix `slider.steps.**`, EPIC table SLD-5 row + "session 030", contribution `FileOps` supprimé. FR switcher 10 langues. `./gradlew :slider:check` VERT — 425 PASS / 0 FAIL / 2 SKIP (UP-TO-DATE, zéro régression — les READMEs ne sont pas du code). | ✅ 8/10
| 030 | 2026-07-14 | feat+dogfood+tdd+bdd | EPIC SLD-5 TERMINÉ (7/7 US) — US-5.6 Dogfooding : 3 decks demo fr/en/ar étendus à 10 langues via `translateDeck` (LLM réel `gpt-oss:120b-cloud` port 11439). 54 fichiers générés (27 `.adoc` + 27 `deck-context.yml`). `DeckTranslator.kt` enrichi `ensureRtlDirection` (injecte/supprime `:revealjs_direction: rtl` post-traduction). Validation Playwright LTR (fr→en `slides=5, rtl=false`) + RTL (fr→ar `slides=5, rtl=true, screenshot=32KB`). 3 `deck-context.yml` créés pour decks demo. Baby-step TDD/BDD : `DemoDeckContextTest` (6 unit) + `TranslatedDemoDeckTest` (~50 unit paramétrés) + `DeckTranslatorTest` (+4 RTL) + `14_deck_translation_dogfood.feature` (~20 scénarios BDD). `./gradlew :slider:check` VERT 425 PASS / 0 FAIL / 2 SKIP. README master EN mis à jour. | ✅ 9/10
| 029 | 2026-07-13 | feat+ddd+tdd+bdd | EPIC SLD-5 Deck Translation Pipeline — 6/7 US terminées. Domaine DDD `slider.translation` (8 fichiers : TranslationRequest, DeckTranslationPlan, TranslationResult sealed 3 variants, TranslationOutcome, LanguageModelAdapter port, DeckTranslator domain service, OllamaLanguageModelAdapter adaptateur langchain4j, TranslateDeckTask tâche `translateDeck`). Pipeline one-to-many : deck source → 10 variantes localisées (top 10 langues `LanguageCatalog.supportedCodes()`). Baby-step TDD/BDD 3 niveaux : 71 unit + 4 functional + 25 cucumber = +100 tests. `./gradlew :slider:check` VERT 270 PASS / 0 FAIL / 2 SKIP. US-5.6 (dogfooding) restante. | ✅ 9/10
| 028 | 2026-07-11 | docs+i18n+pub | README i18n (20 fichiers — 2 EN masters + 9 consommateurs + 9 plugins) mis à jour avec domaine DDD `slider.repository` (SlideDeploymentRequest, SlideDeployer, JGitSlidePusher) + 2 autres domaines (`slider.capsule`, `slider.rtl`). Nouvelle section "Domain model (DDD)" dans les 10 README consommateurs (traduite dans 9 langues). Module layout enrichi dans les 10 README plugin. EPIC status mis à jour toutes langues (SLD-2/3/4 ✅, CNV-6/7/8 ✅, PUB 0.0.8 + 0.0.9 ✅). Bump 0.0.8 → 0.0.9 (build.gradle.kts + 2 catalogues libs.versions.toml). Publication Maven Central via nmcp — bundle `63048fec-9f69-4cfa-812c-7adb63fa0fef`, publishingType AUTOMATIC, 3 artefacts signés (slider:0.0.9 + 2 plugin markers `education.cccp.slider` + `education.cccp.slider.translator`). `./gradlew :slider:check` VERT 211 PASS / 0 FAIL / 3 SKIP (inchangé). | ✅ 9/10
| 027 | 2026-07-11 | refactor+ddd+tdd+bdd | Extraction `JGitSlidePusher` (adaptateur JGit) depuis `SliderManager.Git` — `initAndCommit` (Git.init + remoteAdd + add + commit) + `push` (Git.open + push + credentials) avec types domaine `CommitResult`/`SlidePushResult` (zéro leak JGit). `SliderManager.Git` réduit ~120→55 lignes (suppression `initAddCommitToSlides` + `pushSlide` + 7 imports JGit). Tests intégration pipeline avec bare repo local (`SlideDeploymentPipelineIntegrationTest` 3 tests). Suppression `GitOperationResult` dead code (leak `RevCommit`/`PushResult` dans `models.kt`). Baby-step TDD/BDD : 6 unit (JGitSlidePusherTest) + 3 unit (pipeline integration) + 3 cucumber (12_git_push_adapter.feature). `./gradlew :slider:check` VERT 211 PASS / 0 FAIL / 3 SKIP. **EPIC SLD-4 TERMINÉ** 8/8 US. | ✅ 9/10
| 026 | 2026-07-11 | refactor | Rename Cucumber steps package `slider.scenarios` → `slider.steps`. Fix double `scenarios` dans chemin `src/test/scenarios/slider/scenarios/` → `src/test/scenarios/slider/steps/`. 12 fichiers renommés, glue Cucumber mise à jour, exclusion locale `slider.steps.**` ajoutée au `test` task. Zéro régression. | ✅ 8/10
| 025 | 2026-07-11 | refactor+ddd+tdd+bdd | Refactor DDD slider.repository — extraction `SlideDeploymentRequest` + `GitCredentials` (value objects) + `SlideDeployer` (createRepoDir/copySlides/cleanup, typed results RepoDirResult/CopyResult) depuis `SliderManager.Git`. `FileOps` + `FileOperationResult` supprimés (dead code). `Git.pushSlides` délègue au domaine. Baby-step TDD/BDD : 16 unit (10 SlideDeploymentRequestTest + 6 SlideDeployerTest) + 2 functional (SlideDeploymentFunctionalTest) + 9 cucumber (11_deployment_request.feature). `./gradlew :slider:check` VERT 196 PASS / 0 FAIL / 4 SKIP. | ✅ 9/10
| 024 | 2026-07-11 | cleanup | Cleanup dead code + Playwright @Disabled + check vert — @Disabled 2 tests Playwright (gem JRuby isolé, root cause S-023), suppression BakeryPluginFunctionalTests.kt (388 lignes mortes), fix KDoc Reveal.js 3.9.1→5.2.1, suppression TODO Slides.kt:44. `./gradlew check` vert pour la première fois. 94 unit (0 fail, 2 skip) + 11 functional + 65 cucumber (0 fail, 1 skip). | ✅ 8/10
| 023 | 2026-07-09 | investigation | Investigation Playwright Gem JRuby — diagnostic root cause GradleRunner crée Gradle user home isolé dans /tmp, JRuby ne trouve pas la gem asciidoctor-revealjs. 5 approches testées (withGradleUserHomeDir absent API 9.6.1, GRADLE_USER_HOME env var non respectée, --offline bloque mavenLocal, asciidoctorGemsPrepare insuffisant, gradleTestKit() compile mais méthode absente). Non fixable sans modification API GradleRunner. | 🔍 7/10
| 022 | 2026-07-09 | bump+publish | Bump 0.0.8 + Publication Maven Central — version 0.0.1→0.0.8 (slider + catalogue racine), codebase 0.0.1→0.0.5 (dernière Central), readme 0.0.0→0.0.1, correction namespace plugin ID `slider.translator`→`education.cccp.slider.translator` (Central education.cccp.* seulement), bundle nmcp 4684447b AUTOMATIC, 3 artefacts confirmés repo1.maven.org (slider:0.0.8 + 2 plugin markers), zéro régression 94 unit + 65 cucumber + 11 functional identiques pré/post (2 fail Playwright env préexistant) | ✅ TERMINÉ
| 021 | 2026-07-09 | feat+tests+bdd | US-3.4-bis Validation visuelle Playwright RTL — domaine DDD slider.rtl (RtlAssertionCode + RtlAssertionResult + SlideRenderData + RtlSlideAssertion, pure logic 4 contraintes 3 P0 + 1 P1) + RtlPlaywrightFunctionalTest (GradleRunner -Planguage=ar mavenLocal 0.0.7 → Playwright-jvm HTML → SlideRenderData → assertAll) + 10_rtl_visual_validation.feature 6 scénarios BDD — révélation modèle RTL Reveal.js = rtl:true config JS + .rtl classe runtime PAS <html dir>, assertions adaptées — 36 unit (+8) + 5 functional (+1) + 64 cucumber (+6) PASS | ✅ TERMINÉ
| 020 | 2026-07-07 | feat+tests+bdd+dogfood | SLD-2 US-2.5 (Capsule Feed — domaine DDD slider.capsule: CapsuleScript + SlideSegment + CapsuleScriptWriter + AsciidocSpeakerNoteParser, refactor generateCapsule task, audit cross-borough contrat réel capsule-gradle, 24 unit + 4 functional + 9 cucumber BDD 9_capsule_feed.feature + 4 Cucumber dogfood) + SLD-3 US-3.6 (dogfooding 3 decks demo fr/en/ar + CapsuleFeedDemoDeckTest 4 unit + 4 cucumber) — 28 unit + 4 functional + 58 cucumber PASS, BUILD SUCCESSFUL | ✅ TERMINÉ
| 019 | 2026-07-06 | feat+tests+bdd | SLD-3 i18n US-3.3 (RevealUiMessages value object + RevealUiMessageCatalog 10 langues + RevealUiMessagesWriter messages_{code}.js + generateRevealUiMessages task group=slider) + US-3.4 (RevealRtlResolver resolveRtl/rtlLanguages + câblage asciidoctorRevealJs setRightToLeft -Planguage=ar|ur Groovy-Kotlin interop) + US-3.5 (8_i18n_fallback.feature 6 scenarios fallback/validation cascade) — 57 unit (was 48, +9 RevealRtlResolver) + 3 functional + 46 cucumber (was 34, +12: 6_reveal_ui_i18n 10 + 7_rtl_layout 6 + 8_i18n_fallback 6) PASS, ./gradlew check green | ✅ TERMINÉ
| 018 | 2026-07-06 | feat+tests+governance | SLD-3 i18n US-3.2 (TranslatorManager 2→10 langues via LanguageCatalog.supportedCodes, adaptive prompt with native names via LanguageCatalog.findByCode, slider.translator plugin registered, 90 translation tasks, displaySupportedLanguages native names) + English Only governance (Rule 7 AGENT.adoc, workspace AGENTS.adoc transverse rule, Gherkin FR purge cross-borough: logback-exclusion-conventions.feature + SignUp.feature migrated EN) — 11 unit + 4 functional + 7 cucumber PASS, ./gradlew check green | ✅ TERMINÉ
| 017 | 2026-07-06 | fix+feat+tests | Baseline A (rupture noms tâches RAG collectRagIndex→reindexRag, generateDeckContext→proposeDeckContext, cucumberTest 6→16 PASS) + CNV-8 logback-exclusion convention (conventions-gradle TDD feature fr + plugin 24 lignes, migration slider -13 lignes, ./gradlew check vert) | ✅ TERMINÉ
| 016 | 2026-07-06 | refactor | CNV-6/7 Migration functional-test + cucumber conventions — 4/4 conventions plugins appliquées (gradle-plugin + publishing + functional-test + cucumber), build.gradle.kts 251→157 lignes (-114, -45%). functionalTest ✅, cucumberTest stable (10 FAIL préexistants), test 22 PASS (bug filtre package S-007 résolu). | ✅ TERMINÉ
| 015 | 2026-07-06 | feat+tests | SLD-3 i18n US-3.1 — I18nConfigResolver (cascade 5 couches CLI > gradle.properties > DSL > YAML > default) + SliderExtension.language/supportedLanguages + RagTasks câblé. 9/9 I18nConfigResolverTest PASS. | ✅ TERMINÉ
| 014 | 2026-07-06 | feat+tests | SLD-3 i18n US-3.0 — dépendance i18n-contracts:0.0.2 + DeckContext.language ("French") → languageCode ("fr" ISO 639-1) + requireValidLanguage() (LanguageCatalog.supportedCodes). 5/5 DeckContextLanguageTest PASS. 3 sites corrigés (SliderManager, AssistantManager, steps Cucumber). 10 échecs Cucumber préexistants (infra GradleRunner). | ✅ TERMINÉ
| 013 | 2026-07-03 | dev | CNV-6 Migration — convention plugins education.cccp.build.* v0.0.1 appliquées (gradle-plugin + publishing), build.gradle.kts 301→205 (-96), compileKotlin SUCCESS. Variantes gardées : buildscript annotations force, exclusions logback, extendsFrom legacy, compatibility configurationCache=false | ✅ TERMINÉ
| 011 | 2026-06-24 | docs+i18n | README i18n 10 langues (consommateur + plugin) — 20 fichiers Markdown créés (README.consommateurs/ + README.plugin/), switcher i18n cross-langue, badges Maven Central/Plugin Portal, markers version 0.0.1, RTL ar/ur. Socle doc SLD-3.6. | 9/10
| 009 | 2026-06-11 | publish | Publication Maven Central 0.0.1 — version 0.0.6→0.0.1 hardcodé, POM sans guard, Sonatype→nmcp, settings nmcp, publié avec -x cucumberTest -x functionalTest | 9/10
| 008 | 2026-06-02 | governance | Borough-isation — slider-gradle devient MIAMI 🌴, 21ème borough | 10/10
| 007 | 2026-05-30 | refactor | Migration plugin ID com.cheroliv.slider → education.cccp.slider — 5 fichiers corrigés (4 tests + 1 source : SliderWorld.kt, ProposeDeckContextSteps.kt, 2× logback-test.xml, SliderManager.kt). Zéro référence restante. test ✅, functionalTest ✅, cucumberTest 6/17 PASS (10 FAIL: proposeDeckContext, infra préexistante) | 9/10
| 006 | 2026-05-30 | fix+tests | SLD-3a Playwright E2E fix — 3 bugs corrigés : file:// → HTTP local (npx serve + startHttpServer polling 15s), chemin output corrigé (build/docs/asciidocRevealJs/intro.html), strict mode .first() sur 4 sections reveal.js. Test unitaire Playwright PASSED : navigate OK, title/slides/h2 visibles, bounding box viewport contraint, screenshot > 10 KB. | 9/10
| 005 | 2026-05-30 | feat+tests+refactor | SLD-3a Playwright-jvm e2e foundations — renommage package com.cheroliv.slider→slider (20 fichiers), fix bug dormant TASK_ASCIIDOCTOR_REVEALJS="generateSlides"→"asciidoctorRevealJs", implementationClass="slider.SliderPlugin", playwright-jvm 1.52.0 en testImplementation, SliderPlaywrightE2eTest.kt (GradleRunner mavenLocal → asciidoctorRevealJs BUILD SUCCESSFUL, HTML généré ✅), Playwright navigate ⚠️ file:// CDN (TODO HTTP serve session 006) | 8/10
| 001 | 2026-05-10 | feat | EPIC SLD-1 — US-1.1 + US-1.2 (Upgrade reveal.js + Transition) | ✅ TERMINE
| 002 | 2026-05-10 | feat | EPIC SLD-1 — US-1.3 + US-1.4 (Auto-Animate + Tests) | ✅ TERMINE
| 003 | 2026-05-10 | feat | EPIC SLD-1 — US-1.5 (Validation dogfooding) | ✅ TERMINE
|===

'''
== Roadmap — Etat Actuel

|===
| EPIC | Description | Statut | Priorite
| SLD-0 | Bootstrap gouvernance agent slider (.agents/, AGENT.adoc, doc/) | ✅ TERMINE | P0
| SLD-1 | Upgrade Reveal.js 3.9.1→5.2.1 + Auto-Animate + Transitions configurables | ✅ TERMINE (5/5 US) | P0
| SLD-2 | Playwright E2E + Theme pro + Capsule feed (5 US) | ✅ TERMINÉ (5/5 US) | P0
| SLD-3 | i18n 10 langues — contrats N0 i18n-contracts, pattern bakery BKY-I18N (7 US, ~15 pts) | ✅ TERMINÉ (7/7 US) | P1
| US-3.4-bis | Validation visuelle Playwright RTL complète sur deck arabe (reportée US-3.4 S-019) | ✅ TERMINÉ (S-021) | P1
| CNV-6 | Migration conventions plugins education.cccp.build.* v0.0.1 (gradle-plugin + publishing) | ✅ TERMINÉ (session 013, -96 lignes) | P1
| CNV-7 | Migration functional-test + cucumber conventions (4/4 conventions) | ✅ TERMINÉ (session 016, -114 lignes) | P1
| CNV-8 | logback-exclusion convention (conventions-gradle plugin + migration slider) | ✅ TERMINÉ (session 017, -13 lignes slider) | P1
| Publication Maven Central 0.0.1 | nmcp, POM sans guard, version 0.0.1 hardcodé, settings nmcp | ✅ 2026-06-11 | P2
| Publication Maven Central 0.0.8 | Bump strictement montant (0.0.1→0.0.8, codebase 0.0.5, readme 0.0.1), correction namespace `slider.translator`→`education.cccp.slider.translator`, bundle 4684447b AUTOMATIC, 3 artefacts confirmés repo1.maven.org | ✅ 2026-07-09 | P2
| Publication Maven Central 0.0.9 | README i18n slider.repository (20 fichiers) + bump 0.0.8→0.0.9, bundle 63048fec AUTOMATIC, 3 artefacts signés | ✅ 2026-07-11 | P2
| Publication Maven Central 0.0.10 | Bump 0.0.9→0.0.10 (SLD-5 translation + README i18n S-031), bundle ea117827 AUTOMATIC, 3 artefacts signés. BOM 0.0.9 publiée (slider constraint 0.0.1→0.0.10, module name fix). | ✅ 2026-07-14 | P2
| Publication Maven Central 0.0.11 | Bump 0.0.10→0.0.11 (Playwright @Disabled fix S-034), bundle b18431e5 AUTOMATIC, 3 artefacts signés. BOM 0.0.10 publiée (bundle 61519263, slider constraint 0.0.11). | ✅ 2026-07-15 | P2
| Publication Maven Central 0.0.12 | Bump 0.0.11→0.0.12 (SLD-6.8 capsule task decomposition S-040), bundle 842a5a72 AUTOMATIC, 3 artefacts signés. BOM 0.0.11 publiée (bundle 261de995, slider constraint 0.0.12). | ✅ 2026-07-22 | P2
| Publication Maven Central 0.0.13 | Bump 0.0.12→0.0.13 (fix CR-GLOBALE-002 S-045 : guard plan vide + regex uppercase + SSL scopé), bundle ec715b09 AUTOMATIC, confirmé repo1.maven.org. | ✅ 2026-08-04 | P2
| Publication Maven Central 0.0.14 | Bump 0.0.13→0.0.14 (SLD-9.1 AssistantManager i18n + SLD-10 content domain complet 7/7 US), bundle 6f7b84be AUTOMATIC, 3 artefacts signés (slider + 2 plugin markers). Synchronisation capsule-gradle (0.0.6→0.0.14 + plugin ID education.cccp.slider). BOM 0.0.14 publiée (bundle c8bf911c, constraint slider 0.0.14). | ✅ 2026-08-05 | P2
| Publication Maven Central 0.0.15 | Bump 0.0.14→0.0.15 (post-SLD-11 expulsion `slider.capsule` S-058, -113 tests slider). Bundle slider 832cea2a AUTOMATIC, 3 artefacts signés (slider + 2 plugin markers). BOM 0.0.15 publiée (bundle 02307b00, constraint slider 0.0.15). Sync capsule catalogue slider 0.0.15. Coordination capsule 069 : EPIC CAP-MODEL (unification modèle capsule.feed). | ✅ 2026-08-05 | P2
| CR-GLOBALE-002 | Fix 3 bugs Option A — guard plan vide `translateDeck`, regex `buildOutputFile` uppercase, propriétés système SSL scopées `RagManager`. | ✅ TERMINÉ (S-045, 3/3 US) | P2
| SLD-4 | Refactor DDD slider.repository — extraction domaine depuis SliderManager.Git (SlideDeploymentRequest, GitCredentials, SlideDeployer, JGitSlidePusher, CommitResult, SlidePushResult) | ✅ TERMINÉ (S-025 + S-027, 8/8 US) | P1
| SLD-5 | Deck Translation Pipeline — génère 10 decks localisés (one-to-many) depuis un deck source (deck-context.yml + .adoc). Domaine DDD `slider.translation` + tâche `translateDeck`. Aligné sur `LanguageCatalog.supportedCodes()` (en, zh, hi, es, fr, ar, bn, pt, ru, ur — top 10 langues les plus parlées). | ✅ TERMINÉ (S-029 + S-030, 7/7 US) | P1
| SLD-6 | Refactor DDD SliderManager decomposition — `SliderManager.kt` (968 lignes, 9 nested objects) est un god-object. Extraction de chaque responsabilité dans un domaine dédié (`slider.scaffold`, `slider.config`, `slider.prerequisite`, `slider.extension`, `slider.wiring`, `slider.revealjs`, `slider.playwright`, `slider.capsule` consolidé) + consolidation `slider.repository` existant. Cible : `SliderManager` thin orchestrator < 100 lignes. Fix deprecation `properties[...]` (lignes 82, 744). Baby-step TDD/BDD 3 niveaux, English only. | ✅ TERMINÉ (S-042, 11/11 US) | P1
| SLD-7 | Câblage Explicite translateDeck → Capsule — tâche composite `translateAndGenerateCapsule` (dependsOn translateDeck → generateCapsule). Contrat d'interface slider↔capsule pour pipeline multi-langue. | ✅ TERMINÉ (S-041, 3/3 US) | P1
| SLD-CR3 | Revue CR-GLOBALE-003 — Déprécier `translate/` doublon + resserrer `catch (_: Exception)` CapsulePlugin auto-apply slider. | ✅ TERMINÉ (S-044, 2/2 US) | P1
| SLD-8 | Consommation socle N1 codebase — unifier les appels LLM slider. Premier consommateur de la couche LLM codebase : `BuildService` LLM (Provider<ChatModel> via LlmProviderResolver/OllamaPool), koog/langchain4j en `api` (pas de koog direct via BOM), `AssistantManager` réduit à du câblage (4 factories supprimées), `DeckPipelineGraph` (propose-context/validate-context/generate-deck) sur services codebase. DI native Gradle (Decision 001). | 🟡 EN COURS (S-061 cadrage US-8.3, US-8.0/8.1/8.2 ✅, US-8.3 à implémenter, US-8.4 à planifier) | P1
| SLD-9 | i18n Completion via LLM — `AssistantManager` i18nisé (13 clés → 10 langues générées par LLM), 9 tests de traduction par langue (pattern plantuml), `TaskDescriptionsI18nTest` functional, `SliderMessagesTest` unitaire + Cucumber BDD. Périmètre restrictif : que de l'i18n, pas de refacto. | ✅ TERMINÉ (S-057, 5/5 US — 9.0/9.1/9.2/9.3/9.4) | P1
| SLD-10 | Slide Content Model & Rendering Validation — domaine DDD `slider.content` (SlideContent, TextBlock, SlideLayout, OverflowDetector, AsciidocContentParser, ContentSlideAssertion, ContentRenderData, ContentRenderAssertion) + adaptateur Playwright (ContentPlaywrightAdapter). Tests 3 niveaux (unit → functional → cucumber). Pattern de référence : `slider.rtl.RtlSlideAssertion` + `slider.capsule.AsciidocSpeakerNoteParser`. | ✅ TERMINÉ (S-054, 7/7 US — 10.0/10.1/10.2/10.3/10.4/10.5/10.6) | P1
| SLD-11 | Expulsion `slider.capsule` — realignment boundary slider ↔ capsule. Le domaine `slider.capsule` (4 fichiers prod + 8 fichiers test) est une fuite de responsabilite (preparation de script video dans slider). Expulsion vers capsule-side : capsule parse `.adoc` directement, slider s'arrete au HTML Reveal.js. 6 US ~14 pts. | ✅ TERMINÉ (S-058, 6/6 US — 11.0 cadrage S-056 + 11.1→11.6) | P1
|===

'''
== Décisions d'Architecture

Voir `slider-plugin/.agents/DECISIONS_ARCHITECTURE.adoc` (convention ADR workspace) :
* **Decision 001** — PicoContainer : rejet assumé de la DI conteneur (✅ ADOPTÉE, S-046)
* **Decision 002** — Consommer codebase (N1) comme socle LLM unifié (🟡 EN COURS, S-046)

_Discussion architecturale complète_ : `slider-plugin/.agents/ARCHITECTURE_DISCUSSION_SLD8.adoc`
(audit éparpillement 8 boroughs, gap codebase, stratégie transferts unitaires, risques).

'''
== EPIC SLD-1 — Detail US

_Vision_ : Des transitions modernes et des slides fluides grace
a Auto-Animate (reveal.js 4.0+), le tout configurable depuis
le DeckContext YAML.

|===
| US | Description | Statut
| US-1.1 | Upgrade reveal.js (5.2.1) + gem asciidoctor-revealjs (5.0.0) | ✅ TERMINE
| US-1.2 | Exposer `transition` dans `RevealJsContext` + `data-transition` per-slide | ✅ TERMINE
| US-1.3 | Exposer Auto-Animate dans `SlideHint` + system prompt | ✅ TERMINE
| US-1.4 | Tests Cucumber — scenarios transition + auto-animate (17/17 PASS) | ✅ TERMINE
| US-1.5 | Validation dogfooding — deck avec transitions custom + auto-animate | ✅ TERMINE
|===

'''
== EPIC SLD-2 — Detail US

_Vision_ : Rendu visuel professionnel validé par Playwright, thème reveal.js pro,
transitions fluides, feed capsule pour capsule-gradle.

|===
| US | Description | Statut
| US-2.1 | Ajouter Playwright via node-gradle (npm install @playwright/test + browsers) | ✅ TERMINE
| US-2.2 | Tâche `slider:visualTest` — Playwright screenshot chaque slide + snapshot CI | ✅ TERMINE
| US-2.3 | Theme CSS reveal.js pro (typo, palette Talaria, header/footer, barre progression) | ✅ TERMINE
| US-2.4 | Tests validation visuelle Playwright — 10 contraintes P0/P1/P2 par slide | ✅ TERMINE
| US-2.5 | Feed capsule — extraction notes orateur → `*-script.txt` contrat capsule-gradle, domaine DDD `slider.capsule` (CapsuleScript + SlideSegment + CapsuleScriptWriter + AsciidocSpeakerNoteParser), refactor generateCapsule task, tests 3 niveaux (24 unit + 4 functional + 13 cucumber) | ✅ TERMINÉ (S-020)
|===

'''
== EPIC SLD-3 — Detail US

_Vision_ : Internationalisation 10 langues (en, zh, hi, es, fr, ar, bn, pt, ru, ur)
via les contrats N0 `education.cccp:i18n-contracts:0.0.1`. Pattern aligné sur
bakery BKY-I18N (config cascade 5 couches, MessageBundle, LocaleResolver).
RTL support pour arabe (ar) et ourdou (ur) via Reveal.js `rtl: true`.

_Dependance_ : i18n-contracts N0 (workspace-bom MEM-3, mavenLocal, publication Central en attente).

|===
| US | Description | Statut | Estimation
| SLD-3.0 | Ajouter dépendance `i18n-contracts` + remplacer `DeckContext.language` (String "French" → ISO 639-1) + valider contre `LanguageCatalog.supportedCodes()` | ✅ TERMINÉ (S-014) | 2 pts
| SLD-3.1 | Configuration layer — DSL `language`/`supportedLanguages` + cascade 5 couches (CLI `-Planguage` > gradle.properties > DSL > deck-context.yml > default "fr") + `I18nConfig.validate()` | ✅ TERMINÉ (S-015) | 3 pts
| SLD-3.2 | TranslatorManager modernisation — remplacer `setOf(FRENCH, ENGLISH)` par `LanguageCatalog.ALL` (2→10 langues) + system prompt LLM adaptatif par langue | ✅ TERMINÉ (S-018) | 3 pts
| SLD-3.3 | Reveal.js UI i18n — générer `messages_{code}.js` pour 10 langues (navigation, contrôles, tooltips) + injecter `lang: {code}` dans config Reveal.js | ✅ TERMINÉ (S-019) | 2 pts
| SLD-3.4 | RTL support — slides arabe/ourdou avec `rtl: true` + layout CSS RTL + validation visuelle Playwright | ✅ TERMINÉ (S-019, resolver + câblage, validation Playwright deck complète restante US-3.6) | 2 pts
| SLD-3.5 | Tests Cucumber BDD — scénarios i18n (génération par langue, RTL, fallback, validation config) | ✅ TERMINÉ (S-019) | 2 pts
| SLD-3.6 | Documentation + dogfooding — deck multilingue de démonstration (fr+en+ar) + CapsuleFeedDemoDeckTest (4 unit) + 4 scénarios Cucumber dogfood | ✅ TERMINÉ (S-020) | 1 pt
|===

_Total_ : 7 US, ~15 pts.
_Pattern de référence_ : bakery-gradle BKY-I18N (US-I18N-0→6, 23 pts).
_Prérequis_ : i18n-contracts publié sur Maven Central (US-MEM-3.6).

'''
== EPIC SLD-5 — Detail US

_Vision_ : Pipeline de traduction de deck one-to-many. Partant d'un deck source
(deck-context.yml + .adoc) dans une langue, génère les 10 variantes localisées
couvrant le top 10 des langues les plus parlées (source : `10 langues les plus
parlés.csv` — aligné sur `LanguageCatalog.supportedCodes()` : en, zh, hi, es, fr,
ar, bn, pt, ru, ur).

_Problématique_ : Aujourd'hui slider sait générer un deck dans UNE langue
(`proposeDeckContext -Planguage=X`) et traduire du texte libre entre 10 langues
(`translate{From}To{To}`). Il ne sait PAS prendre un deck existant et produire
ses 10 variantes localisées. SLD-5 comble ce manque — c'est le chaînon manquant
du story i18n entamé en SLD-3.

_Domaine DDD_ : `slider.translation` (nouveau) — value objects purs
(TranslationRequest, TranslationResult, DeckTranslationPlan) + adaptateur LLM
(DeckTranslator) + tâche Gradle thin (`translateDeck`). Zéro leak LLM hors
adaptateur (pattern `slider.repository` / `slider.capsule`).

_Dépendances_ :
* `i18n-contracts:0.0.2` (LanguageCatalog.supportedCodes, findByCode) — déjà branché
* `slider.ai.AssistantManager` (resolveModel, OllamaChatModel) — déjà en place
* `slider.translate.TranslatorManager.PromptManager.getTranslatePromptMessage` — réutilisé pour le prompt LLM par langue cible

_Méthode_ : baby-step TDD/BDD — RED → GREEN → REFACTOR. Trois niveaux de tests
par US : unit (value object pur) → functional (GradleTestKit) → cucumber (BDD
end-to-end). English only dans le code, Gherkin, commentaires.

_Contrats de test_ :
* Unit : value objects purs, zéro dépendance Gradle/LLM. Mock LLM via interface.
* Functional : tâche `translateDeck` exécutée via GradleRunner, asserts sur
  fichiers générés (10 × 2 = 20 fichiers : 10 `*-<lang>-deck-context.yml` +
  10 `*-<lang>-deck.adoc`).
* Cucumber : `13_deck_translation.feature` — scénarios BDD one-to-many,
  fallback langue source, validation LanguageCatalog, fichier source absent.

|===
| US | Description | Statut | Estimation

| SLD-5.0
| Cadrage EPIC — vision, domaine `slider.translation`, découpage US, dépendances. Mise à jour INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine MIAMI.
| ✅ TERMINÉ (S-029) | 1 pt

| SLD-5.1
| Value object `TranslationRequest` (source DeckContext + targetLanguages: List<String> validée contre LanguageCatalog.supportedCodes()) + `DeckTranslationPlan` (permutation source→target, 9 tasks si source ∈ supported, 10 sinon). Validation : refus langue source invalide, refus liste cible vide, refus doublon. *Unit tests purs* (zéro Gradle, zéro LLM).
| ✅ TERMINÉ (S-029, 19 unit) | 3 pts

| SLD-5.2
| Value object `TranslationResult` (per-language : DeckContext traduit + adoc traduit + status Translated/Skipped/Failed) + `TranslationOutcome` (agrégation 10 résultats, statistiques ok/skipped/failed). *Unit tests purs*.
| ✅ TERMINÉ (S-029, 25 unit) | 2 pts

| SLD-5.3
| `DeckTranslator` (domain service) — orchestre la traduction par langue cible via LLM. Réutilise `TranslatorManager.PromptManager` pour le prompt. Interface `LanguageModelAdapter` (port) pour mockable. Zéro leak langchain4j dans le domaine. *Unit tests avec mock LLM* (retour contrôle, fallback, partiel).
| ✅ TERMINÉ (S-029, 10 unit) | 3 pts

| SLD-5.4
| Tâche Gradle `translateDeck` — `@DisableCachingByDefault`, group "translator", inputs : `-Pdeck.context=<path>` + `-Ptarget.languages=en,zh,hi,...` (défaut : LanguageCatalog.supportedCodes()). Outputs : `slides/misc/<slug>_<lang>-deck-context.yml` + `<slug>_<lang>-deck.adoc` × 10. Délègue à `DeckTranslator`. *Functional tests GradleTestKit*.
| ✅ TERMINÉ (S-029, 2 functional) | 3 pts

| SLD-5.5
| Cucumber `13_deck_translation.feature` — scénarios BDD : one-to-many (1 source → 10 cibles), langue source invalide rejetée, langue cible inconnue filtrée, fallback langue source, fichier source absent erreur claire, idempotence (re-run écrase). *BDD end-to-end*.
| ✅ TERMINÉ (S-029, 13 cucumber) | 2 pts

| SLD-5.6
| Dogfooding — les 3 decks demo fr/en/ar (S-020) étendus à 10 langues via `translateDeck`. Vérification visuelle (Playwright) sur au moins 2 decks traduits (1 LTR en + 1 RTL ar). Documentation README i18n mise à jour.
| ✅ TERMINÉ (S-030) | 2 pts
|===

_Total_ : 7 US, ~16 pts (7/7 terminées, 16 pts livrés).
_Pattern de référence_ : `slider.repository` (S-025/027) — value objects purs + adaptateur infrastructure + tâche thin.
_Prérequis_ : SLD-3 terminé (✅), i18n-contracts:0.0.2 publié Central (✅).

'''
== EPIC SLD-6 — Detail US

_Vision_ : `SliderManager.kt` est un god-object de 968 lignes contenant 9 nested
objects (Configuration, Prerequisites, Scaffold, Repositories, Plugins,
Dependencies, Extensions, Tasks, Git). Alors que les 6 domaines métier DDD
(`slider.repository`, `slider.capsule`, `slider.rtl`, `slider.i18n`,
`slider.translate`, `slider.translation`) ont déjà été extraits proprement, le
`SliderManager` reste le point d'agglomération résiduel — il mélange Gradle
plumbing, scaffold, configuration, enregistrement de tâches et adaptateur Git.

L'EPIC SLD-6 applique le même pattern DDD déjà utilisé pour `repository`/`capsule`/
`translation` au `SliderManager` lui-même : extraire chaque responsabilité dans un
domaine dédié, baby-step TDD/BDD 3 niveaux (unit → cucumber), jusqu'à ce que
`SliderManager` devienne un thin orchestrator (< 100 lignes) qui ne fait que
déléguer.

_Problématique technique_ :
* 2 warnings deprecation `properties: (Mutable)Map<String, out Any?>` (lignes 82,
  744) — API Gradle obsolète, à corriger dans US-6.2
* `Tasks` object (~398 lignes) — 13 tâches dans un seul bloc, sans cohésion
* `Scaffold` (~184 lignes) — 3 méthodes métier dans une nested class
* `Git` (~57 lignes) — déjà thin adapter vers `slider.repository`, à consolider

_Méthode_ : baby-step TDD/BDD — RED → GREEN → REFACTOR. Trois niveaux de tests
par US : unit (logique pure, zéro Gradle) → functional (GradleTestKit) →
cucumber (BDD end-to-end). English only dans le code, Gherkin, commentaires.

_Contrats de test_ :
* Unit : value objects purs et helpers, zéro dépendance Gradle. Test logique
  scaffold/config/prerequisite isolée.
* Functional : tâches Gradle exécutées via GradleRunner, asserts sur fichiers
  générés et état du build.
* Cucumber : `15_scaffold_decomposition.feature`, `16_config_decomposition.feature`,
  etc. — scénarios BDD par domaine extrait.

_Dépendances_ :
* Aucune nouvelle dépendance externe — refactoring pur, signature API publique
  (`SliderPlugin.apply`) préservée.
* `slider.repository` (S-025/027), `slider.capsule` (S-020), `slider.translation`
  (S-029/030) — domaines déjà extraits, références à nettoyer dans `SliderManager`.

|===
| US | Description | Statut | Estimation

| SLD-6.0
| Cadrage EPIC — vision, découpage US, dépendances. Mise à jour INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine MIAMI.
| ✅ TERMINÉ (S-035) | 1 pt

| SLD-6.1
| Extraction `Scaffold` → `slider.scaffold` (nouveau domaine). 3 méthodes : `scaffoldSlidesIfAbsent` (extraction `slides.zip` + `isSlidesConfigComplete`), `scaffoldSlidesContextIfAbsent` (default `SlidesConfiguration` → YAML), `scaffoldDeckContextIfAbsent` (default `DeckContext` → YAML). Value object `ScaffoldResult` sealed (Created/Skipped/Failed). `SliderManager.Scaffold` devient thin adapter. *Unit tests purs* (zéro Gradle, tmpdir) + *Cucumber `15_scaffold_decomposition.feature`* (scénarios : slides absentes → extracted, slides complètes → skipped, slides-context.yml absent → generated, deck-context absent → generated).
| ✅ TERMINÉ (S-035, 12 unit + 15 cucumber) | 3 pts

| SLD-6.2
| Extraction `Configuration` → `slider.config` (nouveau domaine). `SlidesConfigLoader` (read YAML via `yamlMapper`), `ConfigPath` value object, `YamlMapperFactory` (factory ObjectMapper). Fix deprecation warnings `properties[...]` lignes 80 et 638 (API Gradle obsolète → `findProperty`). `SliderManager.Configuration` → thin adapter. *Unit tests purs* (16 unit) + *Cucumber `16_config_decomposition.feature`* (15 scénarios).
| ✅ TERMINÉ (S-036, 16 unit + 15 cucumber) | 3 pts

| SLD-6.3
| Extraction `Prerequisites` → `slider.prerequisite`. `JavaVersionGuard` (value object + `requireJava23()`). Extraction de la logique pure (parse version + guard) hors du contexte Gradle. *Unit tests purs* + *Cucumber `17_prerequisite_decomposition.feature`*.
| ✅ TERMINÉ (S-037, 9 unit + 9 cucumber) | 1 pt

| SLD-6.4
| Extraction `Extensions` → `slider.extension`. `SliderExtensionRegistrar` (crée DSL `slider {}` + pin RevealJS 5.2.1). `SliderExtension` déménage dans le domaine. *Unit tests purs* + *Cucumber `18_extension_decomposition.feature`*.
| ✅ TERMINÉ (S-038, 9 unit + 9 cucumber) | 2 pts

| SLD-6.5
| Extraction `Repositories` + `Plugins` + `Dependencies` → `slider.wiring` (Gradle plumbing groupé). `GradleWiring` facade (configureRepositories, applyPlugins, configureDependencies). *Functional tests GradleTestKit* (vérifie resolution repo + plugin apply + gem dep).
| ✅ TERMINÉ (S-039, 28 unit + 2 functional + 17 cucumber) | 2 pts

| SLD-6.6
| Extraction `registerAsciidoctorRevealJsTask` + `registerCleanSlidesBuildTask` + `registerDashboardTask` → `slider.revealjs` (cœur métier). `RevealJsTaskRegistrar` (3 tâches Reveal.js). `registerAsciidoctorTask` délégué. *Functional tests GradleTestKit*.
| ✅ TERMINÉ (S-039, 24 unit + 17 cucumber) | 3 pts

| SLD-6.7
| Extraction `registerServeSlidesTask` + `registerVisualTestTask` + `registerInstallPlaywrightTask` → `slider.playwright`. `PlaywrightTaskRegistrar` (3 tâches Playwright/serve). Cohérent avec `PlaywrightFixtureSupport.kt` (S-034). *Functional tests GradleTestKit*.
| ✅ TERMINÉ (S-039, 16 unit + 19 cucumber) | 2 pts

| SLD-6.8
| Extraction `registerAsciidocCapsuleTask` → `slider.capsule.CapsuleTaskRegistrar` (cohérent domaine `slider.capsule` existant S-020). `SliderManager.Tasks.registerAsciidocCapsuleTask` (~30 lignes) délègue au registrar. *Functional tests GradleTestKit* + *Cucumber `22_capsule_task_decomposition.feature`*.
| ✅ TERMINÉ (S-040, 12 unit + 13 cucumber) | 1 pt

| SLD-6.9
| Consolidation `registerPublishSlidesTask` + `Git` object → `slider.repository.PublishTaskRegistrar` + `slider.repository.SlidePublisher`. `Git` object (~57 lignes) déjà thin adapter — déménage dans le domaine `slider.repository`. Fix deprecation `properties[CONFIG_PATH_KEY]` ligne 744 (déjà traité US-6.2 pour 82, ici pour 744). *Functional tests GradleTestKit* + *Cucumber `20_publish_task_decomposition.feature`*.
| ✅ TERMINÉ (S-042) | 2 pts

| SLD-6.10
| `SliderManager` devient thin orchestrator (< 100 lignes). `SliderPlugin.apply()` délègue à `slider.scaffold.ScaffoldService`, `slider.config.SlidesConfigLoader`, `slider.prerequisite.JavaVersionGuard`, `slider.extension.SliderExtensionRegistrar`, `slider.wiring.GradleWiring`, `slider.revealjs.RevealJsTaskRegistrar`, `slider.playwright.PlaywrightTaskRegistrar`, `slider.capsule.CapsuleTaskRegistrar`, `slider.repository.PublishTaskRegistrar`. Suppression `SliderManager` object (remplacé par `SliderPlugin` direct). Vérification : `./gradlew :slider:check` VERT, zéro régression, 428 PASS maintenus.
| ✅ TERMINÉ (S-042) | 2 pts
|===

_Total_ : 11 US, ~20 pts (11/11 terminées — SLD-6.0→6.10 ✅).
_Pattern de référence_ : `slider.repository` (S-025/027) — value objects purs + adaptateur infrastructure + tâche thin.
_Prérequis_ : SLD-0→5 terminés (✅), CNV-6/7/8 terminés (✅), build vert 654 PASS (✅).

'''
== EPIC SLD-7 — Câblage Explicite translateDeck → Capsule

_Source_ : Session 097 — Audit CTO MVP SaaS. Le pipeline `translateDeck` (SLD-5,
10 langues, 425 tests PASS) et le pipeline capsule `generateCapsuleVideo`
(capsule-gradle, 217 tests PASS) sont fonctionnels mais non câblés explicitement.
SLD-7 crée le contrat d'interface slider↔capsule pour le pipeline multi-langue.

_Dépendance_ : SLD-6 terminé, CAP-29 (capsule-gradle).

[cols="1,3,2,2", options="header"]
|===
| US | Description | Pts | Priorité
| SLD-7.0 | Cadrage EPIC — contrat d'interface slider↔capsule pour pipeline multi-langue | 1 | P0
| SLD-7.1 | Tâche composite `translateAndGenerateCapsule` — dependsOn translateDeck → generateCapsule | 2 | P0
| SLD-7.2 | Tests Cucumber — scénario BDD pipeline complet slider→capsule | 3 | P1
|===

_Total_ : 3 US, ~6 pts. ✅ TERMINÉ (Session 041, 2026-07-31).

'''
== EPIC SLD-10 — Slide Content Model & Rendering Validation

_Vision_ : slider génère des slides AsciiDoc → HTML via Reveal.js, mais n'a
**aucun modèle de contenu de slide** qui permette d'évaluer le rendu. Les
validations Playwright existantes (LTR/RTL) vérifient la structure globale
(titre, slides, bounding box) mais pas le contenu textuel individuel.

SLD-10 crée le domaine DDD `slider.content` modélisant le contenu d'une slide
et des assertions de rendu Playwright pour valider que le texte est dans
l'écran, que les notes orateur sont bien rendues, et que la lisibilité est
correcte.

_Périmètre_ :
- `SlideContent` (aggregate root : titre, sous-titres, paragraphes, listes, notes orateur)
- `TextBlock` (value object : texte, position estimée, bounding box)
- `SlideLayout` (value object : dimensions viewport, marges, font sizes)
- `OverflowDetector` (domain service : prédit si un TextBlock dépasse le viewport)
- `AsciidocContentParser` (parseur .adoc → SlideContent, réutilise le pattern
  `AsciidocSpeakerNoteParser` + étend aux titres/paragraphes/listes)
- Assertions Playwright (pures, consomment un snapshot de rendu enrichi) :
  `TextOverflowAssertion` (P0), `NotesRenderingAssertion` (P0),
  `FontSizeAssertion` (P1), `ContrastAssertion` (P1)

_Patterns de référence_ :
* `slider.rtl.RtlSlideAssertion` (object pur, `assertAll(data) → Result`,
  codes P0/P1 enum) — structure des assertions
* `slider.capsule.AsciidocSpeakerNoteParser` (object pur, parseur `.adoc`
  ligne par ligne) — structure du parseur

_Méthode_ : baby-step DDD/TDD/BDD — RED → GREEN → REFACTOR. Trois niveaux de
tests par US : unit (value objects purs, zéro Gradle/Playwright) → functional
(Playwright fixtures + GradleTestKit) → cucumber (BDD end-to-end). English
only dans le code, Gherkin, commentaires.

_Contrats de test_ :
* Unit : value objects purs et domain services, zéro dépendance
  Gradle/Playwright. Test logique overflow/layout/parser isolée.
* Functional : assertions exécutées via Playwright sur les fixtures HTML
  existantes (LTR + RTL), asserts sur `ContentAssertionResult.passed` et
  codes d'échec.
* Cucumber : `24_slide_content_validation.feature` — scénarios BDD par
  assertion (overflow, notes, font size, contrast).

_Dépendances_ :
* Aucune nouvelle dépendance externe — pure extraction de domaine + assertions pures
* Réutilise `slider.rtl.SlideRenderData` (à enrichir ou étendre via nouveau
  `ContentRenderData`)
* Réutilise `slider.capsule.AsciidocSpeakerNoteParser` (pattern de parseur,
  pas d'héritage direct — AsciidocContentParser couvre un périmètre plus large)
* Playwright fixtures existantes (`./gradlew generatePlaywrightFixtures`) pour
  les functional tests

_Non-périmètre_ :
* Pas de LLM (SLD-8/SLD-9 en pause)
* Pas de refacto `SliderManager` (déjà fait SLD-6)
* Pas de nouvelle tâche Gradle (assertions consommées par `visualTest` existant)

|===
| US | Description | Statut | Estimation

| SLD-10.0
| Cadrage EPIC — vision, domaine `slider.content`, découpage US, dépendances. MàJ INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine MIAMI.
| ✅ TERMINÉ (S-049) | 1 pt

| SLD-10.1
| Value object `TextBlock` (texte, position estimée, bounding box) + `SlideLayout` (viewport, marges, font sizes). *Unit tests purs* (zéro Gradle, zéro Playwright).
| ✅ TERMINÉ (S-049, 20 unit) | 2 pts

| SLD-10.2
| `OverflowDetector` (domain service) — prédit si un `TextBlock` dépasse le viewport en fonction du `SlideLayout`. *Unit tests purs* (zéro Gradle, zéro Playwright).
| ✅ TERMINÉ (S-049, 12 unit) | 3 pts

| SLD-10.3
| `AsciidocContentParser` (parseur .adoc → `SlideContent`) — étend `AsciidocSpeakerNoteParser` aux titres `==`, paragraphes, listes `* `, notes `[NOTE.speaker]`. *Unit tests purs*.
| ✅ TERMINÉ (S-050, 34 unit) | 3 pts

| SLD-10.4
| `SlideContent` aggregate root + `ContentAssertionCode`/`ContentAssertionResult`/`ContentAssertionFailure` (codes P0/P1) + `ContentSlideAssertion` object (`assertAll`). Pattern `RtlSlideAssertion`. *Unit tests purs*.
| ✅ TERMINÉ (S-051, 11 unit) | 3 pts

| SLD-10.5
| Assertions Playwright enrichies — `ContentRenderData` (snapshot DTO pur, mirroring `SlideRenderData` RTL) + `ContentRenderAssertion` (object pur, mesure a posteriori, réutilise les 4 codes `ContentAssertionCode` existants) + `ContentPlaywrightAdapter` (extraction Playwright → snapshot, port 4337) + `ContentPlaywrightFunctionalTest` (LTR fixture, réutilise `PlaywrightFixtureSupport` S-034). Nouvelle constante `MIN_CONTRAST_RATIO = 4.5` (WCAG AA). 3 baby-steps TDD : unit (8+9) → functional (1 Playwright). Rapport cadrage : `slider-plugin/.agents/sessions/052-sld-10.5-cadrage.adoc`. Rapport implémentation : `slider-plugin/.agents/sessions/053-sld-10.5-content-render-validation.adoc`.
| ✅ TERMINÉ (S-053, 17 unit + 1 functional) | 3 pts

| SLD-10.6
| Cucumber `24_slide_content_validation.feature` — scénarios BDD : texte overflow détecté (a priori + a posteriori), notes orateur rendues, font size lisible, contraste WCAG valide, slide sans note rejetée. Validation double : `ContentSlideAssertion` (estimation) + `ContentRenderAssertion` (mesure). *BDD end-to-end*.
| ✅ TERMINÉ (S-054, 11 scénarios) | 3 pts
|===

_Total_ : 7 US, ~18 pts. _Pattern de référence_ : `slider.repository` (S-025/027)
+ `slider.rtl` (S-021) — value objects purs + assertions pures + adaptateur
infrastructure. _Prérequis_ : SLD-2/3/6 terminés (✅), build vert 660 PASS (✅).

'''
== EPIC SLD-11 — Expulsion slider.capsule : realignment boundary slider ↔ capsule

_Vision_ : Le domaine `slider.capsule` (4 fichiers production + 8 fichiers test)
est une *fuite de responsabilite* — il fait du travail de capsule (preparation
de script video) a l'interieur de slider. Slider doit s'arreter a produire le
HTML Reveal.js ; l'extraction des notes orateur et la serialisation en
`*-script.txt` sont des preoccupations de capsule, pas de slider.

*Symptomes du probleme* :
* Doublon de modele divergent : `slider.capsule.SlideSegment`/`CapsuleScript`
  vs `capsule.SlideSegment`/`CapsuleScript` (champs differents — capsule ajoute
  `type`/`manimScene`, renomme `segments`→`slides`).
* Format intermediaire proprietaire `=== CAPSULE SCRIPT ===` maintenu des deux
  cotes pour zero valeur ajoutee.
* Couplage artificiel : slider produit `*-script.txt` que capsule re-parse
  immediatement. Capsule peut parser le `.adoc` (ou le HTML genere) directement.
* 3 features Cucumber slider (`9_capsule_feed`, `22_capsule_task_decomposition`,
  `23_translate_and_generate_capsule`) testent une logique qui n'est pas slider.
* 5 classes de test unitaire slider (672 lignes) + 3 classes steps (289 lignes)
  testent une logique qui n'est pas slider.

_Boundary cible_ :
[cols="2,3,3"]
|===
| Boundary | Avant | Apres

| slider produit
| HTML Reveal.js + `*-script.txt`
| HTML Reveal.js seulement

| capsule consomme
| `*-script.txt` + HTML
| `.adoc` source directement (+ HTML existant)

| Format intermediaire
| `=== CAPSULE SCRIPT ===` (.txt proprietaire)
| Supprime (capsule parse .adoc directement)

| `SlideSegment`/`CapsuleScript`
| Doublon divergent (2 modeles, 2 packages)
| Un seul modele cote capsule

| `generateCapsule` task
| slider-side (extrait notes → .txt)
| capsule-side (extrait notes → TTS directement)

| `translateAndGenerateCapsule` task
| slider-side (dependsOn translateDeck + generateCapsule)
| capsule-side (dependsOn translateDeck + extraction interne)
|===

_Inventaire a expulser (slider-side)_ :
* Production (4 fichiers, `slider.capsule`) :
  `AsciidocSpeakerNoteParser.kt` (85l), `CapsuleScript.kt` (68l),
  `CapsuleTaskDomain.kt` (82l), `CapsuleTaskRegistrar.kt` (81l)
* Tests unitaires (5 fichiers, 672l) :
  `AsciidocSpeakerNoteParserTest.kt` (239l), `CapsuleScriptWriterTest.kt` (166l),
  `CapsuleTaskDomainTest.kt` (115l), `CapsuleFeedDemoDeckTest.kt` (102l),
  `CapsuleSmokeRealDeckTest.kt` (50l)
* Steps Cucumber (3 fichiers, 289l) :
  `CapsuleFeedSteps.kt` (109l), `CapsuleTaskDecompositionSteps.kt` (141l),
  `TranslateAndGenerateCapsuleSteps.kt` (39l)
* Features Cucumber (3 fichiers, 290l) :
  `9_capsule_feed.feature` (221l), `22_capsule_task_decomposition.feature` (50l),
  `23_translate_and_generate_capsule.feature` (19l)
* Câblage plugin : `SliderTasks.kt:26-27` (2 lignes `CapsuleTaskRegistrar`)
* Reference documentation : `AsciidocContentParser.kt` KDoc reference a
  `slider.capsule.AsciidocSpeakerNoteParser` (a mettre a jour)

_Inventaire a recevoir (capsule-side)_ :
* `CapsuleManager.parseScript` (lignes 160-224) parse deja le `.txt` — a
  etendre pour parser le `.adoc` directement (nouveau `AsciidocSpeakerNoteParser`
  cote capsule, ou fusion avec `parseScript` existant)
* `CapsuleManager.resolveScriptDir`/`resolveDeckDir` (lignes 226-255) cherchent
  les `*-script.txt` — a faire pointer vers `slides/misc/*.adoc`
* `CapsuleBuildTask`/`CapsuleVideoTask` consomment `CapsuleScript` capsule —
  aucun changement (le modele capsule est deja le bon)
* `CapsuleExtension.sliderScriptDir` → renommer `adocSourceDir` (ou ajouter)
* Tests capsule existants : `capsule.feature` (306l) + `HtmlSectionParserTest`
  + 19 classes test — a enrichir avec les tests expulses de slider

_Contrat de non-regression_ :
* Zero perte de fonctionnalite — `generateCapsuleVideo` doit produire la meme
  sortie video apres migration
* Le pipeline `translateDeck → generateCapsule` (SLD-7) doit fonctionner
  depuis capsule-side apres migration
* Les 3 decks demo (`capsule-feed-demo-{fr,en,ar}-deck.adoc`) doivent produire
  les memes scripts apres migration
* `./gradlew :slider:check` doit rester VERT (tests slider sans `slider.capsule`)
* `./gradlew :capsule:check` doit rester VERT (tests capsule enrichis)

_Dependances_ :
* SLD-6 terminé (✅ — `SliderManager` supprimé, `SliderTasks` thin orchestrator)
* SLD-7 terminé (✅ — `translateAndGenerateCapsule` composite task)
* SLD-10 terminé (✅ — `AsciidocContentParser` reference `AsciidocSpeakerNoteParser`
  dans KDoc, a mettre a jour)
* capsule-gradle stable (195 unit + 22 Cucumber PASS, session 068)
* Aucune dependance cross-borough nouvelle — capsule parse `.adoc` (format
  AsciiDoc standard), pas d'API slider a appeler

_Methode_ : baby-step DDD/TDD/BDD — RED -> GREEN -> REFACTOR. Trois niveaux de
tests par US : unit (value objects purs, zero Gradle) -> functional
(GradleTestKit) -> cucumber (BDD end-to-end). English only dans le code,
Gherkin, commentaires.

_Priorite_ : P1 — dette architecturale, pas de nouvelle feature.

|===
| US | Description | Statut | Estimation

| SLD-11.0
| *Cadrage EPIC* — vision, inventaire a expulser, boundary cible, non-regression.
  Mise a jour INDEX/BACKLOG/PROMPT_REPRISE + table AGENTS.adoc racine MIAMI.
| ✅ TERMINÉ (S-056) | 1 pt

| SLD-11.1
| *Capsule-side : nouveau parseur `.adoc`* — domaine `capsule.feed` (nouveau) :
  `AsciidocSpeakerNoteParser` (port depuis `slider.capsule`), `CapsuleScript`
  modele unifie (utilise le modele capsule existant avec `type`/`manimScene`),
  `CapsuleScriptWriter` (serialise `*-script.txt` pour compat arriere si
  necessaire, ou supprime si capsule consomme directement). *Unit tests purs*
  (port + adaptation des 239l `AsciidocSpeakerNoteParserTest` + 166l
  `CapsuleScriptWriterTest` + 102l `CapsuleFeedDemoDeckTest` cote capsule).
  Baby-step TDD : RED (tests portes) -> GREEN (parseur porte) -> REFACTOR.
| ✅ TERMINÉ (S-058) | 3 pts

| SLD-11.2
| *Capsule-side : tache `extractSpeakerNotes`* — `CapsuleTaskRegistrar` (nouveau,
  cote capsule) register `extractSpeakerNotes` task (group `capsule`, lit
  `slides/misc/*.adoc` → extrait notes → `CapsuleScript` modele capsule).
  Remplace `generateCapsule` slider-side. *Functional tests GradleTestKit*
  (port des tests fonctionnels slider + adaptation).
| ✅ TERMINÉ (S-058) | 3 pts

| SLD-11.3
| *Capsule-side : Cucumber BDD* — `capsule_speaker_notes.feature` (nouveau,
  port des 13 scenarios `9_capsule_feed.feature` cote capsule) + steps portes
  dans `capsule.scenarios`. Tags `@capsule @feed`. *BDD end-to-end*.
| ✅ TERMINÉ (S-058) | 2 pts

| SLD-11.4
| *Capsule-side : pipeline `translateAndExtract`* — tache composite
  `translateAndExtractSpeakerNotes` (dependsOn `translateDeck` →
  `extractSpeakerNotes`). Remplace `translateAndGenerateCapsule` slider-side.
  Capsule devient autonome sur la chaine complete. *Functional + Cucumber*
  (port de `23_translate_and_generate_capsule.feature`).
| ✅ TERMINÉ (S-058) | 2 pts

| SLD-11.5
| *Slider-side : expulsion `slider.capsule`* — suppression des 4 fichiers
  production (`AsciidocSpeakerNoteParser`, `CapsuleScript`, `CapsuleTaskDomain`,
  `CapsuleTaskRegistrar`), des 5 classes test unitaire (672l), des 3 steps
  Cucumber (289l), des 3 features Cucumber (290l). Suppression câblage
  `SliderTasks.kt:26-27`. Mise a jour KDoc `AsciidocContentParser.kt`
  (reference `slider.capsule.AsciidocSpeakerNoteParser` → supprimee).
  *Verification* : `./gradlew :slider:check` VERT, zero regression, tests
  residuels passent (660 - ~55 tests expulses = ~605 tests attendus).
| ✅ TERMINÉ (S-058) | 2 pts

| SLD-11.6
| *Capsule-side : integration `generateCapsuleVideo`* — `CapsuleBuildTask` et
  `CapsuleVideoTask` consomment directement `extractSpeakerNotes` output
  (CapsuleScript capsule) au lieu de `parseScript(*-script.txt)`. Suppression
  `CapsuleManager.parseScript` (devenu mort) ou conservation pour compat
  arriere. *Verification non-regression* : `./gradlew :capsule:check` VERT,
  pipeline `translateDeck → extractSpeakerNotes → generateCapsuleVideo` produit
  la meme sortie video qu'avant. Cucumber `capsule.feature` enrichi.
| ✅ TERMINÉ (S-058) | 3 pts
|===

_Total_ : 6 US, ~14 pts. _Pattern de reference_ : SLD-6 (expulsion SliderManager
→ domaines DDD) — meme methode baby-step, meme garantie zero regression.
_Prerequis_ : SLD-6/7/10 termines (✅), capsule-gradle stable (✅), build vert
slider 660 PASS (✅), build vert capsule 195 unit + 22 Cucumber PASS (✅).
_Risque_ : cross-borough (2 repos modifies en coordination) — mitigé par
non-regression stricte (tests portes avant suppression source).

'''
== EPIC SLD-8 US-8.3 — DeckPipelineGraph (koog orchestration, Session 061 — Cadrage)

_Vision_ : `proposeDeckContext` (step 1/2) et `generateDeck` (step 2/2) sont aujourd'hui
deux tâches Gradle indépendantes, chacune avec son appel LLM direct. Il n'y a **aucune
orchestration** entre les deux : pas de validation du contexte proposé, pas de retry,
pas de branchement conditionnel. L'utilisateur doit manuellement enchaîner
`./gradlew proposeDeckContext` puis `./gradlew generateDeck`.

US-8.3 introduit une **orchestration koog** du pipeline deck via un graphe
`DeckPipelineGraph` qui chaîne les 3 nœuds : `propose-context` → `validate-context` →
`generate-deck`. koog gère la topologie (nœuds, edges conditionnels, state), langchain4j
exécute les appels LLM via `LlmProviderChatModelAdapter` (S-060).

_Principe fondateur_ (KOOG_AGENTIC_PATTERNS.adoc) : *koog orchestre, langchain4j exécute.*

_Domaine DDD cible_ : `slider.pipeline` (nouveau) — value objects purs + graphe koog
+ adaptateur Gradle thin. Zéro leak koog hors du domaine (pattern `slider.translation`).

_State koog_ : `DeckState` (data class Kotlin immutable, `copy()`) —
`subject`, `language`, `authorName`, `authorEmail`, `ragContext`, `deckContextJson`,
`contextValid`, `validationError`, `deckAdoc`, `error`.

_Graphe cible_ :
```
DeckState(subject, language, authorName, authorEmail, ragContext, deckContextJson,
           contextValid=false, validationError=null, deckAdoc="", error=null)

  [nodeStart]
      ↓
  [proposeContext]   ← LLM call via ChatModel (AssistantManager.PromptManager.contextSystemPrompt)
      ↓
  [validateContext]  ← pure validation (champs requis, langue valide, JSON parseable)
      ↓                    ↓
  (valid)             (invalid)
      ↓                    ↓
  [generateDeck]     [nodeFinish avec error]
      ↓                    ↓
  [nodeFinish]       (DeckState.error = validationError)
```

_Edges conditionnels_ :
- `validateContext → generateDeck onCondition { it.contextValid }`
- `validateContext → nodeFinish onCondition { !it.contextValid }` (avec `error = validationError`)

_Patterns koog consommés_ (KOOG_AGENTIC_PATTERNS.adoc) :
- **Pattern 13 (Subgraphs)** : structure composite (validate → generate), `execute()` séquentiel
  comme `KoogAugmentedContextGraph.execute()` (codebase)
- **Pattern 08 (Guardrails)** : `validateContext` est un guardrail pur (pas de LLM) —
  validation input avant l'appel LLM de génération
- **Pattern 05 (Routing)** : edge conditionnel `onCondition { it.contextValid }` entre
  validation et génération

_Références existantes codebase_ (à consulter pendant l'implémentation) :
- `codebase.koog.KoogAugmentedContextGraph` — pattern hybride graphe koog + `execute()` séquentiel
- `codebase.koog.state.AugmentedState` — pattern data class immutable pour state koog
- `codebase.koog.llm.LlmProvider` — fun interface suspend `call(prompt): String`
- `codebase.koog.llm.service.LlmBuildService` — BuildService Gradle exposant `LlmProvider`

_Contraintes_ :
- koog transitif via `codebase-plugin:0.0.5` (pas de `ai.koog` direct dans slider BOM —
  Decision 002). Vérifier que `ai.koog.agents.core.*` est accessible transitivement.
- `ChatModel` langchain4j via `LlmProviderChatModelAdapter` (S-060, déjà câblé)
- DI native Gradle `BuildService` (Decision 001 — pas de PicoContainer)
- `ToolSelectionStrategy.NONE` (pipeline deck = raisonnement pur, pas d'outils)
- `proposeDeckContext` + `generateDeck` tâches existantes préservées (rétrocompatibilité) —
  le graphe est une **nouvelle tâche** `generateDeckPipeline` qui orchestre les 2 étapes

_Dépendances_ :
* SLD-8 US-8.0/8.1/8.2 terminés (✅ — LlmBuildService, AssistantManager câblage, LlmProviderChatModelAdapter)
* `codebase-plugin:0.0.5` mavenLocal (✅ — republié S-060, JAR inclut LlmBuildService)
* `office/pilotage/codebase/KOOG_AGENTIC_PATTERNS.adoc` (✅ — consommé S-061)
* Aucune nouvelle dépendance externe — koog consommé transitivement via codebase

_Méthode_ : baby-step DDD/TDD/BDD — RED → GREEN → REFACTOR. Trois niveaux de tests
par baby-step : unit (value objects purs, mock LLM) → functional (GradleTestKit) →
cucumber (BDD end-to-end). English only dans le code, Gherkin, commentaires.

_Contrats de test_ :
* Unit : `DeckState` (value object pur, invariants), `DeckContextValidator` (validation pure,
  zéro LLM), `DeckPipelineGraph` (graphe koog avec mock LLM — proposeContext retourne JSON
  fixture, validateContext valide, generateDeck retourne adoc fixture). Test des edges
  conditionnels (valid → generate, invalid → finish avec error).
* Functional : tâche `generateDeckPipeline` exécutée via GradleRunner, asserts sur
  fichiers générés (deck-context.yml + deck.adoc). Mock LLM via `-Pollama.baseUrl`.
* Cucumber : `26_deck_pipeline_koog.feature` — scénarios BDD : pipeline complet
  (subject → context → deck), contexte invalide (champ manquant → error, pas de deck),
  LLM échec propose (error propagée), LLM échec generate (error propagée).

_Baby-steps prévus_ (4 étapes TDD) :

[cols="1,3,2,2"]
|===
| Step | Description | Tests | Estimation

| 8.3a
| *`DeckState` + `DeckContextValidator` (value objects purs)* — `DeckState` data class
  immutable (11 champs, invariants `init`), `DeckContextValidator` object pur
  (`validate(deckContextJson): ValidationResult` — sealed Valid/Invalid, vérifie champs
  requis subject/audience/language/slides, langue ISO 639-1 valide, JSON parseable).
  Zéro dépendance koog/Gradle/LLM. *Unit tests purs* (20-25 tests).
| 20-25 unit | 2 pts

| 8.3b
| *`DeckPipelineGraph` (graphe koog)* — `strategy<DeckState, DeckState>` avec 3 nœuds
  (`proposeContext`, `validateContext`, `generateDeck`) + edges conditionnels. Nœuds
  `proposeContext` et `generateDeck` appellent `ChatModel` (mocké en test). Nœud
  `validateContext` délègue à `DeckContextValidator`. Pattern `KoogAugmentedContextGraph`
  (graphe koog + `execute()` séquentiel). Domaine `slider.pipeline`. *Unit tests avec mock
  LLM* (15-20 tests : pipeline complet, contexte invalide skip generate, error LLM propagée).
| 15-20 unit | 3 pts

| 8.3c
| *`GenerateDeckPipelineTask` (tâche Gradle thin)* — `@DisableCachingByDefault`, group
  "slider", inputs : `-Psubject`, `-Planguage`, `-Pauthor.name`, `-Pauthor.email`,
  `-Pai.provider`. Délègue à `DeckPipelineGraph.execute()`. Outputs : `slides/misc/
  <slug>-deck-context.yml` + `slides/misc/<slug>-deck.adoc`. *Functional tests GradleTestKit*
  (5-8 tests : pipeline complet génère 2 fichiers, contexte invalide génère 0 fichier +
  error message, mock LLM via `-Pollama.baseUrl`).
| 5-8 functional | 2 pts

| 8.3d
| *Cucumber `26_deck_pipeline_koog.feature`* — scénarios BDD : pipeline complet (subject →
  context → deck), contexte invalide (champ manquant → error, pas de deck), LLM échec
  propose (error propagée), LLM échec generate (error propagée), vérification fichiers
  générés. *BDD end-to-end* (8-12 scénarios).
| 8-12 cucumber | 2 pts
|===

_Total_ : 4 baby-steps, ~9 pts. _Pattern de référence_ : `slider.translation` (S-029, value
objects purs + adaptateur LLM + tâche thin) + `codebase.koog.KoogAugmentedContextGraph`
(graphe koog + execute séquentiel). _Prérequis_ : SLD-8 US-8.0/8.1/8.2 terminés (✅),
`codebase-plugin:0.0.5` mavenLocal (✅), build vert 1192 PASS (✅).

_Non-périmètre_ :
* Pas de retry/boucle sur `validateContext` (v1 — edge conditionnel simple valid/invalid)
* Pas de Checkpoints/Time Travel (Pattern 15 — hors scope v1)
* Pas de Self-Reflection (Pattern 10 — hors scope v1)
* Pas de modification des tâches `proposeDeckContext`/`generateDeck` existantes
  (rétrocompatibilité — le graphe est une nouvelle tâche `generateDeckPipeline`)
* Pas de RAG pgvector dans le graphe (RAG reste dans les tâches existantes, le graphe
  reçoit `ragContext` en input)

'''

== Fichiers Associes

|===
| Fichier | Role | Type
| `AGENT.adoc` | Directives completes | EAGER
| `.agents/INDEX.adoc` | Dashboard + roadmap + portefeuille | EAGER
| `.agents/SESSION_CHECKLIST.adoc` | Checklist ouverture/fermeture session | EAGER
| `.agents/SESSION_PROCEDURE.adoc` | Procedure 6 etapes fin de session | EAGER
| `.agents/PROMPT_REPRISE.adoc` | Mission session en cours | EAGER
| `.agents/SESSIONS_HISTORY.adoc` | Historique complet des sessions | EAGER
| `.agents/sessions/` | Archives par session | LAZY
| `.agents/archives/` | Archives taches terminees | LAZY
| `../../BACKLOG.adoc` | Resume public du backlog (source de verite = cet INDEX) | LAZY
| `slider-plugin/doc/SLD-1_UPGRADE_REVEALJS_AUTO_ANIMATE.adoc` | Spec technique EPIC SLD-1 | LAZY
|===