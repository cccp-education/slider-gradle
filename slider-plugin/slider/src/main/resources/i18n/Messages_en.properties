# Slider Plugin — Messages (English)
# Base language, all keys defined here

# ── Task Groups ──
task.group.build=build
task.group.generate=generate
task.group.info=info
task.group.setup=setup
task.group.verify=verify
task.group.deploy=deploy
task.group.slider=slider
task.group.translator=translator

# ── cleanBuild ──
task.cleanBuild.description=Delete generated presentation artifacts from the build directory.
task.cleanBuild.cleaned=Cleaned {0} artifacts from {1} (slides.json={2}, images={3}, html={4})

# ── asciidoctorRevealJs ──
task.asciidoctorRevealJs.description=Compile AsciiDoc sources into a Reveal.js HTML presentation.

# ── generateDashboard ──
task.generateDashboard.description=Generate index.html and slides.json listing all Reveal.js presentations.
task.generateDashboard.success=Dashboard generated successfully!
task.generateDashboard.generated=Generated files:
task.generateDashboard.presentations={0} presentation(s) found

# ── serveSlides ──
task.serveSlides.description=Serve slides using the serve package executed via npx.
task.serveSlides.serving=Serving slides via npx serve...

# ── installPlaywright ──
task.installPlaywright.description=Install Playwright browsers (chromium) for visual testing.

# ── visualTest ──
task.visualTest.description=Run Playwright visual snapshot tests on generated slides.

# ── deploySlides ──
task.deploySlides.description=Deploy generated slides to the configured remote repository.

# ── generateCapsule ──
task.generateCapsule.description=Extract speaker notes from AsciiDoc decks and generate a capsule script (consumed by capsule-gradle).
task.generateCapsule.skipped=Capsule script skipped for {0} (no speaker notes found)
task.generateCapsule.written=Capsule script -> {0} ({1} slides)

# ── translateAndGenerateCapsule ──
task.translateAndGenerateCapsule.description=Translate a deck into multiple languages then generate capsule scripts for all translated decks. Depends on translateDeck and generateCapsule.

# ── translateDeck ──
task.translateDeck.description=Translate a deck (deck-context.yml + .adoc) into multiple languages.
task.translateDeck.translating=Translating deck ''{0}'' from {1} to {2} languages
task.translateDeck.provider=Provider: {0}
task.translateDeck.targets=Targets: {0}
task.translateDeck.translated={0} -> {1}
task.translateDeck.failed={0} — {1}
task.translateDeck.summary={0}
task.translateDeck.noTargets=No target languages to translate into. Source deck is already in language: {0}

# ── generateRevealUiMessages ──
task.generateRevealUiMessages.description=Generate Reveal.js UI i18n messages_{code}.js files for all 10 supported languages.
task.generateRevealUiMessages.written={0} Reveal.js i18n message files written to {1}

# ── reportTests ──
task.reportTests.description=Run checks and open the unit test report in Firefox.

# ── reportFunctionalTests ──
task.reportFunctionalTests.description=Run checks and open the functional test report in Firefox.

# ── asciidoctor ──
task.asciidoctor.description=Compile AsciiDoc sources into a Reveal.js HTML presentation (aggregate task).

# ── translateDeck errors ──
translate.error.deckContextNotFound=Deck context file not found: {0}
translate.error.adocNotFound=Deck AsciiDoc file not found: {0}. Specify it with -Pdeck.adoc=<path>
