[versions]
slider = "0.0.14"
readme = "0.0.1"

[libraries]

[plugins]
slider = { id = "education.cccp.slider", version.ref = "slider" }
readme = { id = "education.cccp.readme", version.ref = "readme" }

[bundles]
